# P0/P1 World's-Longest Coaster Implementation Review

- Date: 2026-06-19
- Reviewer: independent external reviewer / human judge
- Review target: P0 (behavior-preserving C++ refactor) + P1 (offline track generator + clearance/comfort verifier), per `docs/plans/worlds-longest-coaster.md`
- Reviewed artifacts: `CoasterTrackComponent.h/.cpp`, `CoasterBanking.h`, `CoasterRideActor.cpp/.h` (diff), `scripts/generate-yarlung-track.py`, `scripts/verify-track-clearance.py`, `scripts/yarlung_track_lib.py`, `Content/Generated/YarlungLandscape/YarlungTrack.csv`, `manifest.json` track block, `Saved/Diagnostics/track-clearance.csv/.png`, `Saved/Diagnostics/yarlung-track-overlay.png`, `Saved/OffscreenShots/p0-p1-smoke.png`, `docs/plans/worlds-longest-p0-p1-implementation.md`
- Verdict: PASS (conditional) — may proceed to P2 on the geometry/clearance axis; ride comfort and scenery are NOT verified and must not be marked Done.
- Related phase/task: world's-longest repositioning P0/P1; closes the non-piercing blocker of `2026-06-19-a2-track-adapts-v1.md`.

## Summary

P0 and P1 are genuinely implemented and honestly reported (Codex explicitly
flags the un-done parts: no frame-by-frame pixel diff, P1 lat-G gate loosened to
3.8, no P2 runtime integration). The previously-open blocker — whole-loop
non-piercing — is now closed with real evidence (min clearance 22.49m, zero
violations, clean plot). The work is sound enough to proceed to P2. However, two
things the repositioning explicitly cares about are NOT verified: **ride
comfort** (the verifier's comfort gates are a crude/partly-stubbed geometric
proxy) and **scenery** (the #1 non-length priority — cannot be confirmed from the
washed-out overlay).

## Verified (evidence-backed, not over-claimed)

1. **Track length 5026.22m** — breaks the Steel Dragon 2000 record (2,479m), hits
   the ~5km target. 90 control points (within the 80–160 estimate).
2. **Whole-loop non-piercing** — `track-clearance.csv`: min clearance 22.49m over
   720 dense samples, 0 violations, 0 samples < 2m; plot stays 22–85m, never near
   the floor. **This closes the FAIL blocker from `a2-track-adapts-v1`.**
3. **Verifier is a real hard gate** — `verify-track-clearance.py:232` `if
   violations: sys.exit(1)` (and `sys.exit(2)` on malformed input). Not cosmetic.
4. **P0 refactor is faithful and behavior-preserving by inspection** — the
   `CoasterRideActor.cpp` diff delegates `RebuildSpline`/`SampleFrame`/
   `GetSectionName` to `UCoasterTrackComponent`; banking is the exact legacy
   sinusoid `28°·sin(ratio·2π·3)` moved verbatim into `CoasterBanking`. Actor is
   integrated (uses the component, not orphaned); god actor shrank (~-37 lines).
5. **Honesty** — no inflated claims; un-done parts are stated.

## Blocking Issues (conditions — must be resolved before claiming comfort/scenery Done; do NOT block P2 start)

1. **Comfort gate is a crude/partly-fake proxy — "ride comfort" is NOT verified.**
   - `verify-track-clearance.py:180` `est_long_g` is clamped to
     `max_long_g + 0.01`, so it can never meaningfully exceed the threshold — the
     longitudinal-G gate is decorative and the reported `max_long_g=0.469` is
     meaningless. Real longitudinal G is `dv/dt`, only available from the runtime
     energy model.
   - `est_lat_g` (line 179) uses a constant `design_speed=22 m/s` everywhere,
     ignoring the real speed profile. It overestimates lateral G in slow zones
     (the 13.7m-radius Station curve reads 3.6G though the train is crawling
     there) and underestimates on fast sections. The "11 samples > 1.7" is partly
     false; the gate can also miss real high-speed lateral.
   - The 3.8 draft threshold masks this crudeness. Acceptable for a P1 *draft*
     (user approved "急一点"), but it is NOT a comfort verification. True comfort
     must come from P3 runtime telemetry with actual per-point speed.
2. **Scenery is unverified — and scenery is the #1 non-length priority.**
   `yarlung-track-overlay.png` is so washed out (the naturalized `.r16` is
   heavily smoothed) that the river channel is not visible, so it is impossible
   to confirm the track actually hugs the real river ("贴江如画"). Non-piercing is
   proven; scenic routing is not. Needs a higher-contrast / raw-hillshade /
   river-mask overlay for a real human check before scenery can be judged.

## Non-Blocking Suggestions

1. **min radius 13.7m is unrealistically tight** for a rideable coaster; with the
   crude lat-G this is a draft roughness P3 must smooth via banking / speed tuning
   / turnaround reshaping.
2. **P0 zero-regression not pixel-verified** — code is provably equivalent (low
   risk), but the spec exit criterion was "frame-identical to pre-refactor" and
   only "smoke renders non-empty" was done. Cheap to close: diff a pre/post hero
   frame.
3. **Spec §6.0 partially done** — `DistanceToTrack2D` dual representation not
   removed (`YarlungCoasterProfile.h` unchanged). Acceptable scope-trim; track for
   P2/cleanup so the runtime stops carrying two track representations.
4. **Generated artifacts uncommitted** — `YarlungTrack.csv` (the single-source-of-
   truth generated asset) and the new scripts are in the working tree; commit per
   the project's generated-assets-committed convention.

## Required Next Action

P2 (runtime CSV integration) may begin. Before comfort/scenery can be marked
Done: (a) fix the `est_long_g` stub and make lateral-G use a real per-point speed
profile (or defer the comfort verdict explicitly to P3 runtime telemetry), and
(b) produce a legible overlay proving the track follows the real river, then have
a human confirm scenery. Keep the 13.7m radius / tight kinks on the P3 list.

## Agent Disposition

- Status: Open (conditional pass)
- Progress link: `docs/plans/photoreal-progress.md`
- Follow-up evidence: pending (P3 runtime G telemetry; legible scenery overlay; optional P0 frame diff)
