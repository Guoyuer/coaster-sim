# 照片级迭代进度（活文档 — 每轮迭代必须更新）

> 这是断点续跑的状态文件。任何 agent 开工前先读这里确定"现在在哪、下一步做什么"，收工前写回。
> 流程：`photoreal-overhaul.md` · 验收：`photoreal-acceptance.md` · 操作：`AGENTS.md`

## 当前状态
- **当前阶段**：阶段 A/B 交叉 + 世界最长轨道重定位 P0–P3 已闭环；A1 Done；A2 **参数层已验收通过**；**B2 颜色（橙黄）+ 曝光收尾均已验收通过**；P3 ride-experience / D9 airtime / powered-coaster 物理可行性已按外审重做 PASS。当前挡住照片级 D1 的"梯田台阶"**已查清不是廉价 bug**：`.r16` 是真 16-bit(~62k unique)、上采样是 bilinear+自适应 box-blur、非 posterize。**真根因 = 单值 heightfield + 30m Copernicus 源分辨率 + bilinear 只 C0 不 C1 的法线折痕 + 裸 macro 无 detail normal** 的固有上限，在陡崖掠射第一人称视角被最大化暴露。**这不是能"修好"的 bug，是表示方法的天花板。** 2026-06-19 新思路已落两刀：撤掉 cliff fascia + 软化近崖坡、再加独立 `AYarlungSceneryActor` 做走廊级岩石/林下实例散布。数据/净空/构建/inspect 均 PASS，但英雄帧仍明显阶梯；且本地没有真实树/灌木资产，现有 boulder proxy 不能读成林芝密林。**2026-06-19 用户新增硬门槛：整条过山车第一人称可视范围内都不允许出现台阶；可使用 Nanite。** 这把近景台阶从 "deferred" 升级为 C 阶段硬 gate：C2 已开始把运行时可见地形从 Landscape 切到 Nanite StaticMesh；旧 Landscape LOD/normal 台阶路径已结构性绕开，但 2017 grid 高度场仍读成大三角折面，**C 仍不能标 Done**。**2026-06-20 另修掉一处独立 defect（非台阶）：第一人称帧两侧的竖向"雨帘"查清是支撑柱——轨道是峡谷高架（平均高出地形 157m），旧代码每 7.2m 落一根 9cm 细柱到谷底，~3515 根糊成竖线幕；已改为每 72m 一座按高度缩放粗细的纪念碑式桥墩（~70 座），雨帘消失，详见 `support-piers-v1`；随后又把桥墩脚埋到 `terrain_z-6000cm` 消除浮脚。**另查清"画面偏暗"是 C2 换地形的隐性回归（非光照 bug）：太阳已是 120000 lux 物理日光、曝光正确（天空不过曝），但新 Nanite 地形用暗程序化顶点色反照率（forest 0.06 等），对着 B2 当年标定的亮曝光渲成近黑；已在 `M_YarlungMeshTerrain` 加 `TerrainBrightness=1.6` 乘子抬反照率回到白天，未动天空/曝光，详见 `support-piers-bright-v1`。这两刀都与 C2 折面无关，地形天花板仍 Open。**
- **基线**：当前为原型/灰盒（顶点色假天空/假河、Cube 轨道、程序化解析地形、单张 macro 平涂地表）。基线截图：`Saved\hero-baseline.png`（2560×1440，`WaitSeconds 12`）
- **英雄段时间点**：`WaitSeconds 12`。理由：六张候选图（3/6/9/12/15/18s）中，12s 同框包含第一人称轨道、山谷/河道走廊、对岸坡面与远处山形，最接近“临江高弯 + 远中景”验收目标。
- **参考锚点**：`docs/refs/references.md`（外链，不下载入库；逐文件 license 验证后才可提交图片）
- **下一步动作（按顺序，已从便宜牌转入 Nanite/StaticMesh 根治线）**：
  1. ✅ **决定性诊断完成**：`python scripts\dump-yarlung-height-profile.py` 已沿陡崖自动 dump `.r16` 剖面到 `Saved\Diagnostics\yarlung-height-profile-*.csv/.png`。最陡 profile：`x=21-116, y=636`，96 点全唯一、636m relief、仅 3 对 `<15cm` 近平台段，**不是 8-bit/posterize 那种硬平台阶梯**；但相邻高度差呈分段重复/坡度拐点（`mean_abs_d2≈0.55m`），符合 **bilinear C0 连续、C1 不连续导致法线折痕**。
  2. ✅ **B-spline 上采样完成并验证**：`generate-yarlung-landscape-assets.py:sample_cm` 已从 bilinear 改为 cubic B-spline（比 Catmull-Rom 更稳，不追求过点，优先 C2 平滑）。同一剖面 `x=21-116,y=636`：`kinks_gt_1_5m 10→2`、`mean_abs_d2 0.552→0.423`、`flat_pairs 3→0`。数据层 cheap fix 有效。
  3. ✅ **P3 外审阻断闭环**：`p3-powered-cruise` 已把车辆明确改为 powered coaster：有界 drive 在主线路维持巡航/上坡补能，Turnaround 有界控速，Brake/Station 才允许停车；验证 `vert=-0.89/3.07G`、`airtime_samples=6@<=-0.30G`、主线路 `low_speed_samples=0`、`stall_samples=0`、0 violations。P3 可标 Done。
  4. ✅ **P4 风景 overlay + river-mask 对齐完成**：新增 `scripts\diagnose-yarlung-scenery-overlay.py`，输出 `Saved\Diagnostics\yarlung-scenery-overlay.png`、`Saved\Diagnostics\yarlung-scenery-overlay-crop.png`、`Saved\Diagnostics\yarlung-scenery-overlay.csv`。诊断先确认根因不是轨道远离真谷底，而是旧 macro river mask 没贴 DEM thalweg；随后 `generate-yarlung-landscape-assets.py` 改为从 naturalized DEM 提取 river guide，并把 river mask 半宽从 700m 收窄到 260m。复验：`river-mask samples 89/198(44.9%)→187/198(94.4%)`，`macro-mask center offset med 670m→137m`，`DEM thalweg offset med/p90/max=110/148/149m` 保持不变（轨道未被乱动）。
  5. ✅ **D/P4 江水迁出 v1 完成**：新增 `YarlungRiver.csv` 与独立 `AYarlungRiverActor`，commandlet 生成关卡时放置 `YarlungRiverScenery`；`ACoasterRideActor` 旧解析河 ribbon/foam/rapids/mist 组件和构建函数已删除，ride actor 只保留车/轨/支撑/石头等运行时职责。水体读取 DEM thalweg river samples，生成独立 procedural water mesh + foam mesh，材质资产 `M_YarlungRiverWater/Foam`。验证：UE build/import PASS；inspect 报告 `Saved\Diagnostics\yarlung-map-inspect.txt` 确认关卡 3 actors 且只有 `YarlungRiverScenery` 拥有 `WaterMesh/FoamMesh`；运行时日志 `Loaded Yarlung river CSV: 213 samples`；截图 `Saved\OffscreenShots\p4-river-actor-v2.png` 已 Read，水面进入英雄帧且与谷底/轨道同源。限制：仍是 v1 平带/简单透明材质，D3 不能标 Done；近景 Landscape 台阶仍归 C。
  6. ✅ **外审 finding 跟进：支撑/雾锚点改真数据**：针对 `river-actor-migration` 非阻断 Finding，`UCoasterTrackComponent` 现在读取并按距离插值 `YarlungTrack.csv terrain_z`，`ACoasterRideActor` 支撑脚改用该真实地形高度，不再落到旧 `RiverZCm=2652m`；谷雾改读 `YarlungRiver.csv` 平均水面 Z（日志 `Anchored valley fog to generated Yarlung river average Z: 267655.0cm`，inspect `fog_component=ValleyFog relative_z_cm=267725.0`）。验证：build/import/inspect/offscreen `Saved\OffscreenShots\p4-real-anchors-v1.png` PASS/已 Read。限制：旧 `YarlungRiverCenterY/YarlungLandscapeHeight` 仍服务于 `BoulderOutcrops`，且当前 inspect 显示 boulder instances=0；完整 scatter/旧影子地形退役仍归 A3/C，不在本小步冒称完成。
  7. ✅ **D/P4 水体材质 groundwork v1**：`M_YarlungRiverWater/Foam` 改为 ProceduralMesh vertex color 驱动 base color；`AYarlungRiverActor` 水面从 5 条横向采样细分到 11 条，增加轻微岸线扰动/中心色带/复合 ripple；泡沫从 4 lane 增到 7 lane，并按岸边权重变化宽度/颜色。验证：C++ build PASS；`import-yarlung-landscape.ps1 -Build -ForceMaterials` PASS；inspect PASS；offscreen `Saved\OffscreenShots\p4-water-vertex-v1.png` 已 Read。肉眼结论：水面/泡沫层次比平带稍强、无回退；但仍是 translucent procedural ribbon，缺真正 flow normal / depth fade / reflection-refraction，且近景 Landscape 台阶仍是主短板，D3 不能标 Done。
  8. ✅ **C1 分层 Landscape 材质 groundwork v1**：`create_landscape_material()` 不再只接单张 macro albedo/roughness + 错配 AO；现在用 macro mask 的 forest 通道在 `AerialGrassRock` 与 `LeafyGrass` 两套 PolyHaven PBR 之间混合，接入 detail BaseColor/Normal/Roughness/AO，并用独立 detail Landscape coords 做宏观/微观尺度分离。验证：`import-yarlung-landscape.ps1 -Build -ForceMaterials` PASS；inspect PASS；offscreen `Saved\OffscreenShots\c1-landscape-detail-v1.png` / `c1-landscape-detail-v2.png` 已 Read。肉眼结论：材质层次/粗糙度略有变化，无明显 moire 回归，但近景右崖规则台阶几乎不动，证明 **detail-normal-only 不能根治几何台阶**。
  9. ✅ **C1 走廊级 cliff mesh 接管 v1**：新增独立 `AYarlungCliffActor` 与 `M_YarlungCliffRock`，commandlet 生成 `YarlungCliffScenery`，从 `YarlungTrack.csv` 的 Outbound 段和 `YarlungTsangpo_1009.r16` 生成轨道视线侧 cliff fascia。首版共 `363` 个沿线样本、`3993` 顶点、`7240` 三角，材质吃 procedural mesh vertex color + rock normal/rough/AO。验证：C++ build PASS；`import-yarlung-landscape.ps1 -Build -ForceMaterials` PASS；inspect PASS（map `actor_count=4`，ride actor 未回退旧 scenery，cliff actor 独立存在）；offscreen `Saved\OffscreenShots\c1-cliff-fascia-v1.png` 已 Read。
  10. ✅ **C1 gated follow-up 已试并终止**：按外审 `stage-c-cliff-and-detail` 的唯一允许方向，临时试过 flush/no-pull/低 lift/真几何法线版（截图 `c1-cliff-flush-v1/v2` 已 Read），结果右崖出现大片黑斑/破碎，视觉比 `c1-cliff-fascia-v1` 更假。该失败试验已撤回不提交；结论=不要再把轮次耗在 cliff 表皮上，按用户既定出口转 D/E。
  11. ✅ **D water alpha note 处理**：`create_translucent_vertex_color_material()` 现在把 `VertexColor.A` 接到 `Opacity`，让 `AYarlungRiverActor` 已计算的水面/泡沫顶点 alpha 真正参与渲染。验证：py_compile PASS；`import-yarlung-landscape.ps1 -Build -ForceMaterials` PASS；inspect PASS；offscreen `Saved\OffscreenShots\d1-water-alpha-v1.png` 已 Read。肉眼变化很小、无回退；这是关闭外审 note 的材质管线修正，不标 D3 Done。
  12. ✅ **台阶新思路第一刀：撤 fascia + 软化 Outbound 崖坡**：按用户拍板「换思路、别复杂化」，删除 `AYarlungCliffActor`/`YarlungCliffScenery`/`M_YarlungCliffRock`；`generate-yarlung-landscape-assets.py` 新增 Outbound guide slope softening（manifest：`affected_samples=45764`、`max_lowered_cm=30811.652`、`mean_lowered_cm=7824.012`），并把 forest mask 从离江 900m 起改到 260m 起。严格按顺序重生 terrain→`YarlungTrack.csv`→clearance：`violations=0`、`min_clearance=42.40m`。验证：py_compile PASS；UE build/import/inspect PASS（map `actor_count=3`，无 cliff actor）；截图 `c1-soft-slope-v1` 已 Read。肉眼结论：旧表皮撤干净，数据层软化有效，但英雄帧右侧仍有大块 heightfield 阶梯，C/D1/D5 不过。额外试 `MaxLODLevel=0/ForcedLOD=0` 截图 `c1-soft-slope-lod0-v1`，反而暴露原始网格块，已撤回不提交；剩余问题不是继续调 LOD0 能解决。
  13. ✅ **台阶新思路第二刀：独立 full-density scatter 管线已落，视觉未过**：新增 `AYarlungSceneryActor`，commandlet 生成 `YarlungForestRockScenery`，从 `YarlungTrack.csv` 的 Outbound 段和 `.r16` 真 heightmap 采样高度/法线，生成 `RockOutcrops` + `UnderstoryClumps` 两组 HISM；inspect 硬门槛要求唯一 scenery actor 且总实例 ≥4000。验证：UE build PASS；`import-yarlung-landscape.ps1 -Build -SkipAssetGeneration -SkipMaterials -Verify` PASS；inspect `actor_count=4`，`RockOutcrops instances=2007`，`UnderstoryClumps instances=11000`，无 `YarlungCliffActor`。截图 `Saved\OffscreenShots\c1-full-scatter-v1.png` 已 Read。肉眼结论：散布确实进入英雄帧、岩石贴坡，但无法遮住/消除右侧 heightfield 台阶；本地仅有 boulder mesh，无真实树/灌木资产，绿色 boulder proxy 读成暗块/噪点而非林芝密林。**C 不能标 Done；若继续 C，需要外部/下载的真实植被与岩壁资产（Fab/Megascans/CC0），否则按既定 fail-exit 转 D/E（水、轨道/车、成像）并把近崖真根治记为 Nanite/mesh terrain deferred。**
  14. 🟨 **D water energy groundwork v1**：`AYarlungRiverActor` 新增 `RiverEnergy()`（由 DEM river sample 的局部坡度、曲率和 flow 相位合成），用它驱动水面 milky tint、微起伏法线倾斜、泡沫 lane 宽度/alpha；没有新增独立遮挡几何，仍只在 `WaterMesh/FoamMesh` 内工作。验证：UE build PASS；`import-yarlung-landscape.ps1 -Build -SkipAssetGeneration -SkipMaterials -Verify` PASS；inspect 确认 river actor 只有 `WaterMesh/FoamMesh`，无回退旧 ride river 组件；截图 `Saved\OffscreenShots\d1-river-energy-v1.png` 已 Read。肉眼结论：比平带略多乳白/泡沫变化，无明显回退；但仍缺真实 flow normal texture/depth fade/reflection-refraction，且右崖台阶继续压画面，D3 不过。过程记录：试过独立 `RapidsMesh` 大白水 patch（`d1-rapids-patches-v2/v3`），读成半透明几何色片，已撤回不提交。
  15. 🟨 **全轨道台阶风险诊断 v1**：新增 `scripts\diagnose-yarlung-staircase-risk.py`，沿 `YarlungTrack.csv` 全环采样第一人称走廊横剖面，按近轨陡坡的一阶坡度与二阶折痕给台阶风险排序并输出 `Saved\Diagnostics\yarlung-staircase-risk.csv`。验证：`python -m py_compile scripts\diagnose-yarlung-staircase-risk.py` PASS；`python scripts\diagnose-yarlung-staircase-risk.py --fail-risk 1.01` 输出 `samples=792`、worst `sample=2 distance_m=8.47 section=Station risk=1.000 side=right second_cm=4525.54 slope=1.215`。限制：这是排序/回归诊断，不替代截图验收；默认 `--fail-risk 0.72` 会把当前 baseline 判 fail。过程记录：试过 procedural corridor terrain replacement（单 mesh/strip/禁阴影/emissive），截图 `c2-corridor-terrain-*` 均出现黑色遮挡和拼布感，比原台阶更假，已撤回不提交。结论：不要再做 procedural overlay；下一刀必须是正经 StaticMesh/Nanite 地形接管，并处理底层 Landscape 可见性/接缝。
  16. 🟨 **C2 Nanite mesh terrain takeover v1：Landscape 台阶路径已绕开，视觉仍 FAIL**。commandlet 现在从 post-clearance-cut heightfield 构建 `/Game/Generated/YarlungLandscape/SM_YarlungMeshTerrain`，2017×2017 grid（4,068,289 verts / 8,128,512 tris），cubic B-spline 采样 + 自定义中心差分法线，启用 Nanite（keep/fallback 100%）；关卡生成 `YarlungMeshTerrainScenery`，运行时可见组件为 `StaticMeshComponent`，并把 `YarlungTsangpoLandscape` 设为 hidden in game。新增 `M_YarlungMeshTerrain` 为 vertex-color opaque 材质，启用 StaticMesh+Nanite usage。验证：UE build PASS；`import-yarlung-landscape.ps1 -SkipAssetGeneration -SkipMaterials -SkipModels -Verify` PASS；inspect 确认 hidden Landscape + 可见 StaticMesh terrain；offscreen `Saved\OffscreenShots\c2-nanite-terrain-v5-customnormals.png` 已 Read。肉眼结论：旧 Landscape LOD/normal 横向台阶路径已结构性移除，但 30m heightfield 上采样成单张 Nanite mesh 后仍有大三角/低模山体折面，且材质偏暗、proxy scatter/支撑线噪声明显；**全第一人称无台阶/无折面硬 gate 仍 Open，C 不能标 Done**。过程记录：`c2-nanite-terrain-v4-smoothnormals` 的重算法线试验把材质洗白，已撤回；下一步应做 chunked/更高密度走廊 mesh、Nanite displacement/岩壁资产化，或把近景崖改为真正 author rock/vegetation asset，而不是回到 Landscape LOD 调参。
  17. 🟨 **C2 sub-30m rock displacement v1：已执行“脱离 DEM”第一刀，但仍未过 gate**。在同一张 Nanite terrain mesh 内加入轨道可视走廊 rock mask：按最近轨道距离、坡度与高度权重，对 DEM 宏观地形叠加程序化 sub-30m strata/fracture/ridged displacement；同时 `M_YarlungMeshTerrain` 改接 `AerialGrassRock` detail normal/roughness/AO，不再只是 vertex-color 平涂。验证：`python -m py_compile scripts\create-coaster-materials.py scripts\inspect-yarlung-map.py` PASS；UE build PASS；`import-yarlung-landscape.ps1 -SkipAssetGeneration -ForceMaterials -SkipModels -Verify` PASS；commandlet 日志 `displaced_vertices=1,879,931`、`max_displacement_cm=4938.3`、`nanite=true`；inspect 仍确认 hidden Landscape + 可见 StaticMesh terrain；offscreen `Saved\OffscreenShots\c2-sub30-rock-displacement-v2.png` 已 Read。肉眼结论：比纯 vertex-color DEM mesh 多了岩面噪声/法线细节，但大折面仍主导中上部山壁，视觉仍 FAIL；**全第一人称无台阶/无折面 gate 仍 Open，C 不能标 Done**。下一步不要再只加连续 heightfield 位移；要么做 chunked corridor mesh/局部 author cliff assets，把最坏风险段替换成非 heightfield 岩壁体块，要么引入真实岩壁/植被资产。
  18. ❌ **C2 authored terrain patch / vertex reshape 试验已否决，不提交运行时改动**。按 reviewer 指令试过两条本地实验：a) 在同一 Nanite terrain 内按 `yarlung-staircase-risk.csv` 风险节点挖掉底层 terrain 三角，再用 authored cliff patch 补面；b) 不挖洞，只按风险节点重塑 heightfield 顶点。验证均已跑 C++ build / import / offscreen，并 Read 截图：`c2-author-cliff-replacement-v1.png`/`v2.png` 出现右侧 sky hole、直边蓝面与竖向条纹；`c2-author-cliff-vertex-reshape-v1.png` 把第一人称相机/轨道埋进浅色地形。结论：**程序化 patch/heightfield reshape 在当前生成器里会破坏第一人称，不能作为 C2 通过路径**；本轮代码与资产已回退到 `c2-sub30-rock-displacement-v2` 状态。
  19. 🟨 **C2 材质/光诊断 + 迭代解耦 v1**：按 2026-06-20 reviewer 新文档，先暂停 chunk，验证“裸暗几何是否误导判断”。临时试过独立 Nanite chunk：`c2-nanite-cliff-chunk-v1/v2` 是连续幕墙/假 slab，`c2-nanite-rock-buttress-v1` 是离散闭合岩体但仍出现右侧巨大多面体/亮面块，均已 Read，判 FAIL 且不提交运行时改动。随后在现有 Nanite terrain displacement 上改 `M_YarlungMeshTerrain`：加入 `AerialGrassRock` diffuse 与 vertex color 混合、正常日光曝光（`AutoExposureBias=1.65`、SkyLight=4.0，不碰 SkyAtmosphere 散射），并截图 `c2-rock-material-light-v1/v2`。结论：材质/光让判断更清楚，但**台阶/大折面仍主导**，不是单靠材质/曝光能过 gate；chunk 路线暂停。为加快后续循环，`scripts/import-yarlung-landscape.ps1` 新增 `-SkipMapImport`：材质/光小改只跑材质生成+existing-map verify，不再触发 208MB `SM_YarlungMeshTerrain` 重建；实测 `-SkipAssetGeneration -ForceMaterials -SkipModels -SkipMapImport -Verify` 约 25.8s，无 `Built Nanite Yarlung mesh terrain` 日志。
  20. 🟨 **C2 map/scenery 迭代解耦 v2**：新增 `YarlungLandscapeImport` commandlet 参数 `-SkipTerrainMeshBuild`，在仍重建 `.umap` / actor placement / scenery 的情况下复用现有 `/Game/Generated/YarlungLandscape/SM_YarlungMeshTerrain`，避免每次 map/scenery 小改都重烤 208MB / 4.07M verts / 8.13M tris Nanite terrain。实测 `-SkipAssetGeneration -SkipMaterials -SkipModels -SkipTerrainMeshBuild -Verify`：commandlet 日志 `Reusing existing Yarlung mesh terrain`，commandlet 本体 1.57s、峰值物理内存 1787MB、script map-import step 34.1s、verify 12.9s，且无 `Built Nanite Yarlung mesh terrain` 日志。适用范围：只改 actor placement、river/scenery/light/camera/postprocess 等关卡内容，且 heightfield、track clearance、terrain mesh displacement/vertex color 未变。这是迭代速度工具，不改变 C2 视觉结论，`c2-skip-terrain-mesh-build-smoke` 已 Read，台阶/大折面 gate 仍 Open。
  21. ❌ **C2 adaptive terrain midpoint tessellation 已试并否决，不提交运行时改动**。针对用户判断“asset 效果一般，是否改地形 mesh 几何本身”，本轮直接改 `SM_YarlungMeshTerrain` 生成拓扑：在 rock/corridor mask 命中的 quad 加中心点，约 `adaptive_centers=1,155,493`，三角 `8.13M→10.44M`，Nanite build 峰值约 `16.1-16.6GB`。两版均 Build/import/verify/offscreen 并 Read：`c2-adaptive-terrain-midpoints-v1` 保持原 49m 级程序化位移，出现更明显菱形/假岩面；`v2` 把位移降到 2.2-14m 后，主山体大三角/竖切面仍主导且更像低模折纸。结论：**要改的是表示方法，不是继续加密同一张规则 heightfield mesh**；主 terrain midpoint tessellation 不能作为 C2 通过路径。本轮代码和生成资产已回退到 HEAD。下一步若继续 C2，只能走非 heightfield 的 author cliff/rock/forest wall（真实资产/手工雕/外援），或据实转 D/E，不能把主 terrain 再细分当根治。
  22. ✅ **C2 relief UT + 重构护栏 v1**：为继续大改山体前先加防回归网。新增纯 C++ `YarlungTerrainProfile`（高度编码范围、河道中心线、Smooth01）和 `YarlungTerrainRelief`（sub-30m relief 计算 + 法线位移），把 commandlet 内嵌的 coherent noise/fBm/ridged/relief 算法迁出；`ACoasterRideActor` 与 commandlet 共用同一河道中心线，避免运行时/生成器参数漂移。新增 UE Automation tests `CoasterSim.Yarlung.TerrainRelief.*` 覆盖：平地/河道保护、近轨正向 relief 抑制、确定性与幅度边界、法线方向位移。验证：UE C++ build PASS；Automation `Found 4 automation tests` 且 4/4 `Result={Success}`，`TEST COMPLETE. EXIT CODE: 0`。本项只提升可测性/可维护性，不改变当前视觉裁决；C2 台阶/大折面 gate 仍 Open。
  23. 🟨 **C2 first-person view-corridor relief gate v1：预算方向修正，视觉仍 FAIL**。针对用户指出“模糊带不该沿河，而应沿过山车第一人称视角覆盖”，新增基于 `YarlungTrack.csv` 的近似第一人称视锥走廊 mask：按轨道切线、前向距离、水平视场和侧向 fade 对全环取 union；`YarlungTerrainRelief::ComputeReliefCm()` 现在用该 mask 只在相机可能扫到的走廊内加强 cliff-fold/sub-30m relief，river band 只保留为河道保护/排除，不再当视觉预算中心。`scripts/preview-terrain-relief.py` 同步生成 `Saved\Diagnostics\terrain-relief-preview-viewcorridor.png`，本轮 `view_mask_coverage=57.9%`。验证：`python -m py_compile scripts\preview-terrain-relief.py` PASS；UE C++ build PASS；Automation `Found 5 automation tests` 且 5/5 `Result={Success}`；全量 terrain rebuild PASS，日志 `vertices=4068289 triangles=8128512 displaced_vertices=3814991 view_corridor_vertices=2355944 max_displacement_cm=10437.6 nanite=true`；offscreen `Saved\OffscreenShots\c2-viewcorridor-relief-v1.png` 已 Read。肉眼结论：山体预算确实跟随第一人称走廊，但画面仍读成低模折纸/大块山体幕墙，C2 硬 gate 仍 Open。下一刀不应继续叠噪声，而应做**宏观山体几何重建/自然化**：把英雄段和高风险走廊的近中景山墙从“单值 30m heightfield 外推”改成照片向的 talus slope + buttress ridge + ravine/gully 组合，保持轨道净空和河谷大轮廓。
  24. ✅ **宏观地貌重建前的 repo 精简/护栏 v2**：先把后续会复用的第一人称走廊算法从 `YarlungLandscapeImportCommandlet.cpp` 抽成 `YarlungViewCorridor` 深模块（`DistanceToTrackCm` + `ComputeMask`），新增 Automation `CoasterSim.Yarlung.ViewCorridor.*` 覆盖距离投影、mask 覆盖和 fail-closed；验证：UE build PASS，`Found 2 automation tests` 且 2/2 `Result={Success}`，`TEST COMPLETE. EXIT CODE: 0`。随后清理 `ACoasterRideActor` 里的旧环境职责：删除 `BoulderOutcrops`、`BuildBoulderOutcrops()`、`YarlungLandscapeHeight()` 和旧 `RiverZCm` synthetic heightfield，ride actor 不再拥有 boulder/scenery scatter；关卡环境仍由独立 `AYarlungSceneryActor` 负责。验证：UE build PASS；`import-yarlung-landscape.ps1 -SkipAssetGeneration -SkipMaterials -SkipModels -SkipTerrainMeshBuild -Verify` PASS，inspect 显示 ride actor 只剩 train/rails/ties/supports/camera，`YarlungForestRockScenery` 仍有 `RockOutcrops=2007`、`UnderstoryClumps=11000`；offscreen `Saved\OffscreenShots\refactor-ride-scenery-cleanup-smoke.png` 已 Read。视觉结论未变：山体仍 FAIL；但宏观地貌下一刀的接口/职责更干净。
- **禁区**：① 不要再调 SkyAtmosphere 散射；② 不要用调色/几何遮盖去"掩盖"台阶；③ 不要再做 procedural overlay/corridor blanket 伪替换；④ **现在允许并应优先考虑 Nanite/StaticMesh 地形接管**，因为便宜牌、detail normal、soft slope、proxy scatter 都已证明不足，且用户已把全轨道第一人称无台阶设为硬门槛；⑤ 不要再把主 `SM_YarlungMeshTerrain` 的规则 heightfield midpoint/quad tessellation 当 C2 根治线，已验证它只会把同一张假地形烤得更细，不能消掉大折面。

## 阶段状态表
| 阶段 | 内容 | 状态 | 出口标准（见 acceptance §3） | 备注 |
|---|---|---|---|---|
| 0 | 英雄段+参照+基线 | ✅ Done | 时间点选定/refs≥3/baseline 存档 | hero=`WaitSeconds 12`; baseline=`Saved\hero-baseline.png`; refs=`docs/refs/references.md` |
| A | 地形单一真相源+真实DEM | 🟦 进行中 | 无假几何/DEM不穿山/scatter贴地/**D1 改为中远景轮廓正确（近景崖细节 deferred to C）** | A1 Done；A2 参数 PASS；台阶根因=heightfield+30m 源+无 detail normal 固有上限（非 bug）；先 bicubic，近景崖归 C |
| B | 物理天空+光照标定 | 🟦 进行中 | 单一物理天空+大气透视/曝光物理化/D2≥4 D5≥3 | **B2 橙黄根因 + 曝光收尾 PASS**；仍缺云带/大气透视/材质层次 |
| C | 分层Nanite地表材质 | 🟦 进行中 | 崖壁高频细节无moire/冷灰绿/D1≥4 D5≥4 | C1 material groundwork v1 已接入 PBR detail normal/rough/AO；截图证明台阶仍需 mesh/Nanite 接管 |
| D | 江水→轨道/车 | ⬜ TODO | 江面折射流动D3≥4/钢轨D6≥3 | |
| E | 密林+成像收尾 | ⬜ TODO | 密林D4≥4/TSR运动模糊D7≥4 D8≥4/60fps | |
| F | 沿轨道铺开 | ⬜ TODO | 整圈均值≥4无单维<3/60fps | |

状态图例：⬜ TODO ／ 🟦 进行中 ／ ✅ Done ／ ⛔ NEEDS-HUMAN ／ ❌ Blocked

## 独立外审 / Reviewer Notes
> 外审原文归档在 `docs/reviews/*.md`；这里仅保留索引、裁决、阻断摘要。Agent 处理外审时必须把阻断项同步到 `NEEDS-HUMAN / Blocked 待办`，把采纳的流程级决定同步到 `决策记录`。`打分记录` 只放截图/诊断评分，不放长篇外审原文。

| 日期 | Review | Verdict | 状态 | 阻断摘要 |
|---|---|---|---|---|
| 2026-06-19 | [`nanite-mesh-terrain`](../reviews/2026-06-19-nanite-mesh-terrain.md) | **基建 PASS(诚实/真替换)/ 台阶硬 gate 仍 FAIL —— 核心指令「脱离DEM」其实没执行** | Open（C2 进行中）；不标 Done | 复核 `a8c350a`(全环台阶风险诊断)+`f6b9c24`(地形切 Nanite mesh)。**PASS(我独立验)**:`SM_YarlungMeshTerrain` 是真 mesh——`git lfs ls-files` 在册、磁盘 **208MB**、2017²(4.07M verts/8.13M tris)、cubic B-spline+中心差分法线+Nanite;**真替换非盖**——inspect 证 `landscape ... hidden=True` + 可见 `StaticMeshComponent` mesh,旧 Landscape 台阶渲染路径结构性绕开,单层无双面(满足禁区②/双层门);全环风险诊断 `diagnose-yarlung-staircase-risk.py` 是好习惯(防只修一帧)。**诚实**:Codex 自陈梯田台阶已除**但 30m 上采样仍大三角折面**、硬 gate 仍 Open、C 不标 Done,且**撤回**了洗白材质的 v4/更假的 corridor-overlay 试验=无注水。**FAIL/关键发现**:截图 `c2-nanite-terrain-v5` 规则台阶没了**但变成大三角折面**——因为 Codex 做的是 **DEM-忠实** meshing,**没按用户「可以脱离DEM」去解耦/加 sub-30m 细节**,忠实网格必然继承 30m 分辨率贫困→折面。**=核心指令未执行,是绕路非定向那一刀**(但 Nanite 管线+隐 Landscape 是必要管道、做得好,保留)。**下一刀(原指令锐化)**:保留 mesh+隐 Landscape;**真正解耦**——近崖走廊注入 DEM 没有的**程序化 sub-30m 岩石几何/位移**(或真岩壁资产+Nanite displacement),压在风险 CSV 标红处;配真岩石材质防 proxy-tint 翻版。**不得**:回 Landscape LOD 调参 / 拿折面 DEM-mesh 充 Done。 |
| 2026-06-19 | [`soften-scatter-binary-gate`](../reviews/2026-06-19-soften-scatter-binary-gate.md) | **基建 PASS(诚实)/ 视觉 gate FAIL —— 但「真植被」决战其实没真打** | Open（C 卡在资产/决策）；不标 Done | 复核决战一刀 `e6c8298`(软化+撤fascia+重生轨)+`0cdf665`(scatter)。**PASS 部分(我独立验)**:fascia 真删干净(321+49行+material 资产);软化按序重生 terrain→track→**我亲跑 `verify-track-clearance.py`=`violations=0`/`min_clearance=42.40m`/G 全包络**,数值与 Codex 报告一致未注水;scatter 真采 `.r16` 高度/法线放置,`rocks=2007`+`understory=11000`=13007≥4000 门槛;软化没把峡谷推平。**FAIL 部分**:截图 `c1-full-scatter-v1` 右崖 heightfield 台阶仍主导=证实「表示天花板」非可调 bug;**但两组 HISM 都用同一 `boulder_01_1k` proxy 染色(`M_CoasterTint`)**——11000「林下」其实是染绿 boulder,入画读成暗块/噪点非密林。**=用户原话的「真实散布」根本没真打**,是撞**资产墙**而非技术定论。**诚实裁定**:这是高诱惑注水点,Codex 反向操作——自评 D1=3/均值2.0/C not Done、明说 proxy 读成噪点、挂 NEEDS-HUMAN 资产门,无谎报无遮丑充数=reviewer 确认诚实。**升级用户决策**:A 引入真实植被/岩壁资产(Fab/Megascans/CC0)真打这一刀(Yarlung 壁本就森林=真内容非遮盖;但上部裸岩崖真森林也未必盖得住);B 按既定 fail-exit 转 D/E,近崖真根治记 **Nanite/mesh terrain(非 Landscape)** deferred。**不得**:标 C Done / 再调 heightfield-LOD(天花板已证) / 拿 proxy 充植被。 |
| 2026-06-19 | [`stage-c-cliff-and-detail`](../reviews/2026-06-19-stage-c-cliff-and-detail.md) | **PASS（有条件）— 两提交诚实；崖 mesh 是正路非掩盖,但 v1 是视角依赖表皮** | Open（C 进行中）；C/D1/D5 不标 Done | 复核 `e8007a3`+`581ce5e`。**e8007a3** 分层 detail 材质=干净 PASS:PolyHaven PBR 双层按 forest mask 混 + 独立 detail 尺度;无禁区①(没碰 SkyAtmosphere/无 postprocess 调色);**诚实负结果**——明说近景台阶几乎不动、证明 detail-normal-only 治不了几何台阶,正确导向 mesh 接管。**581ce5e 崖 mesh=本轮关键裁决**:① **不是禁区②(掩盖)之实质**——`SampleHeightCm` 真读 `.r16` DEM=崖形是**真地形**非任意墙、是决策记录钦点的「走廊级 mesh 接管」正路、**无欺骗**(Codex 自称"偏大岩壁表皮"、认接缝、`C 不能标 Done`、截图台阶基部仍可见=部分遮非密封幕);② **但只给有条件**——三处捷径使其成**视角依赖表皮**非真替换:`SurfaceLiftCm=850`(浮真面上方 8.5m)、`TrackSidePullCm=5200`(向轨道拉 52m)、**整行统一朝轨道法线**(非真表面法线,像 billboard)。**这正是滑向禁区②的引力**。**裁决**:修正方向必须朝**替换保真度**(真法线+贴近 flush+接缝收边+遮/切底层 Landscape 条),**不得**靠加 lift/装饰/不透明度去埋更多台阶。**建议**(采纳 Codex 自己的 gate):只在能换来真保真度时再做一刀;若仍是另一种假,按用户既定出口转 **D/E(水/植被/成像)**,别在表皮上耗轮。C 在崖从非轨道角度也读成真几何前不得标 Done。 |
| 2026-06-19 | [`anchors-and-water-vertex`](../reviews/2026-06-19-anchors-and-water-vertex.md) | **reviewer-confirmed PASS（两提交诚实且正确）** | Closed（锚点半-Finding + 水材质 groundwork v1）；D3 仍 Open，归 C/D | 复核 `ceabc87`+`472f164`。**ceabc87**=处理 `river-actor-migration` 那条非阻断 Finding 的**一半**:支撑脚从合成 `RiverZCm-35` 改用真 DEM `terrain_z-35`(独立验 `GetGeneratedTerrainZAtDistance` 不变量 `199==198+1` 成立、不会落 0 海平面;截图支撑正常无回退)、谷雾从合成 2652.7m 改读真 `YarlungRiver.csv` 均值 2677.25m;**诚实**——明说只修一半,boulder 的合成 `YarlungRiverCenterY/YarlungLandscapeHeight` 仍在(instances=0),退役留 A3/C,未冒称完成。**472f164**=水材质 groundwork v1:material 改 VertexColor 驱动 BaseColor、水细分 5→11/泡沫 4→7;**无注水**,D3 明确不标 Done。**非阻断 note**:水顶点 Alpha 算了但材质 Opacity 用常量标量=岸边 alpha-fade 当前未渲染。**战略观察**:v1→v2→anchors→vertex 四帧**肉眼几乎无差**,水在 greybox 崖里读不出照片级——**最高收益下一步是 C 阶段(地表材质+台阶),非继续调水**。 |
| 2026-06-19 | [`river-actor-migration`](../reviews/2026-06-19-river-actor-migration.md) | **reviewer-confirmed PASS（水体迁出/DEM 水 v1 架构轴）** | Closed（迁移轴）；D3 照片级水仍 Open，归 C/D | 独立复核 `34cafaa`：迁移**为真非装样**——diff 删掉 ride actor 整套旧河栈(`RiverSurface/Rapids/MistBands/RiverRibbon/FoamRibbon`+`BuildRiver*`~160 行),`inspect-yarlung-map.txt` 证关卡 3 actors、ride 只剩车/轨/支撑/石头、独立 `YarlungRiverScenery` 持 `WaterMesh/FoamMesh`;新水**源自真 DEM thalweg**(`write_river_csv` 取 `river_guide`+`sample_height_from_grid+180cm`,Z≈2726m 贴谷底,**与轨道无关=非循环**);`AYarlungRiverActor` 干净自洽;**无注水**(水仍 8cm sine 平带/简单透明材质,Codex 正确不标 D3 Done;截图 `p4-river-actor-v2` 已读=水沿谷底但 greybox、台阶**明示未遮**);无禁区违规。**非阻断 Finding**:ride actor 仍留旧**合成河/地形锚**(`YarlungRiverCenterY`/`RiverZCm=2652m`)给支撑脚/谷雾用,比真 DEM 水低~74m、路径不同——本次未引入也未谎称修复,但记入 C 阶段需把支撑/雾重锚到真 DEM、退役合成 scaffolding。 |
| 2026-06-19 | [`p4-scenery-alignment`](../reviews/2026-06-19-p4-scenery-alignment.md) | **reviewer-confirmed PASS（风景对齐/几何贴江）** | Closed（对齐轴）；照片级视觉仍归 C/D | 独立读三张图(overlay/crop/in-engine)+CSV 复核确认:overlay 现可判读(旧糊图阻断闭环);轨道**真贴江**——198 点全在 DEM 谷底 **71-149m** 内、零甩飞、按设计偏移(Outbound72/Return148/Turnaround147)贴两岸跟河弯;水 mask 已对齐**同一真 thalweg**(中心 670→137m,覆盖 44.9%→94.4%);**非循环**(thalweg 从 DEM 独立提取,DEM-offset 未变=没偷挪轨道,挪的是水 mask);**无注水**(in-engine 仍 greybox 假水+台阶,Codex 正确 defer C/D)。P3 verifier 复跑仍 `violations=0`。**关掉从 p0-p1 一路挂着的「风景未验证」最后一项(几何/对齐层)。** 仍 TODO:真实水体/材质/近景崖台阶=C/D/E。 |
| 2026-06-19 | [`p3-ride-pacing` + 重审×2](../reviews/2026-06-19-p3-ride-pacing.md) | **重审#2 reviewer-confirmed PASS（P3 乘坐物理轴）** | Closed（乘坐轴）；风景对齐见 p4-scenery-alignment（已 PASS），照片级视觉仍归 C/D | powered coaster(`f7574b4`)经**独立复验**(跑 verifier + 读 CSV + 读 `CoasterRideActor.cpp/.h`+verifier diff)确认:动力是**真有界推力积分非假 clamp**;1.8m/s 地板在 **verifier 和 runtime 两边都删了**、同参同模型(UPROPERTY 28/12/4.2/4.0/0.2);主线最低 **9.99m/s**、`<8m/s` 只在 Station/Lift/Brake、`stall_samples=0`、22min 龟速消失;后半 G 真速度算(lat 0.72→1.01);Turnaround 19.7m 半径在真速度 10.1m/s 下 lat=1.01+41°banking,脆点解除;long 0.43g drive cap、max\|long\|=1.06≤2.5;airtime#1 −0.89g 保留。`samples=1584 speed=0.2-49.7 mainline_min=9.99 lat=1.01 vert=-0.89/3.07 long=1.06 airtime=6 low_speed=0 stall=0 violations=0`。**P3 乘坐轴可标 Done。** 小瑕(非阻断):camelback#2/#3 是 floater 非 ejector。**唯一仍 Open=风景未验证**(最高非长度优先级,overlay 糊看不出贴江),P4 处理。 |
| 2026-06-19 | [`p0-p1`](../reviews/2026-06-19-p0-p1.md) | PASS（有条件） | Open | P0/P1 真落地、不穿山阻断已闭环；乘坐**舒适门禁 + D9 airtime + powered 物理可行性** 已由 P3 powered 闭环；仍 Open：**风景未验证**（overlay 糊看不出贴江），景观相关不得标 Done |
| 2026-06-19 | [`a2-track-adapts-v1`](../reviews/2026-06-19-a2-track-adapts-v1.md) | FAIL | Superseded（不穿山已由 p0-p1 闭环） | 旧 8 点轨道不放行；新 5km `YarlungTrack.csv` 全环净空已过硬门禁，运行时接入(P2)前仍不能标 A2 Done |

## 打分记录（每轮追加，最新在上）
> 格式：`iterN @t<秒>: D1=.. D2=.. ... 均值=.. 短板=.. 根因/下一步=..`
- `c2-macro-landform-v1/v2 + c2-corridor-ridge-v1 @t12s`: **宏观策略反证 / 不提交运行时改动**。按“先把几何搞好、宏观地貌重建”尝试两条快速验证：① 在主 `SM_YarlungMeshTerrain` 上新增 macro landform height delta（talus/buttress/gully，先强后保守）；② 新增独立走廊 ridge procedural mesh actor，脱离主 terrain 生成两侧山体骨架。验证均完整执行：C++ build PASS，macro UT PASS，terrain/map commandlet PASS，offscreen PNG 已 Read。结果：macro v1 过切，v2 虽收敛到 `macro_lower≈-47m / macro_raise≈34m`，但仍是 heightfield 大折面/竖墙；ridge v1 能跑通但读成绿色幕布/面片，且没有解决右侧原竖墙。裁定：**不再把同一张 heightfield 当照片级山体主表达，也不再做轻量 procedural sheet 伪山体**；heightfield 只保留为 DEM 锚点/谷底/远景底图，第一人称可见近中景山体必须由 `c2-erosion-relief-v7` 已验证的法线位移方向继续深化，或改成真实岩壁/植被资产化 volume。运行时代码与生成资产已回退，未提交坏视觉。
- `c2-erosion-relief-v7-skylight @t12s`: **山体几何根治：DEM facet → 程序化合成 sub-30m 岩石几何（Direction 1，非插值非遮盖）**。用户全权交付修山体几何；并行 Codex 的 A(adaptive-midpoints)/B(Namaqualand 散布) 两条均判 FAIL 并已由用户回退。本刀按外审唯一允许方向"脱离 DEM、合成 DEM 没有的高频"重做地形位移（`YarlungLandscapeImportCommandlet.cpp`）：① 加 coherent lattice 噪声 + fBm + ridged multifractal + domain warp（旧 `YarlungValueNoise` 是 white-noise 不可用）；② 位移从"只在窄轨道走廊"扩成"全可见坡面"，slope-gate 接管中等坡 `(slope-0)/0.10`、river-gate 保谷底、near-track 只许下挖保净空；③ **关键根因：位移方向从 world-Z 改为沿法线**——竖直位移在陡崖上只是沿面滑动、面仍是平的（facet 没消的真因），沿法线才把陡面推出/凹进真正打碎；④ strata 几何项删掉（4m 网格上 height-banded sin 读成等高线 moire，留给材质法线）；⑤ `SkyLight 4→8` 抬背阴崖面回白天。建 `scripts/preview-terrain-relief.py`（numpy 离线 hillshade 镜像 C++ 噪声）先把噪声字符调好再进 UE，省多次 ~5min 重烤。验证：C++ build PASS；mesh 重烤 PASS（`displaced_vertices=3.81M`、`max_displacement_cm≈83m`、nanite=true）；verify PASS；逐帧 Read `c2-erosion-relief-v1…v7`（v3 远峰仍 facet 正是暴露竖直位移 bug → v4 改法线后峰部变糙）。终帧 v7：上部山体读成真实糙岩、silhouette 破开、无 facet/水晶/moire，谷底保平、桥墩干净。评分：D1=3（旧2）、D2=3、D3=2、D4=1、D5=3、D6=3、D7=2、D8=2，均值≈2.4。短板：下半背阴崖面仍偏暗（自然背光，可再微调补光/曝光）；近景岩面缺真材质 normal/strata（归材质层）；仅验 hero t=12，全环逐帧归 F。下一步：材质层补 strata/rock normal + 背阴补光；或按需引真扫描资产做 hero 近景。
- `c2-adaptive-terrain-midpoints-v1/v2 @t12s`: **C2 terrain mesh geometry experiment / visual FAIL / not committed as runtime state**。用户指出真实 cliff asset 效果一般，询问是否应改地形 mesh 几何本身；本轮直接验证。v1：主 `SM_YarlungMeshTerrain` 在 rock/corridor mask quad 加 midpoint center vertex，`adaptive_centers=1,155,493`、`triangles=10,439,498`、`max_displacement_cm=4938.3`、PeakPhysMemoryMB=16102，Build/import/verify PASS，截图已 Read；结果右侧仍是巨大三角面/竖切面，且强位移区出现菱形假岩面。v2：同拓扑但把程序化位移降到 `max_displacement_cm=1329.0`、PeakPhysMemoryMB=16572，Build/import/verify PASS，截图已 Read；结果低模折纸感更明显，大折面未消。评分：D1=1、D2=3、D3=2、D4=1、D5=1、D6=2、D7=2、D8=2，均值≈1.75。结论：**改地形 mesh 几何方向是对的，但不能继续在同一张规则 heightfield 主 mesh 上加密/调位移；这不是 C2 解法。** 运行时代码与生成资产已回退；下一步若继续 C2，应上非 heightfield 的 author cliff/rock/forest wall 或请外援/真实资产。
- `support-piers-bright-v1 @t12s`: **接两刀跟进：桥墩浮脚消除 + 偏暗回归根治（终于回到白天）**。① 桥墩脚改为埋到 `terrain_z-6000cm`（远超 mesh 最大 sub-30m 位移 ~49m），从轨道相机看埋入段被地表遮挡=零视觉成本，彻底消除"浮脚"；腿粗/横拉杆改按**可见**高度计算（埋入段不再吃粗细/横杆）。② **"偏暗"查清是 C2 换地形时的隐性回归，不是光照 bug**：HEAD 的太阳已是 **120000 lux** 物理日光、曝光是 manual ISO100/1·500s/+1.65（≈EV15.9 晴天），天空一直曝光正确（亮蓝不过曝）；但 C2 把**亮的航拍 macro 反照率 Landscape** 换成 **暗的程序化顶点色 Nanite 地形**（forest `(0.06,0.25,0.11)`、wet-rock `(0.22,0.29,0.27)`），B2 当年是对着亮地形标的曝光，于是同一曝光把暗反照率地形渲成近黑。修法：在 `M_YarlungMeshTerrain` BaseColor 前加 `TerrainBrightness=1.6` 乘子抬地表反照率，**不动**已正确的天空/曝光（抬曝光会过曝天空）。验证：C++ build PASS；`-Build -SkipAssetGeneration -ForceMaterials -SkipModels -SkipTerrainMeshBuild -SkipMapImport -Verify` PASS（复用 208MB mesh，未重烤）；offscreen `Saved\OffscreenShots\support-piers-bright-v1.png` 已 Read——地表回到晴天亮灰、天空仍正确蓝、桥墩干净落地无浮脚。评分：D1=2、D2=3、D3=2、D4=1、D5=3（反照率/曝光配套回正，旧 2）、D6=3、D7=2、D8=2，均值≈2.25。短板仍是 30m-DEM 大折面（亮起来后更显眼，仍归 C2 chunk/资产线）；近景前景略washed，`TerrainBrightness` 是 scalar param 可再微调（1.6→1.4 可降）。
- `support-piers-v1 @t12s`: **支撑"雨帘"defect 已根治 / D6 结构升级，C 地形折面仍 FAIL**。用户对当前帧报"visual 有很大问题"；systematic-debugging 定位最刺眼伪影=**支撑柱**而非散布/地形：轨道是峡谷高架（全程平均高出地形 **157m**，min 42.5/max 331.1m，198 采样全部 >42m），旧代码 `SupportStep=720`(每7.2m)向谷底各落一根 **9cm 细柱**→~3515 根细线在透视里糊成竖向"雨帘"。附带查清 `MakeSegmentTransform` 忽略 `ScaleCm.X`（长度由端点算），旧 `YokeLeft.Z` 当尺寸是 dead code；`YokeCenter.Z > 850` 撑斜撑的判据用绝对世界 Z 恒真。修法（`CoasterRideActor.cpp` BuildTrack 支撑段）：`SupportStep 720→7200`(每72m)，腿柱粗细按柱高 `Clamp(PierHeight*0.014, 150, 470)cm` 缩放，去掉恒真细斜撑、改为按高度加 0–4 道水平横拉杆，读成纪念碑式高架桥墩。验证：C++ build PASS（`offscreen-shot.ps1 -Build`，4.42s 编译，Result: Succeeded）；offscreen `Saved\OffscreenShots\support-piers-v1.png` 已 Read——竖向雨帘消失，变为~70 根离散桥墩，画面立刻从"故障感"回到"高架峡谷"。评分：D1=2、D2=3、D3=2、D4=1、D5=2、D6=3（结构可读，旧 2）、D7=2、D8=2，均值≈2.1。短板仍是右侧 30m-DEM 山体大折面（表示天花板，需 chunk/真岩壁资产，归 C2，未在本刀处理）；另近景桥墩脚未严格贴可变地形（脚 Z 取轨道中心线 terrain_z，侧向偏移处可能略浮）。下一步：C2 折面仍按既定 chunk/资产线；可选后续把桥墩脚按实际 X/Y 采样地形高度消除轻微浮脚。
- `c2-skip-terrain-mesh-build-smoke @t12s`: **C2 pipeline decoupling smoke / C still FAIL**。目标不是改画质，而是验证 map/scenery 快循环可复用现有 Nanite terrain。验证：C++ build PASS；`import-yarlung-landscape.ps1 -SkipAssetGeneration -SkipMaterials -SkipModels -SkipTerrainMeshBuild -Verify` PASS；日志确认 `Reusing existing Yarlung mesh terrain`，commandlet 本体 1.57s / PeakPhysMemoryMB=1787，未出现 `Built Nanite Yarlung mesh terrain`；offscreen PNG 已 Read。评分沿用当前视觉状态：D1=2、D2=3、D3=2、D4=1、D5=2、D6=2、D7=2、D8=2，均值=2.0。结论：快路径未破坏关卡结构，轨道/支撑/river/scatter/terrain 均可见；右侧山体大折面仍主导，C2 视觉不通过。
- `c2-nanite-rock-buttress-v1 + c2-rock-material-light-v1/v2 @t12s`: **C2 diagnostic / C still FAIL**。按最新文档先试暂停 chunk 前的局部 Nanite 岩体与材质/光分叉。chunk 路线：v1/v2 连续 slab、rock-buttress v1 离散闭合岩体，均在右侧读成假几何/亮面块，已撤回不提交。材质/光路线：`M_YarlungMeshTerrain` 混入 `AerialGrassRock` diffuse，曝光从 `+1.2` 提到 `+1.65`、SkyLight `3→4`，不调 SkyAtmosphere；`v1` 洗白，`v2` 回到湿灰绿但台阶/大折面仍清楚。评分：D1=2、D2=3、D3=2、D4=1、D5=2、D6=2、D7=2、D8=2，均值=2.0。结论：材质/光是必要诊断但不是充分解；下一步按“解耦+加速”做真正局部 corridor/chunk geometry pipeline，避免每轮重建 208MB whole-world mesh。
- `c2-author-cliff-replacement-v1/v2 + c2-author-cliff-vertex-reshape-v1 @t12s`: **C2 rejected experiment / not committed as runtime state**。目标是把风险 CSV 标红段从 DEM heightfield 解耦：先试“挖 terrain 三角 + authored patch”，再试“不挖洞、仅 vertex reshape”。验证：C++ build PASS；`import-yarlung-landscape.ps1 -SkipAssetGeneration -ForceMaterials -SkipModels -Verify` PASS；offscreen 三张 PNG 均已 Read。评分：D1=1、D2=2、D3=2、D4=1、D5=1、D6=2、D7=2、D8=2，均值=1.6。短板：patch 版出现 sky hole/直边蓝面/竖线，vertex reshape 版把相机埋进地形；比上一版回归，已回退不提交运行时改动。下一步：不要再在主 terrain heightfield 上挖/推；改做真正独立的局部 Nanite cliff/chunk asset，并用单段截图先验。
- `d1-river-energy-v1 @t12s`: **D water energy groundwork / D3 still not passing**。`AYarlungRiverActor` 用局部 river energy 驱动水面 milky tint、choppy normals、foam lane 宽度/alpha；不新增遮挡水面的独立 patch mesh。验证：C++ build PASS；import/inspect PASS；offscreen `Saved\OffscreenShots\d1-river-energy-v1.png` 已 Read。评分：D1=3、D2=2、D3=2、D4=1、D5=2、D6=2、D7=2、D8=2，均值=2.0。短板：水仍是 translucent procedural ribbon，缺 depth fade/反射折射/真实流动法线；右侧 heightfield 台阶仍主导。下一步：D 若继续，应走材质层 flow normal/depth fade 或 UE Water，不要用大块 foam patch 几何遮。
- `c1-full-scatter-v1 @t12s`: **Full-density scatter infrastructure PASS / visual C still FAIL**。实现独立 `AYarlungSceneryActor`，由 commandlet 生成 `YarlungForestRockScenery`；从真实 `YarlungTrack.csv` Outbound + `.r16` heightmap 采样贴坡，HISM 实例 `rocks=2007/understory=11000`，inspect PASS（唯一 scenery actor，总实例>4000，无 cliff actor）。截图已 Read。评分：D1=3、D2=2、D3=2、D4=1、D5=2、D6=2、D7=2、D8=2，均值=2.0。短板：右侧近崖 heightfield 台阶仍主导画面；没有真实树/灌木资产，boulder proxy 不能读成密林。下一步：不要再调 heightfield/LOD；要么引入真实植被/岩壁资产继续 C/E，要么按 fail-exit 转 D/E。
- `c1-soft-slope-v1 @t12s`: **C1 soft-slope route / C still not passing**。实现新文档路线第一刀：撤掉独立 cliff fascia actor/material，Landscape commandlet 不再生成 `YarlungCliffScenery`；terrain generator 读取上一版 Outbound track guide 做局部 slope softening 并扩大近江 forest mask，随后重生 `YarlungTrack.csv` 和 clearance。验证：Python py_compile PASS；terrain manifest softening `affected_samples=45764/max_lowered=308.1m/mean_lowered=78.2m`；`verify-track-clearance.py` PASS (`violations=0`, `min_clearance=42.40m`)；UE build/import/inspect PASS（map `actor_count=3`，无 cliff actor）；offscreen `Saved\OffscreenShots\c1-soft-slope-v1.png` 已 Read。肉眼结论：fascia 彻底撤除，数据层自然坡有效，但右侧近景仍有明显 heightfield 块状阶梯，照片级 C/D1/D5 不过。附加反证：`c1-soft-slope-lod0-v1` 已 Read，强制 LOD0 反而显示原始网格块，未提交。下一步=只做真实实例化植被/岩石散布；若仍不解，按 fail exit 转 D/E。
- `d1-water-alpha-v1 @t12s`: **D water alpha plumbing / D3 still not passing**。外审指出水/泡沫顶点 Alpha 已算但材质 Opacity 仍用常量；本轮把 translucent vertex-color 材质的 `VertexColor.A` 接入 `MP_OPACITY`，使 `AYarlungRiverActor` 里的岸边 fade/泡沫 alpha 真正渲染。验证：Python py_compile PASS；`import-yarlung-landscape.ps1 -Build -ForceMaterials` PASS；inspect PASS（river actor/foam/water 仍独立，cliff actor 未回退）；offscreen `Saved\OffscreenShots\d1-water-alpha-v1.png` 已 Read。肉眼结论：水边透明度变化很小，无明显回退，但仍缺 flow normal/depth fade/reflection-refraction，D3 不过。另：按 `stage-c-cliff-and-detail` 外审 gate 试过 `c1-cliff-flush-v1/v2`（flush/no-pull/真法线），画面黑斑更假，已撤回不提交；C 线暂止，下一步转 D flow normal/depth fade 或 E 植被/成像。
- `c1-cliff-fascia-v1 @t12s`: **C1 corridor cliff mesh takeover v1 / C still not passing**。新增独立 `AYarlungCliffActor`，由 commandlet 放置 `YarlungCliffScenery`；actor 读取 Outbound `YarlungTrack.csv` 与 16-bit `.r16` heightmap，生成贴轨道视线侧的 procedural cliff fascia（`363` 沿线样本、`3993` 顶点、`7240` 三角），材质 `M_YarlungCliffRock` 使用 procedural mesh vertex color + `AerialGrassRock` normal/rough/AO。验证：Python py_compile PASS；C++ build PASS；`import-yarlung-landscape.ps1 -Build -ForceMaterials` PASS；inspect PASS，map `actor_count=4` 且 cliff actor 独立，ride actor 未回退旧 scenery；offscreen `Saved\OffscreenShots\c1-cliff-fascia-v1.png` 已 Read。肉眼结论：右侧近景规则台阶被明显压住，证明 mesh 接管路线比继续调 Landscape 材质有效；但现在仍像偏大的岩壁表皮，接缝/支撑穿插/岩块 silhouette 不自然，照片级 D1/D5 不过。下一步=只做一刀接缝+岩块化验证；若仍假，按用户要求跳过台阶线，转水体/植被/成像。
- `c1-landscape-detail-v2 @t12s`: **C1 Landscape material groundwork / C not passing**。`M_YarlungLandscapeGround` 生成逻辑改为 macro albedo + macro masks + PolyHaven `AerialGrassRock/LeafyGrass` detail PBR 分层：BaseColor 按 forest mask 轻混 detail diffuse，Normal 直接接 detail normal，Roughness/AO 按同一 mask 混合；detail coords 独立于 macro coords（`mapping_scale=140`），避免把近景细节绑到整图 macro。验证：`import-yarlung-landscape.ps1 -Build -ForceMaterials` PASS；inspect PASS（landscape 仍全组件挂 `M_YarlungLandscapeGround`，actor_count=3，river/ride 未回退）；offscreen `Saved\OffscreenShots\c1-landscape-detail-v1.png` 与 `c1-landscape-detail-v2.png` 均已 Read。肉眼结论：远中景坡面略有材质/粗糙度变化且无明显 moire，但右侧近景台阶仍是规则几何硬边，D5 有 groundwork、D1/C 出口不达标。根因=heightfield geometry/silhouette，不是纯材质。下一步=生成 corridor cliff/Nanite mesh 接管近景掠射崖面，而不是继续调材质强度。
- `p4-water-vertex-v1 @t12s`: **D/P4 water material groundwork / D3 still not passing**。`create-coaster-materials.py` 新增 translucent vertex-color river material，`M_YarlungRiverWater/Foam` 的 BaseColor 由 procedural mesh vertex color 驱动；`AYarlungRiverActor` 水面横向采样 `5→11`，加入轻微岸线扰动、中心青绿/乳白色带、复合 ripple；泡沫 lane `4→7`，按岸边权重改变宽度/颜色/alpha。验证：C++ build PASS；`import-yarlung-landscape.ps1 -Build -ForceMaterials` PASS；inspect PASS，仍为 3 actors、`YarlungRiverScenery` 拥有 `WaterMesh/FoamMesh`，日志确认 `Loaded Yarlung river CSV: 213 samples` 与 `Anchored valley fog...267655.0cm`；offscreen `Saved\OffscreenShots\p4-water-vertex-v1.png` 已 Read。肉眼结论：水体层次有小幅改善且无架构回退，但当前图仍像透明平带/雾化面，反射折射、流向法线、岸边深度衰减未做，右侧 Landscape 台阶仍压倒画面；D3 不标 Done。下一步=水材质 flow normal/depth fade，若收益仍低则回 C 处理近景崖。
- `p4-real-anchors-v1 @t12s`: **external-review finding cleanup / no photoreal rescore**。处理 `river-actor-migration` 非阻断 Finding 的一半：`UCoasterTrackComponent` 保存 `terrain_z` 并按 track distance 插值，支撑脚从旧固定 `RiverZCm-35` 改为 `terrain_z-35`；`ValleyFog` 从旧 `RiverZCm+70` 改为读取 `YarlungRiver.csv` 平均水面 `267655.0cm` 后 +70cm。验证：C++ build PASS；`import-yarlung-landscape.ps1 -Build` PASS，日志确认 `Anchored valley fog to generated Yarlung river average Z: 267655.0cm`；inspect PASS，`Supports instances=3515`、`fog_component=ValleyFog relative_z_cm=267725.0`；offscreen `Saved\OffscreenShots\p4-real-anchors-v1.png` 已 Read，画面未变坏且未遮台阶。限制：`YarlungRiverCenterY/YarlungLandscapeHeight` 仍因旧 `BoulderOutcrops` 残留（inspect 当前 boulder instances=0），完整 scatter 迁出/影子地形退役留给 A3/C。
- `p4-river-actor-v2 @t12s`: **D/P4 water migration v1 cleanup / D3 not yet passing**。`ACoasterRideActor` 删除旧 `RiverSurface/Rapids/MistBands/RiverRibbonMesh/FoamRibbonMesh` 与 `BuildRiver*` 代码，水体职责完全迁到 `AYarlungRiverActor`。`inspect-yarlung-map.py` 新增旧 river 组件 forbidden gate，并写 `Saved\Diagnostics\yarlung-map-inspect.txt`；报告确认 map actor_count=3，ride actor 只有 `TrainBody/LeftRail/RightRail/Ties/Supports/BoulderOutcrops`，river actor=`YarlungRiverScenery` 且拥有 `WaterMesh/FoamMesh`，材质为 `MID_M_YarlungRiverWater/Foam`。验证：`import-yarlung-landscape.ps1 -Build` PASS；inspect PASS（仅 EditorLevelLibrary deprecation warnings）；offscreen `Saved\OffscreenShots\p4-river-actor-v2.png` 已 Read。肉眼结论：水面仍可见并沿谷底，但仍是平面透明带/分段水边，右侧 Landscape 台阶仍明显；架构迁移 Done，D3 不标 Done。下一步=水体细分+流向法线/泡沫/反射折射。
- `p4-river-actor-v1 @t12s`: **D/P4 water migration v1 / D3 not yet passing**。新增 `Content/Generated/YarlungLandscape/YarlungRiver.csv`（213 samples，DEM thalweg + height-derived Z + half-width 220m），新增 `AYarlungRiverActor` 负责 scenery river procedural water/foam mesh，`YarlungLandscapeImportCommandlet` 放置 `YarlungRiverScenery`，`ACoasterRideActor::RebuildEnvironment()` 停止生成旧解析河。材质脚本新增 `M_YarlungRiverWater`/`M_YarlungRiverFoam` 透明参数材质。验证：`import-yarlung-landscape.ps1 -Build -ForceMaterials` PASS；日志确认 `Loaded Yarlung river CSV: 213 samples`；offscreen `Saved\OffscreenShots\p4-river-actor-v1.png` 已 Read，水面可见并沿谷底/轨道走廊；但水面仍是平带、边界有分段感、缺流动法线/反射折射/真实湍流泡沫，D3 仍低分。下一步=水体细分+材质流动/泡沫，再评 D3。
- `p4-river-mask-narrow @t12s`: **P4 river-mask alignment validation / no photoreal rescore**。新增/更新诊断图：`Saved\Diagnostics\yarlung-scenery-overlay.png`、`Saved\Diagnostics\yarlung-scenery-overlay-crop.png`、CSV `Saved\Diagnostics\yarlung-scenery-overlay.csv`；offscreen：`Saved\OffscreenShots\p4-river-mask-narrow.png`。改动：`generate-yarlung-landscape-assets.py` 从 naturalized DEM 提取 thalweg river guide，macro preview/masks/albedo/roughness 均使用同一 guide；`RIVER_MASK_HALF_WIDTH_CM=26000`，避免 700m 半宽把整段谷底/崖壁涂成假水。诊断 PASS：`river-mask samples=187/198(94.4%)`、`macro-mask center offset med/p90/max=137/200/274m`、`edge distance med/p90/max=0/0/31.8m`、`DEM thalweg offset med/p90/max=110/148/149m`；P3 verifier 复跑仍 PASS：`samples=1584 ... violations=0`。截图肉眼看：江道现在跟轨道/谷底同源，但画面仍是 macro 材质假水 + 近景 Landscape 台阶；下一步转真实水体/材质和 C 阶段近景崖处理。
- `p4-scenery-overlay @no-render`: **P4 风景构图诊断 / no photoreal rescore**。新增诊断图：`Saved\Diagnostics\yarlung-scenery-overlay.png`（全图+右侧统计面板）、`Saved\Diagnostics\yarlung-scenery-overlay-crop.png`（轨道走廊 crop），CSV：`Saved\Diagnostics\yarlung-scenery-overlay.csv`。图像已 Read 肉眼检查：轨道整体沿 DEM 谷底/北岸布置，能看出轨道+谷底+对岸山体关系；不是旧 overlay 那种“糊到无法判断”。关键统计：`DEM thalweg offset med/p90/max=110/148/149m`，说明轨道基本按生成器设计贴着 DEM 河谷两岸；但 `macro-mask center offset med/p90/max=670/1154/1313m`、`river-mask samples=89/198(44.9%)`、`river-mask edge distance med/p90/max=34/441/524m`，说明当前 cyan river mask / 未来视觉水体没有跟真实 DEM 谷底对齐。分区：Turnaround 完全贴近 macro river(`18/18 inside, med offset 41m`)，Outbound/Return 多数在 mask 北缘或外侧，Station/Lift/Launch/Brake 明显离 macro mask 更远。结论：**overlay 缺失阻断已闭环，P4 下一步不是挪轨道，而是把 river mask / water body 改为沿 DEM thalweg 生成**；修完再判断“贴江如画”。
- `p3-powered-cruise @t12s`: **P3 powered-coaster feasibility validation / no photoreal rescore**。验证图：`Saved\OffscreenShots\p3-powered-cruise.png`；离线诊断：`Saved\Diagnostics\track-clearance.csv/.png`。改动：车辆模型由纯重力车改为用户拍板的 powered coaster。`verify-track-clearance.py` 与 runtime 同步：Outbound/Return/Coast 用有界 drive 追 `28m/s` 巡航；Turnaround 用有界 drive/brake 控到 `12m/s`；Brake/Station 才允许停车；数值 stall floor 从旧 `1.8m/s` 语义降为 `0.2m/s`，并新增主线路 `low_speed_samples` 与非 Brake/Station `stall_samples` 门槛。验证 PASS：`samples=1584 length=5055.2m min_clearance=42.40m min_radius=19.7m max_grade=63.6% speed_range=0.2-49.7mps est_max_lat_g=1.01 est_vert_g=-0.89/3.07 est_max_long_g=1.06 airtime_samples=6@<=-0.30G low_speed_samples=0@<8.0mps stall_samples=0 violations=0`；主线路速度统计：Outbound `24.97-49.72m/s`、Turnaround `9.99-26.85m/s`、Return `11.08-26.53m/s`、Launch `26.39-34.39m/s`，0.2m/s 只在 Brake/Station。UE build PASS；offscreen 日志确认 runtime 加载 `YarlungTrack.csv (198 points, 5057.2m)`；HUD 截图有效（Outbound 20.8m/s）。结论：外审指出的 clamp-masked stall / 22.6min crawl 已闭环，P3 可标 Done；视觉台阶仍归 P4/C。
- `p3-airtime-redo @t12s`: **P3 ride-experience validation / no photoreal rescore**。验证图：`Saved\OffscreenShots\p3-airtime-redo.png`；离线诊断：`Saved\Diagnostics\track-clearance.csv/.png`。改动：默认轨迹控制点间距 `35m→25m`（198 点/约 5.06km runtime 长轨），出程不再用均匀正弦波伪 airtime，改为早段 300m 集中落差建速 + 3 个局部 raised-cosine camelback；返程额外高度 `32m→12m` 消除 turnaround 坡度热点；闭环 XY 平滑只改平面、不再抹掉竖向丘形。验证 PASS：`samples=1584 length=5055.2m min_clearance=42.40m min_radius=19.7m max_grade=63.6% speed_range=1.8-49.7mps est_max_lat_g=0.72 est_vert_g=-0.89/3.07 est_max_long_g=1.06 airtime_samples=6@<=-0.30G violations=0`。新增 verifier airtime gate：Outbound 上 `vert_g<=-0.30` 默认至少 2 个采样，否则失败。UE import/offscreen 日志确认 runtime 加载 `YarlungTrack.csv (198 points, 5057.2m)`。截图有效；右侧 Landscape 台阶仍明显，属于已知 C/渲染材质问题，不影响 P3 ride-experience 结论。结论：外审 D9 airtime 阻断闭环，P3 可标 Done。
- `p3-airtime-final @t12s`: **P3 geometry/pacing attempt / no photoreal rescore**。验证图：`Saved\OffscreenShots\p3-airtime-final.png`；离线诊断：`Saved\Diagnostics\track-clearance.csv/.png`。改动：默认控制点间距 `55m→35m`（144 点/约 5.24km runtime 长轨），站台/出程起点抬高约 150m，出程做约 225m 重力俯冲，叠约 160m 波长、19m 幅度的 airtime 丘；Lift 目标速度 `7→12m/s`，runtime/verifier 同步。舒适门禁 PASS：`samples=1152 length=5241.5m min_clearance=23.47m min_radius=8.7m max_grade=51.8% speed_range=1.8-34.6mps est_max_lat_g=2.04 est_vert_g=0.17/2.47 est_max_long_g=1.07 violations=0`；压缩段真实存在（max vert 2.47），但 **ejector airtime 未达成**：负 G 样本 0 次，`vert_g<0.2` 仅 1 次且在 Turnaround，不是设计丘。HUD smoke 确认 runtime 加载 `YarlungTrack.csv (144 points, 5248.8m)`。结论（按外审修正）：舒适门禁已闭环，D9 airtime FAIL；P3 ride-experience 不得标 Done。
- `p2-longtrack-render-staircase-diagnostic @no-render`【独立诊断】：针对 `p2-longtrack-smoke.png` 右坡大三角台阶定位「数据 vs 渲染」。在 P2 相机所在 Outbound ~1849m 区段（XY≈(512,-1841)m）沿坡面采当前 naturalized `.r16`：垂直剖面 **2662→3751m / 1.5km 连续单调爬升**，每 quad(~6.7m) Δh `mean_abs=3.2m / max=5.0m`、**60 个 quad 仅 1 次坡度变号** → **高度数据平滑、无平台/量化台阶**。判定：**台阶不在数据、在渲染** = Landscape 网格 LOD/镶嵌大三角 faceting + 裸 macro 无 detail normal + 第一人称掠射；即 2026-06-18 根因里**「法线/材质」那半**，**非「高度数据」那半**（本段数据非瓶颈）。下一步=先试 Landscape LOD/tessellation + 平滑法线（便宜，数据既好可能消大半），并确认 `.umap` 是当前 naturalized `.r16` 重导（排除陈旧导入），近景最掠射崖才到 stage C。与 P2 正交（P2 运行时接入已成）。
- `p3-banked-longtrack-smoke @t12s`: **P3 comfort-gate validation / no photoreal rescore**。验证图：`Saved\OffscreenShots\p3-banked-longtrack-smoke.png`。改动：`roll_deg` 改为生成器按曲率/设计速度写入，runtime 按距离插值 CSV roll 替代旧正弦 banking；`verify-track-clearance.py` 改为 station 低速起步的真实速度剖面 + banking 后座椅 G 门禁。验证：`samples=720 length=5031.8m min_clearance=22.49m speed_range=1.8-35.0mps est_max_lat_g=0.54 est_vert_g=0.87/1.02 est_max_long_g=1.06 violations=0`。HUD smoke：`Section=Outbound / Track=1849.7m / LateralG≈-0.02`。结论：外审指出的“恒速横向G + 空壳纵向G”已闭环；但 D9 乘坐体验还未最终 Done，因为垂直 G/airtime 节奏偏平，下一步需调出更明显俯冲/airtime。
- `p2-longtrack-smoke @t12s`: **P2 runtime validation / no photoreal rescore**。验证图：`Saved\OffscreenShots\p2-longtrack-smoke.png`。日志确认运行时加载 `YarlungTrack.csv (90 points, 5039.6m)`；HUD 显示 `Section: Outbound`、`Track: 1849.7m`，证明第一人称运行时已经接入长轨道 CSV 和距离分段，不再停在旧短环。画面仍是灰盒/地形材质问题，留给 P3/P4/C/D/E。
- `a2-track-adapts-v1 @t12s`: D1=2.5 D2=3.5 D3=1 D4=0 D5=2 D6=1 D7=2 D8=2.5，均值=1.81。验证图：`Saved\OffscreenShots\a2-track-adapts-v1.png`。改动：`ApplyTrackClearanceCut` 默认关闭，控制点 Z 改为 naturalized 地形 + 约 22m。结果：轨道下方/近处由硬挖槽变成连续坡面，证明“轨道适配地形”方向对；但左上近景崖壁仍是规则 heightfield 货架，尚未照片自然。下一步=轨道 XY/英雄段构图避开近墙，或在走廊内对可见崖壁做作者化重塑/mesh 接管。
- `a2-naturalized-v1 @t12s`: D1=2 D2=3.5 D3=1 D4=0 D5=2 D6=1 D7=2 D8=2.5，均值=1.75。验证图：`Saved\OffscreenShots\a2-naturalized-v1.png`。改动：在 `.r16` 生成后增加照片向 naturalization（宽半径平滑 + 坡度 relaxation）。数据剖面改善：同线 `max_delta 16.5m→11.0m`、`kinks 10→1`、`mean_abs_d2 0.552→0.249`。但画面几乎不变，说明主要可见货架不只是 raw `.r16`，而是轨道净空/构图与 heightfield 近墙共同造成。结论：不能只 naturalize 数据，必须轨道适配地形。
- `a2-bicubic-bspline-v1 @t12s`: D1=2 D2=3.5 D3=1 D4=0 D5=2 D6=1 D7=2 D8=2.5，均值=1.75。验证图：`Saved\OffscreenShots\a2-bicubic-bspline-v1.png`。改动：`sample_cm` bilinear → cubic B-spline；重生 `YarlungTsangpo_1009.r16`、macro、`.umap`。数据剖面改善明确：同线 `kinks_gt_1_5m 10→2`、`mean_abs_d2 0.552→0.423`。但视觉英雄帧左侧近景陡崖仍呈大面积水平货架/台阶，D1 不能上修。判定：cheap DEM 插值牌已打完，继续 2017/插值收益低；近景墙转 stage C 材质法线/Nanite，A2 是否放行只看新 D1 口径下的中远景尺度/不穿山/贴地。
- `a2-height-profile-diagnostic @no-render`: 诊断工具：`scripts\dump-yarlung-height-profile.py`；输出：`Saved\Diagnostics\yarlung-height-profile-1.csv/.png`。结果：最陡剖面 `x=21-116, y=636`，96 点全唯一、636m relief、`flat_pairs_lt_15cm=3`、`big_pairs_gt_3m=80`、`kinks_gt_1_5m=10`、`mean_abs_d2=0.552`。判定：**高度数据本身不是硬阶梯/量化平台**，但 bilinear 上采样留下明显分段斜率，足以在陡崖+掠射第一人称下形成法线折痕/横向台阶感。下一步=实现 bicubic/样条采样并重导入 1009 对比；2017 仅作必要时对照。
- **【独立验收 / 人类判官 2026-06-18 第三次】台阶根因审阅：撤回 8-bit 假设，定性为 heightfield 固有上限**：审阅 `generate-yarlung-landscape-assets.py` 采样链确认——`main` 当前上采样是 bilinear 四点 `lerp`(`sample_cm`)，`.r16` 真 16-bit ~62k unique；conditioning/box blur 属于前一轮实验排查语境，**未合入 main**。**我上一轮的 8-bit/量化假设证伪、撤回**；Codex 排除工作（LOD0/2017/conditioning/clearance/旧 boulder）全部认可。真根因=单值 heightfield 无法表达近垂直崖 + 30m 源分辨率天花板(ALOS 也 30m、无免费高精) + bilinear 只 C0 不 C1 的法线折痕 + 裸 macro 无 detail normal。**裁决（含用户拍板）：先 bicubic 上采样 + 剖面诊断这两张便宜牌；近景陡崖的真正修复（detail normal + Nanite cliff mesh）归入 stage C，现在不提前做；A2 的 D1 验收口径下调为"中远景轮廓正确、近景崖细节 deferred to C"。** 详见决策记录 2 条。
- `b2-daylight-exposure-v1 @t12s`: D1=2 D2=3.5 D3=1 D4=0 D5=2 D6=1 D7=2 D8=2.5，均值=1.75。验证图：`Saved\OffscreenShots\b2-daylight-exposure-v1.png`（2560×1440）。改动：`AutoExposureBias 0.0 → +1.2`，保留 `ISO100 / 1/500 / f11` 物理相机参数。像素均值从上一版约 `(65.85,87.98,93.60)` 提升到 `(94.15,122.12,129.62)`；肉眼看蓝天/岩壁/轨道进入晴天可读区间，B2 曝光收尾判 PASS。未到位项清楚暴露：左侧峡谷壁硬水平台阶仍严重，江水/轨道/材质/植被仍是占位，下一步转 A2 高程台阶根因。
- **【独立验收 / 人类判官 2026-06-18 第二次】B2 颜色根因 PASS，曝光未完，新发现 D1 台阶**：核对 `CoasterRideActor.cpp:169-170` 确认 `SkyAtmosphere` 三行手调散射已全删、用物理默认；`:157` SkyLight realtime capture；`:128-134` 已切 `AEM_Manual`+物理相机曝光；`:180` 体积雾关。截图 `b2-daylight-rayleigh-default-v2` **橙黄彻底消失、蓝天回归**——外援诊断（Rayleigh 被放大~33×）兑现，**B2 去橙主目标判 PASS**。但两处未到位：① **曝光偏暗**：算得 f/11·1/500·ISO100 ≈ EV≈15.9，比 sunny-16 暗~1.2 档，整幅读成黄昏/阴天，非"风景如画晴天"→ B2 曝光标定记为进行中。② **D1 新发现**：左侧峡谷壁是水平硬台阶（梯田状），疑似高程图被当 8-bit 量化（256 级 over 4700m≈18m/级，与画面吻合），**台阶不修地形再亮也过不了 D1**，属 A2 账。过程认可：未作弊调色、未谎报、按建议根因修。**裁决：放行继续，下一步先收尾 B2 曝光(EV~14.6)，再回查 A2 8-bit 台阶。** 评分 D2 由 3 上修为 3.5（蓝天对但偏暗）。
- `b2-daylight-rayleigh-default-v2 @t12s`: D1=2 D2=3 D3=1 D4=0 D5=2 D6=1 D7=2 D8=2，均值=1.6。验证图：`Saved\OffscreenShots\b2-daylight-rayleigh-default-v2.png`（2560×1440）。结论：外援诊断命中，移除非物理 `SkyAtmosphere` Rayleigh/Mie/AerialPerspective overrides 后，橙黄天空/红褐地形消失，画面恢复蓝天与绿地；`SkyLight` 开 realtime capture，物理相机 `ISO100 / 1/500 / f11`，体积雾临时关闭。仍不达照片级：无云带、左侧近景地形块面/切槽强、地表材质平、轨道/支撑线过乱。下一步=回到 A2 视觉层，先处理轨道走廊与地形构图/块面问题。
- `b2-daylight-rayleigh-default-v1 @t12s`: D1=2 D2=3 D3=1 D4=0 D5=2 D6=1 D7=2 D8=2，均值=1.6。验证图：`Saved\OffscreenShots\b2-daylight-rayleigh-default-v1.png`。Rayleigh/Mie/AerialPerspective 改回默认后第一次验证：蓝天和绿色地形回归，证明橙黄不是贴图源、不是 HDR、不是截图路径，而是大气散射设置。
- `b2-daylight-jump-v1 @t12s`: D1=2 D2=1 D3=1 D4=0 D5=1 D6=1 D7=2 D8=1，均值=1.1。验证图：`Saved\OffscreenShots\b2-daylight-jump-v1.png`（2560×1440）。工作流 PASS：`scripts\offscreen-shot.ps1` 已默认用 `-CoasterStartSeconds=<WaitSeconds>` 直接跳到英雄帧附近，只截 `CaptureSeconds=1`，临时 `MovieFrame*.png` 自动清理；不再为 `WaitSeconds 12` 生成上千帧。画面 FAIL：仍为橙色天空 + 黑色峡谷剪影，B2 v1 物理光照/曝光改动未解决欠曝；下一步=继续诊断 sun direction / exposure method / SkyAtmosphere 高海拔交互。
- **【独立验收 / 人类判官 2026-06-18】A2 参数层 PASS，视觉层 BLOCKED**：核对 commandlet 确认世界 6.76×8.34km、垂直真实海拔 2600–7300m、分辨率 1009、样条已重定位谷底（X~216m），均符合规格；`YarlungTsangpo_hillshade.png` 经肉眼确认是真实峡谷河谷地形（非程序噪声）。但 `offscreen-smoke.png` 严重橙红欠曝，**D1 壮阔尺度无法判定**（同意 Codex 自评 D1≈2、均值~1.1，不注水）。过程认可：未作弊调色、未谎报 Done、offscreen 截图工具考虑周到。**裁决：橙黑是唯一挡路项 → 调整顺序，B2 光照提前到 A2 视觉验收之前。** 待跟进：hillshade 中部一条竖向 DEM 瓦片拼接缝，后续留意别在地表显形。 D1=2 D2=1 D3=1 D4=0 D5=1 D6=1 D7=2 D8=1，均值=1.1。A2 smoke：`scripts\offscreen-shot.ps1` 成功生成 `Saved\OffscreenShots\offscreen-smoke.png`（2560×1440，`-RenderOffScreen` + `-DUMPMOVIE`）。真实 DEM 轮廓/峡谷尺度已进入画面，但画面严重橙红/低曝光，近处地形与轨道关系仍不可信；下一步=继续 A2 调整轨道/地形 clearance 或构图，然后 B 阶段处理物理天空/曝光。
- `hero-a1-no-fake-sky-ridges @t12s`: D1=1 D2=1 D3=1 D4=0 D5=1 D6=1 D7=2 D8=1，均值=1.0。A1 验收：无方块云、无顶点色天穹、无平面远山；仍有程序化地形、橙色非物理天空、顶点色河道、无植被、方条轨道。根因/下一步=A2 真实 DEM 建地形轮廓；橙色天空留到 B 阶段物理光照/天空标定。
- `hero-baseline @t12s`: D1=1 D2=1 D3=1 D4=0 D5=1 D6=1 D7=2 D8=1，均值=1.0，短板=植被缺失/假天空云/顶点色河道/程序化坡面/方条轨道。根因/下一步=A1 删除假环境几何，A2 用真实 DEM 建立地形轮廓。

## 决策记录（不可逆/重要选择，最新在上）
- 2026-06-20（**继续修山体前先加 UT 与拆出 terrain relief 模块**）：用户要求先防 regression、再做较大重构。执行边界：把 `YarlungLandscapeImportCommandlet.cpp` 内嵌的 sub-30m relief 算法迁出到可单测的 `YarlungTerrainRelief`，把高度编码/河道中心线等共享事实放进 `YarlungTerrainProfile`，并让运行时 actor 与 commandlet 共用同一河道中心公式。新增 Automation tests 作为后续大改山体的护栏；这一步不做视觉评分、不改 mesh 资产、不宣称 C2 gate 通过。
- 2026-06-20（**C2 地形 mesh 几何路线校准：主 heightfield 加密已否决，下一步必须换表示**）：针对“要不要改地形 mesh 几何本身”，已实测两版 `SM_YarlungMeshTerrain` adaptive midpoint tessellation：约 115.5 万中心点、10.44M tris、Nanite 正常构建，但 first-person 英雄帧仍是大三角/竖切面，降低程序化位移后仍像低模折纸。裁定：**要改几何，但不能继续在同一张规则 single-value heightfield 主 mesh 上加密/调位移**；那只是把 30m DEM 的错误表示烤得更细。C2 若继续，只剩两条正路：① 非 heightfield 的 author cliff/rock/forest wall（真实岩壁/植被资产、手工雕或外援）；② 明确放弃 C2 台阶 gate，转 D/E 并据实保留 C not-Done。已回退本轮运行时代码和生成资产，只保留文档与截图证据。
- 2026-06-20（**战略裁定：不再受 heightfield 主表达限制**）：后续追照片级第一人称时，heightfield/DEM 只作为地理锚点、谷底高度、远景轮廓和 collision/reference，不再作为近中景山体的主视觉约束。已反证：宏观 height delta 仍被单值表面/30m 源折面限制，轻量 procedural ridge sheet 又读成幕布。可继续的路线只有两类：① 沿 `c2-erosion-relief-v7` 的已验证方向，把主 mesh 上的法线位移/材质 normal/strata/补光做扎实；② 对 hero/高风险走廊引入真正的岩壁/植被 asset volume（可用 Megascans/Fab/CC0 或外援建模），让第一人称可见山体成为 authored scenery，而不是 DEM heightfield 的奴隶。
- 2026-06-20（**迭代速度解耦第二层：map/scenery 可跳过 terrain mesh rebuild**）：`YarlungLandscapeImport` 支持 `-SkipTerrainMeshBuild`，复用现有 `SM_YarlungMeshTerrain`，但仍重新生成 `.umap`、hidden Landscape、ride/river/scenery actors 与 lighting/postprocess actor state。**规则**：heightfield、track clearance、terrain mesh displacement、vertex color、mesh material slot 或 Nanite settings 改动时禁止使用；只改 actor placement/scenery/water/light/camera/postprocess 时可用它做快循环。
- 2026-06-20（**迭代速度解耦：材质/光快循环不得重建 whole-world Nanite mesh**）：`SM_YarlungMeshTerrain` 是 208MB / 4.07M verts / 8.13M tris，commandlet 每次重建约 2–3 分钟且峰值物理内存约 13GB；材质/曝光小改不应触发它。新增 `scripts/import-yarlung-landscape.ps1 -SkipMapImport`：只更新材质资产并可 verify 现有关卡，实测 `-SkipAssetGeneration -ForceMaterials -SkipModels -SkipMapImport -Verify` 约 25.8s，且无 `Built Nanite Yarlung mesh terrain` 日志。**规则**：只有 heightfield/track/scatter/actor placement/mesh geometry 变化时才跑 full commandlet；材质、曝光、postprocess、lighting 参数快循环用 `-SkipMapImport` + offscreen screenshot。
- 2026-06-20（**插队:材质+光「先验证」一刀,暂停 chunk;reviewer 修正自己「先几何后材质」的顺序 — 用户「加入doc,指导codex」**）：看 WIP 视觉(committed `c2-sub30-rock-displacement-v2` + 未提交 `c2-nanite-cliff-chunk-v2`)后判断:**画面主因可能根本不是几何分辨率,而是「裸几何 + 暗平光」**。所有帧表面都是**无真实岩石 albedo/纹理的暗蓝灰纯几何着色 + 整体严重欠曝**(老 B2 曝光问题仍在);这种条件下**任何几何都读成折面**(唯一视觉信号是三角面明暗,没纹理打碎)。chunk v2 那块硬边角团块比 displacement **更假**=往坏走。**reviewer 修正**:我上轮定的「先打几何、材质留后」**顺序错了**——裸暗几何上**无法判断几何够不够**,材质+光在这里是**能否读成岩石的主因,不是抛光**。**插队指令(在更多几何/chunk 或手雕 fallback 之前先做)**:① **暂停 chunk**(`SM_YarlungNaniteCliffChunk` 这条在退化,先搁置);② **用现有 displacement 几何**测一次:给地形/崖一张**真岩石材质**(强 albedo + 强 normal + triplanar,不是现在隐约的 detail normal,用项目内最像岩石的贴图满强度)+ **修曝光/补光到正常日光**;③ **诊断分叉**:真材质+正常光下现有几何**读成岩石→几何轴本就够,gate 可过、省掉手雕**,材质/光才是真短板;若**折面仍主导→才真的是几何**,手雕 fallback 名正言顺。**禁区守护**:修曝光走 **exposure/补光/eye-adaptation**,**不得再调 SkyAtmosphere 散射(禁区①)**;真岩石材质是真内容非遮盖(非禁区②)。**这一刀比 chunk/手雕都便宜,且一次问清「是几何问题还是材质/光问题」。** 我对 WIP 不下正式 verdict(分工),此为插队指导。
- 2026-06-19（**台阶下一刀锐化:在 Nanite mesh 上「程序化解耦」近崖,先打几何、材质留后 — 用户授权「按你觉得合适的思路」,reviewer 定**）【**顺序已被上一条 2026-06-20 修正:材质+光先验证,再回判几何**】：外审 `nanite-mesh-terrain` 判 `f6b9c24` 基建 PASS(真 208MB Nanite mesh + 隐 Landscape = 真替换)但**台阶硬 gate 仍 FAIL**——根因是 Codex 做了 **DEM-忠实** meshing、**没执行用户「脱离DEM」**,忠实网格继承 30m 分辨率→大三角折面。**reviewer 决定(在程序化 vs 真岩壁资产之间选程序化)**:台阶/折面是**几何**问题,程序化 sub-30m 位移即可解,**不依赖外部资产**(已被 proxy-boulder/无真植被两次资产墙坑过,不再赌资产);真岩石材质/资产留作**后续质感升级**,与几何 gate 解耦——**先打几何,材质后补**。**这一刀边界**:① **保留**现有 Nanite StaticMesh terrain + `Landscape hidden`(真替换管道已对,别推倒);② **近崖走廊**(按 `yarlung-staircase-risk.csv` 标红段)**提高 mesh 密度**到能承载 sub-30m 细节,并注入 **DEM 没有的程序化岩石位移**(多倍频 fractal/岩层 strata/垂直断裂感),把大平三角打成岩石起伏=这就是「脱离DEM」落到 mesh 上;③ 法线由**位移后几何**重算(别再washed-out 平滑法线=v4 错误);④ 材质用程序化岩石(triplanar/vertex-color)v1 可接受。**位移硬约束**:只向**离轨/沿壁**方向加,**不得把壁推进轨道净空**(谷底走廊保持畅通)。**二元 gate(下轮我照此审)**:① **台阶+折面消失**——近壁第一人称(尤其风险 CSV 标红段)有岩石起伏、无大平三角、无梯田;② **真替换非盖**——维持 Landscape hidden、单层 mesh、无双面;③ **离轨截图**强制(证真几何非轨道视线皮);④ **不动轨道**——`verify-track-clearance.py` 仍 `violations=0`(位移没顶进净空);⑤ **材质诚实**——程序化岩石 v1 据实标「台阶 PASS / 岩面照片级 not-Done」,真岩石资产记 deferred 质感升级,不得拿 v1 充照片级。**不得**:回 Landscape LOD 调参 / 拿折面 DEM-mesh 充 Done / 用覆盖盖折面。**手雕 fallback(用户拍板「目前不考虑手雕,除非效果实在无法提升」)**:暂不做手动 editor 雕崖;**仅当程序化 sub-30m 解耦确实把折面提升到见顶、仍过不了 gate** 时,才升级到「人手在 editor 雕/摆真岩石 mesh 做英雄崖 + 受保护源资产、commandlet 不覆盖」。**升级阶梯**:Codex 跑程序化这一刀→若一刀后收益明显见顶仍 FAIL,**据实上报触发手雕 fallback**,不得无限盲调参,也不得在程序化没真试到顶前就跳手雕。详见 `docs/reviews/2026-06-19-nanite-mesh-terrain.md`。
- 2026-06-19（**台阶根治拍板:近崖走廊脱离 DEM、author mesh 岩壁、挖掉底层 Landscape 墙做真替换 — 用户拍板「可以脱离DEM,要解决台阶」**）【**已被上一条在 mesh 路线上锐化/落地:改为在 Nanite mesh 上程序化解耦近崖,不再单独 author 侧向崖墙**】：决战一刀(软化+proxy散布)经外审 `soften-scatter-binary-gate` 判定**基建 PASS/视觉 FAIL**,且「真植被」其实没真打(本地只有 boulder proxy)。台阶=**heightfield 表示天花板**已五次实验钉死。**用户拍板:脱离 DEM 解决台阶**(承接早先「照片自然性>原始地形忠实、DEM 仅大轮廓参考」决策,推到近崖)。**新方向**:① 近崖**走廊墙不再采样 `.r16`**,改 author 成**程序化 mesh 岩壁**(光滑/清晰岩面+真法线,可外凸,不受单值/30m/LOD 约束→台阶按构造消失);② **挖掉/隐掉走廊段底层 Landscape 墙做真替换**(非覆盖;可复用 `ApplyTrackClearanceCut` 类机制把该段 Landscape 压下去),**严禁两层**(mesh 浮在残留 heightfield 墙前=退化成禁区②的皮);③ DEM 仅留**大 valley/中远景**;④ 崖是**轨道侧向布景(离轨 100m+)、不在轨道底下**——改崖不动轨道净空。**这一刀正式激活原禁区③ deferred 的「mesh cliff 接管」**(用户拍板现在做),但**限走廊级 author mesh、非全世界 Nanite terrain 重建**(守住「别复杂化」)。**与被毙 fascia 的区别**:fascia 死在采样 DEM+浮起/歪斜/假法线=视角依赖皮;本方案脱 DEM 自由几何+真法线+真挖底层=真替换。**二元 gate(下轮我照此审)**:① **台阶消失(主目标)**——Outbound 英雄帧近崖无 heightfield facet/梯田;② **真替换非盖**——底层 Landscape 墙已挖/隐,贴近+离轨角度看**无双层/穿插**残留 heightfield 墙;③ **离轨截图**强制(侧/上方一张,证岩壁是真几何、任意角成立非轨道视线皮);④ **不动轨道**——`verify-track-clearance.py` 仍 `violations=0`;⑤ **材质诚实**——无真岩石资产时用程序化岩石材质可接受,但据实标「台阶 PASS / 岩面材质 v1 not-Done」,**不得拿假材质充照片级**(防 proxy-tint 翻版)。**禁区延续**:①不碰 SkyAtmosphere;②不靠覆盖/调色遮台阶(本方案是替换,底层必须真挖,否则退化成盖)。详见 `docs/reviews/2026-06-19-soften-scatter-binary-gate.md`。
- 2026-06-19（**细化:softening 仅铺垫,决战在「一刀做满真实散布」;停调 heightfield/LOD — 用户拍板**）【**台阶路线已被上一条取代:改为脱 DEM author mesh 岩壁真替换**】：本轮验证反证关键一条——**强制 LOD0 反而更糟**,证明短板**不是**「中景 LOD 低」而是 **heightfield 表示本身在近景掠射墙面上不成立**(30m DEM / 1009 grid 在第一人称掠射必现格子)。故定:① **停止再调 heightfield/LOD**(收益已证伪,低 ROI);softening 保留为铺垫即可。② 下一刀**一次做满、不再切片**:把 softening(铺垫)+ **满密度真实植被/岩石实例化林壁** + 撤 fascia **捆成一刀**,目标是把裸 heightfield 变成**湿润峡谷坡面**(真内容,非遮丑)。散布须**真实密度 + 物种/尺寸变化 + 岩石露头 + 地被**,重点压在**最掠射 Outbound 段**(决战帧=那一格),严禁礼貌性薄撒首过。③ **二元 gate**:满密度真散布若把近壁读成可信湿润森林峡谷壁(轨道帧 + 非轨道帧均成立)→ C 继续;若仍 facet/显假 → **立即 fail-exit**,C 停转 D/E,并记「近景崖真根治 = 回 **Nanite/mesh terrain(非 Landscape)**」为 deferred 正解。**一句话:战略 ok,softening 只是铺垫,成败看这一刀真实散布。**
- 2026-06-19（**台阶改思路：软化崖坡 + 真植被,撤掉 fascia mesh — 用户拍板「换思路、别复杂化」**）：fascia 表皮路线判定走不通(视角依赖、滑向禁区②、越修越复杂)。**根因重述**:台阶=逼 heightfield Landscape 渲染**近垂直墙**且从**掠射角**看;之前每招都在治症状。**新方向(攻成因,选项 A)**:① **软化崖坡**——`generate-yarlung-landscape-assets.py` 把英雄 Outbound 走廊的近垂直墙自然化为 heightfield 能干净渲染的**陡坡(目标 ≤~55-60°,掠射不再 facet)**,DEM 仅作大轮廓参考(承接 06-19「照片向自然化、不挖山适配轨道」决策);② **真植被/碎石 scatter**——按现有 forest/rock mask 在坡面铺**真实 instanced 植被**(Yarlung 本就森林峡谷),用真内容而非表皮化解 facet;③ **撤掉 `AYarlungCliffActor`/`YarlungCliffScenery`/`M_YarlungCliffRock`** 这条视角依赖表皮,不留皮。**这不是禁区②**:改的是场景**真实是什么**(自然陡坡+真森林),不是挂布盖缺陷;但植被须 gorge 级真实密度,不得只在缺陷处铺一条薄带充数。**管线顺序硬要求(否则轨道/净空/支撑会与新地形脱钩)**:先重生成地形→**再按新地形重生成 `YarlungTrack.csv`(含 terrain_z)**→`verify-track-clearance.py` 复跑须仍 `violations=0`(不穿山/净空/舒适 G 不退)→支撑/雾锚点已读 terrain_z 会自动跟上。**验收门槛(下轮外审照此验)**:① Outbound 英雄帧近壁**无近垂直掠射 facet**(台阶从成因消失,非被盖住);② **必出一张非轨道角度(侧/上方)截图**证明坡+植被从别的视角也读成自然;③ 植被是真实密度森林壁、非薄带;④ 数据层仍平滑、`verify-track-clearance.py` 仍 PASS;⑤ 无禁区①(SkyAtmosphere)/②(调色/几何遮盖)残留。**FAIL→出口**:若软化+植被后掠射壁仍 facet 或显假,按用户既定优先级**转 D/E 并据实记 C not-Done**,不再在崖上加复杂度。
- 2026-06-19（**崖 mesh「一刀 gate」— 用户拍板「给一刀」**）【**已被上一条取代:用户改判换思路、撤 fascia**】：外审 `stage-c-cliff-and-detail` 判崖 mesh `581ce5e` 为正路非掩盖、但 v1 是**视角依赖表皮**(浮真面上方 8.5m / 向轨道拉 52m / 整行统一朝轨道法线)。**用户决策：只给 Codex 再迭代一刀**,且必须朝**替换保真度**,不得朝更厚遮挡。**这一刀的硬验收门槛(全过才算成、任一不过即触发出口)**:① **真表面法线**——`YarlungCliffActor` 法线须由 mesh 几何(相邻顶点叉积)算,删除统一 `(-Right*0.86+Up*0.34)` 每行常量法线;② **贴近 flush**——`SurfaceLiftCm` 从 850 降到 z-fight 量级(≤~50cm),`TrackSidePullCm` 大幅降/归零,使崖不再是歪向相机的幕;③ **接缝**——mesh↔Landscape 接缝须收边(渐隐/裙边),或遮/切底层 Landscape 走廊条,使其不是「挂在台阶前的帘」;④ **离轨证据**——除轨道英雄帧外,**必须出一张非轨道角度(侧/上方)截图**证明崖从别的视角也读成真几何(不是只对轨道视线成立的表皮)。**FAIL→出口(用户既定)**:若这一刀后仍读成「另一种假」,**停手、不再迭代崖**,按优先级转 **D/E(水材质/植被/成像)**,C 维持 not-Done 据实记录。**不得**用加 lift/装饰岩块/降透明度去埋更多台阶充数(那是禁区②)。详见 `docs/reviews/2026-06-19-stage-c-cliff-and-detail.md`。
- 2026-06-19（powered coaster 物理闭环）：按上一条重审/用户拍板，车辆正式改为 **powered coaster**，不是纯重力车。实现要求：动力不是无限瞬时加速，而是有界 drive/brake 写入 verifier 与 runtime 同一套速度积分；主线路不得再靠 min-speed clamp。当前策略 A：Outbound/Return/Coast 有界 drive 追 `28m/s` 巡航，落差段超出目标时不刹车、保留重力刺激；Turnaround 有界控到 `12m/s`；Brake/Station 才允许停车。旧 `1.8m/s` 速度地板从主线物理中移除，数值 floor 降为 `0.2m/s`，并新增 `low_speed_samples`（主线 <8m/s）与 `stall_samples`（非 Brake/Station 停车）硬门槛。验证：`length=5055.2m speed=0.2-49.7mps mainline_min=9.99mps lat=1.01 vert=-0.89/3.07 long=1.06 airtime_samples=6 low_speed_samples=0 stall_samples=0 violations=0`；0.2m/s 只在 Brake/Station。P3 ride physical feasibility 阻断关闭；下一步回到 P4 风景/环境验证与照片化。
- 2026-06-19（**车辆改为带动力 powered coaster + 重审推翻「airtime 重做闭环」** — 用户拍板）：重审 `p3-airtime-redo`（`9715049`）速度剖面发现：airtime 本身**真做出来了**（camelback#1 vert_g=-0.89G、半径~2400m 平滑丘、40m/s，非假峰），但整条轨道**能量不可行**——列车约 2000m 抛锚，**780/1584(49%) 采样被钉在 1.8m/s min-speed clamp**（1585→4974m，含 355 个「1.8m/s 爬 10-26% 无动力上坡」物理不可能段），真 runtime 同样 Clamp，该 2.2km 实际龟速≈22.6min，后半 G 全在虚构速度下算、「0 violations」后半无意义。故 Codex 上一条「D9 阻断关闭/P3 可标 Done」**作废，P3 仍 Open**。**用户决策**：车辆改为**带动力 powered coaster，假设无尽续航**（这是真实存在品类，化解 5km 重力不可行）。选定动力策略 **A 调速巡航+重力刺激**：马达在平路/上坡维持目标巡航（约 28m/s，可调），落差处松手让重力把车推得更快（到舒适/max-speed 上限为止），airtime 丘按「到达时有速度」重调。**硬约束（不得再注水）**：动力必须建模成**真实有界 drive 推力写进物理积分**、**移除/降到不再兜底的 min-speed clamp**（速度必须真算出来）；纵向 G 仍 ≤2.5 包络（无尽=续航/里程，非瞬时无限加速度）。**新增验收门槛**：无动力维持段速度不得贴 min-speed clamp；后半 lat/vert G 须在真速度下重算；camelback#2/#3 重调到有速度处或删除；Turnaround（现 20-25m 半径）在真速度下重塑。详见 `docs/reviews/2026-06-19-p3-ride-pacing.md` 重审段。
- 2026-06-19（P3 airtime 重做闭环）【**已被上一条重审推翻：airtime 存在为真，但乘坐物理不可行，P3 未闭环**】：外审指出 `p3-airtime-final` 的均匀正弦丘只产生压缩、不产生 ejector airtime。已改为**早段集中落差建速 + 局部 camelback crest**：出程 300m 落差在 8%→36% 区间建立速度，第一段高速区用 raised-cosine crest 产生真实负 G；verifier 新增 `airtime_samples` 硬门槛（默认 Outbound 上 `vert_g<=-0.30` 至少 2 个样本），防止“舒适但平”的轨迹再次误判 PASS。最终验证：`length=5055.2m min_clearance=42.40m min_radius=19.7m max_grade=63.6% speed=1.8-49.7mps lat=0.72 vert=-0.89/3.07 long=1.06 airtime_samples=6 violations=0`；UE runtime 加载 198 点/5057.2m 新轨。P3 ride-experience / D9 阻断关闭；视觉台阶仍归 C/渲染材质账。
- 2026-06-19（P3 外审打回：舒适门禁 PASS，D9 airtime FAIL）：不继续把时间花在台阶 normal/LOD 上；LOD0 强制与 weak detail normal 试验均不能消除阶梯，且 weak normal 会放大规则纹理，未提交。`p3-airtime-final` 确实让轨道有压缩段并保持硬门禁 PASS（`length=5241.5m min_clearance=23.47m max_grade=51.8% lat=2.04 vert=0.17/2.47 long=1.07 violations=0`），但外审指出 **ejector airtime 未达成**：负 G 样本 0 次，`min vert=+0.17` 且在 Turnaround，不是设计 camelback crest。结论：舒适门禁可信化已闭环；P3 ride-experience 仍 Open。下一步应集中早期落差建速 + 局部抛物线丘顶，并重塑 Turnaround 半径（当前 min radius 8.7m 脆弱），而不是继续加均匀正弦幅度。
- 2026-06-19（**台阶根因再更新 + stage C scope 因重定位扩大** — 独立诊断）：`p2-longtrack-smoke` 右坡台阶经数据采样定位——P2 相机区段 `.r16` **数据平滑**（1.5km 单调爬升、无平台、60 quad 仅 1 次坡度变号），**台阶纯属渲染**（Landscape LOD 大三角 faceting + 无 detail normal + 掠射），不是高度数据。**更新 2026-06-18 根因**：至少在此段，瓶颈是 **normal/LOD/材质**、不是数据。**Ordering（便宜→根治）**：① 先打 Landscape **LOD/tessellation + 平滑法线**两张便宜牌（数据既好可能现在就消大半）；② 先确认 `.umap` 是当前 naturalized `.r16` 重导（排除陈旧导入）；③ 最近最掠射崖才到 stage C。**stage C scope 扩大**：重定位后 5km Outbound 段贴 600m 陡墙近距掠射跑一长段，原「一处英雄崖 deferred to C」不够，**C 实际需沿整条 Outbound 走廊做 detail/Nanite 接管**（detail-normal-only 不修几何 LOD 台阶，须到 Nanite mesh 接管才根治）；`photoreal-overhaul.md`/`acceptance.md` 的 C 文案后续按走廊级更新。
- 2026-06-19（P3 第一刀：真实速度剖面 + 曲率 banking）：用户将最终横向 G 门槛调整为 `|lat_g|≤2.5`。生成器不再写装饰性正弦 `roll_deg`，改为按平面曲率、`design_speed_mps=22`、`max_bank_deg=70` 计算 banking 并写入 `YarlungTrack.csv`；runtime 用 `GetGeneratedBankRadiansAtDistance()` 按距离插值 CSV roll。`verify-track-clearance.py` 废除 P1 草案的恒定 22m/s 横向 G 与空壳纵向 G，改为 station `4m/s` 起步、沿轨积分速度、按 banking 后座椅坐标计算 `lat/vert/long` G；默认门槛为垂直 `[-1.5,+5.5]`、横向 `≤2.5`、纵向 `≤2.5`。验证 PASS：`speed_range=1.8-35.0mps est_max_lat_g=0.54 est_vert_g=0.87/1.02 est_max_long_g=1.06 violations=0`；offscreen `p3-banked-longtrack-smoke.png` 有效。限制：这只关闭“舒适门禁可信化”，还没把 D9 节奏/airtime 调到好玩。
- 2026-06-19（P2 运行时接入长轨道 CSV）：`UCoasterTrackComponent::LoadGeneratedTrack()` 已读取 `Content/Generated/YarlungLandscape/YarlungTrack.csv`，按 CSV point section 生成距离区间，`ACoasterRideActor::RebuildSpline()` 不再回退 `DefaultTrackControlPoints()`；CSV 缺失/坏行/点数不足会报错并停止装载。`DefaultTrackControlPoints()` 仅保留为旧原型/显式测试资料。验证：UE build PASS；`generate-yarlung-track.py` 重新生成 90 点/约 5026m；`verify-track-clearance.py` PASS（`length=5031.8m min_clearance=22.49m violations=0`）；offscreen `p2-longtrack-smoke.png` 非空，日志显示 runtime 加载 `YarlungTrack.csv (90 points, 5039.6m)`，HUD 显示 `Section=Outbound / Track≈1849.7m`。
- 2026-06-19（P0/P1 初版落地）：新增 **P0/P1 implementation plan** `docs/plans/worlds-longest-p0-p1-implementation.md`。P0 初版完成行为保持式 C++ 边界抽取：新增 `UCoasterTrackComponent`、`ECoasterSection`、`CoasterBanking` helper，`ACoasterRideActor` 仍保留旧 8 点控制点/物理/视觉行为；编译通过，offscreen smoke `Saved\OffscreenShots\p0-p1-smoke.png` 非空有效。P1 初版完成离线生成+校验：新增 `scripts/generate-yarlung-track.py`、`scripts/verify-track-clearance.py`、`scripts/yarlung_track_lib.py`，生成 `Content/Generated/YarlungLandscape/YarlungTrack.csv`（90 点，约 **5026m**）并写 manifest `track` 块；校验 `samples=720 length=5031.8m min_clearance=22.49m min_radius=13.7m max_grade=21.7% est_max_lat_g=3.60 violations=0`，诊断图 `Saved\Diagnostics\track-clearance.png`、贴 hillshade 预览 `Saved\Diagnostics\yarlung-track-overlay.png`。用户允许「急一点」，所以 P1 draft 横向 G 门槛临时放宽到 `3.8`；P3 仍需用 banking/速度/turnaround 重塑收回最终舒适目标。
- 2026-06-19（**用户拍板，项目重定位 — 流程级、不可逆**）：目标从「峡谷里的 ~600m 小设施」重定位为 **世界最长(~5,000m)沿江往返(out-and-back)实景过山车 + 真物理动力学 + 风景优先**。优先级：安全/舒适 G 限(硬) > 长度(≥2,500m，目标~5km，硬底) > 风景 > 乘坐刺激。舒适包络定「更刺激」档（垂直[-1.5,+5.5]/横向≤2.5/纵向≤2.5，airtime ejector）。完整 codex-ready 设计见 **`docs/plans/worlds-longest-coaster.md`**。架构=离线轨道生成器(thalweg自动提取)+净空/舒适校验器(硬门禁，闭环未结外审的「全环不穿山」)+运行时数据驱动长轨道；先做 **P0 行为保持式 C++ 重构**(切 `UCoasterTrackComponent`/`ECoasterSection`/banking 纯函数，瘦身 802 行 god actor) ∥ **P1 离线生成器+校验器**。photoreal A–F 走廊范围与验收(新增 D9 乘坐/D10 尺度)受此约束。下一步=writing-plans 出 P0/P1 分步计划。
- 2026-06-18（A2 剖面诊断落地）：新增 `scripts\dump-yarlung-height-profile.py`，自动找 `.r16` 中最陡横向崖面剖面并输出 CSV/PNG。当前 1009 profile 显示：高度曲线连续、96/96 unique、无 8-bit/posterize 平台；但坡度有 bilinear 分段重复和 C1 断点。结论：**台阶不是高度量化 bug，而是 bilinear piecewise slope + heightfield 法线/材质暴露**。下一步仍是 bicubic/样条（直接针对 C1 断点），不是先升 2017；2017 如需只作对照。
- 2026-06-19（B-spline cheap fix 结果）：Catmull-Rom bicubic 在陡坡有过冲，剖面 kink 指标变差；改用 cubic B-spline 后数据剖面明显更平顺（同线 `kinks 10→2`）。但 1440p 英雄帧仍有近景巨大水平台阶，确认主因已经超出 DEM 插值：单值 heightfield 不能表达近垂直崖、30m 源分辨率不足、裸 macro 无 detail normal。**结论：不要继续在 2017/插值上耗时；近景崖归 stage C，A2 只按中远景/不穿山/贴地口径验收。**
- 2026-06-19（用户拍板，照片自然性 > 原始地形忠实；轨道适配地形）：用户明确要求“不用特别忠于原始地形，看着必须自然，像照片”。据 `a2-naturalized-v1` 观察，单纯 naturalize `.r16` 数据剖面有改善，但英雄帧近景货架墙几乎不变；结合 commandlet 逻辑，主因是 `ApplyTrackClearanceCut` 用窄半径把山体硬切到轨道净空高度，形成人工台阶峡谷。**流程级决策：DEM 只做大轮廓参考；地形生成要照片向自然化；更重要的是不要挖山适配轨道，而是让轨道高度/路径适配自然地形。** 下一步应撤掉/弱化 clearance cut，并按自然地形重新求轨道控制点 Z/XY（保证不穿山、贴近谷底/坡面、第一人称构图自然）。
- 2026-06-18（用户拍板，D1 验收口径调整 — 流程级、不可逆）：**A2-Done 的 D1 标准从"近景崖面无台阶/高频细节足"下调为"中远景峡谷轮廓/尺度正确、不穿山、轨道贴地"**。原因：台阶是单值 heightfield + 30m 源的固有上限，Landscape 怎么调都救不了近垂直崖面的 silhouette；强求会把 A2 永久卡死在一个本就属于 stage C 的问题上。**近景陡崖的高频细节明确 deferred 到 stage C**（detail normal 材质，必要时 Nanite cliff/rock mesh 接管走廊近景崖，Landscape 只负责中远景轮廓 + 谷底）。acceptance §3 的 D1 文案需同步。
- 2026-06-18（路径裁定：先便宜牌，不提前做 Nanite mesh）：台阶根因审阅后定序——**①剖面诊断 ②bicubic 上采样**两张便宜牌先打（成本几小时，可能让中远景"够用"）；**Nanite cliff mesh 虽是近景崖的正确答案，但不现在做**。理由：它是新工作流（找/雕岩石 mesh、沿走廊摆放、mesh↔Landscape 接缝混合、岩石材质），至少几天且不可逆；且无论如何都得先做 stage C 材质，Nanite 崖天然属于 C，不该插队。**仅当剖面诊断证明数据台阶 bicubic 也救不动、且英雄帧被近垂直墙主导时，才考虑把它拉到 C 之前。** 上采样代码在 `generate-yarlung-landscape-assets.py:sample_cm`（当前 bilinear `lerp`）。
- 2026-06-18（截图工作流默认切换）：低打扰 `scripts\offscreen-shot.ps1` 已连续验证可用，并且直接跳到英雄帧、只保留最终 PNG。默认验收路径改为 offscreen；删除旧窗口截图路径，避免后续误用抢焦点脚本。性能验收以后单独用 `stat unit`/CSV，不再和截图脚本绑定。
- 2026-06-18（B2 曝光收尾）：采用物理相机曝光补偿而非调色遮盖，`AutoExposureBias` 从 `0.0` 提到 `+1.2`，等效把当前 `f/11 · 1/500 · ISO100` 从 EV≈15.9 拉回晴天可读区间。`b2-daylight-exposure-v1` 肉眼与像素统计均确认亮度提升，橙黄未回归；B2 曝光子目标 PASS。阶段 B 仍未 Done，因为还缺体积云/大气透视/材质层次。
- 2026-06-18（B2 验收裁定 + 路径）：**B2 去橙根因判 PASS**（蓝天回归，见独立验收）。但 B2 整体未 Done，拆成两条明确下一步并定序：① **先收尾曝光**——当前 EV≈15.9 偏暗，调亮到晴天 EV~14.5–15（`AutoExposureBias`+1.0~1.3 或 ISO/快门等效），便宜且能立刻看清地形；② **再修 A2 高程台阶**——蓝天下暴露左壁"梯田台阶"，疑似 8-bit/posterize 量化，必须从 `.r16`/DEM 导入链根因修，**禁止用调色或几何遮盖掩盖**。D1 验收以台阶消失为前提。
- 2026-06-18（B2 根因，外援诊断验证）：橙黄天空/红褐地形根因是 `SkyAtmosphere` 散射被手调离物理默认：`SetRayleighScatteringScale(1.1f)` 约等于把默认 Rayleigh 放大数十倍，叠加错误的 `SetAerialPerspectiveStartDepth(9000.0f)`，导致大气像厚黄雾/夕阳。已删除 Rayleigh/Mie/AerialPerspective overrides，保留 UE 默认物理大气；以后不要在 C++ 里手调散射/星球参数。可动项限制为太阳角度/强度、相机曝光、作者化天空/云与合理薄雾。
- 2026-06-18（截图工作流修正）：`scripts\offscreen-shot.ps1` 默认不再按 `WaitSeconds` 全程模拟并 `-DUMPMOVIE` 每帧输出，而是把 `-CoasterStartRatio`、`-CoasterStartSpeed`、`-CoasterStartSeconds=<WaitSeconds>` 传给 runtime，直接把第一人称车推进到目标帧附近，再用 `CaptureSeconds=1` / `CaptureFps=1` 截短片段并自动删除源 `MovieFrame*.png`。如需旧行为，可显式加 `-SimulateWait`；如需保留源帧，可加 `-KeepSourceFrames`。
- 2026-06-18（用户拍板，顺序调整）：**B2 光照/曝光提前到 A2 视觉验收之前**。原因：A2 真实 DEM 参数层已验收通过，但画面严重橙红欠曝，无法肉眼判定峡谷尺度（D1）与轨道贴地；在黑暗里继续调 A2 几何是白费。先 B2 把场景照亮成晴天物理光照，再回头验 A2 地形。A 的其余项（A3 scatter 迁出）仍在 B2 之后按原顺序。
- 2026-06-18（低打扰截图实测）：`scripts\offscreen-shot.ps1` 可用。命令：`powershell -ExecutionPolicy Bypass -File scripts\offscreen-shot.ps1 -Name offscreen-smoke -WaitSeconds 2 -ResX 2560 -ResY 1440 -TimeoutSeconds 180`。输出：`Saved\OffscreenShots\offscreen-smoke.png`，源帧 `Saved\Screenshots\WindowsEditor\MovieFrame00843.png`。脚本修正：加 `-ForceRes` 才能稳定 2560×1440；`Start-Process` 的 `ExitCode` 可能为空，不能当失败。日志仍会出现 Slate window 生命周期记录，因此它是低打扰/offscreen 渲染路径，不等同于严格无窗口证明。
- 2026-06-18（后台执行）：A2 资产生成已跑通：`scripts/generate-yarlung-landscape-assets.py --source copernicus` 下载/缓存 Copernicus GLO-30 N29E094/N29E095 COG 到 gitignored `SourceAssets/DEM/CopernicusGLO30/`，生成 `Content/Generated/YarlungLandscape/YarlungTsangpo_1009.r16`、macro textures、`manifest.json` 和 `YarlungTsangpo_hillshade.png`。实采高程范围约 **2613m–7144m**，编码窗口改为 **2600m–7300m**，导入 scale 为 X≈6.70m/quad、Y≈8.27m/quad、Z≈917.97。轨道控制点已重定位到 `29.769–29.771N / 94.989–94.991E` 谷底附近，控制点 clearance 约 **18m–82m**；占位河高/river mask 同步到 DEM 谷底。UE 导入与截图未执行，避免抢占用户全屏。
- 2026-06-18（低打扰截图探索）：新增实验脚本 `scripts/offscreen-shot.ps1`，尝试用 `UnrealEditor-Cmd.exe -RenderOffScreen` + UE 自己输出 PNG/`-DUMPMOVIE`，避免旧窗口截图脚本的窗口创建和桌面抓图路径。尚未实测；若成功，可用于用户全屏游戏期间的视觉 smoke test。`-NullRHI` 仍只适合 commandlet/资产导入，不能用于照片级截图。
- 2026-06-18（用户拍板）：**A2 选定子区域=大拐弯最深峡谷段**（南迦巴瓦↔加拉白垒之间，世界最深）。bbox `lat 29.745–29.820°N, lon 94.945–95.015°E`（~8.3×6.8km，谷底最深点 29.7697N/94.9899E 居中、加拉白垒作远景雪峰、落差~4500m）。DEM 源=Copernicus GLO-30 优先（ALOS 备选，不用 SRTM）。分辨率维持 1009。**垂直 1:1 真实尺度**：改 commandlet `EncodedMinZ/MaxZ` 为真实海拔(当前 2600–7300m)。导入前先出 hillshade 预览确认河道/雪峰。细节见 plan A2。
- 2026-06-18（用户拍板）：**A2 尺度方案=真实尺度子区域**。近 1:1 导成公里级 Landscape，崖壁/雪山保持真实尺度，过山车样条重定位到峡谷中。**否决"压进 164m 玩具尺度"方案**（Codex 曾算 1083km²→30,668m²，水平~212×/垂直~114× 压缩，第一人称读成桌面沙盘）。轨道样条坐标随新尺度重定位是 A2 核心工作。
- 2026-06-18：A1 完成，代码移除 `SkyDomeMesh`、`CloudLayerMesh`、`DistantRidgeMesh` 及对应 build 函数；验证图 `Saved\hero-a1-no-fake-sky-ridges.png`。不在 A1 里用调色修橙色天空，避免偏离阶段顺序。
- 2026-06-18：阶段 0 英雄段固定为 `WaitSeconds 12`；验收基线图为 `Saved\hero-baseline.png`。候选图为 `Saved\hero-candidates-t3s.png`、`t6s`、`t9s`、`t12s`、`t15s`、`t18s`。
- 2026-06-18：参考锚点使用 `docs/refs/references.md` 外链；public repo 不直接下载普通参考摄影，除非逐文件 license 允许再分发。
- 2026-06-18：旧的 C++/资产未提交改动已丢弃；后续植被/岩石/水体散布应进入 commandlet/PCG/Foliage/独立 scenery actor，而不是 `ACoasterRideActor`。

## NEEDS-HUMAN / Blocked 待办
- [x] **尺度方案**（阶段 A2）：已定=真实尺度子区域（见决策记录）。
- [x] **DEM 选型 + 裁哪段**（阶段 A2）：已定并已生成资产（见决策记录与 plan A2）。源=Copernicus GLO-30；bbox=`lat 29.745–29.820°N, lon 94.945–95.015°E`；分辨率维持 1009；垂直真实海拔编码 2600–7300m；轨道样条已重定位；hillshade 已检查。
- [x] **A2 UE 导入 + smoke 截图**：`scripts\import-yarlung-landscape.ps1 -Verify` 已成功，`offscreen-shot.ps1` 已产出 smoke PNG。
- [x] **B2 橙黄根因（颜色）**：删 SkyAtmosphere 手调散射 + 默认物理大气 + SkyLight realtime capture，蓝天/绿地恢复，独立验收 PASS；截图 `b2-daylight-rayleigh-default-v2`。
- [x] **B2 曝光收尾**：`AutoExposureBias +1.2`，截图 `Saved\OffscreenShots\b2-daylight-exposure-v1.png`；蓝天/岩壁进入晴天可读区间，像素均值提升且橙黄未回归。
- [x] **台阶根因定性**：已排除 8-bit/posterize/nearest（`.r16` 真 16-bit ~62k unique；`main` 当前采样为 bilinear，conditioning 属于未合入实验）。定性为 heightfield + 30m 源 + 法线折痕 + 无 detail normal 的固有上限（见决策记录）。
- [x] **下一步①：剖面诊断**：`scripts\dump-yarlung-height-profile.py` 已完成；结论=无硬平台量化，存在 bilinear 分段坡度/C1 断点，继续 bicubic。
- [x] **下一步②：bicubic/B-spline 上采样**：已改 `generate-yarlung-landscape-assets.py:sample_cm` bilinear→cubic B-spline，重出 `.r16` 并重导入。数据剖面改善，视觉近景陡崖仍不达标；2017 不再作为默认路线。
- [ ] **下一步③：轨道适配自然地形**：撤掉/最小化 `ApplyTrackClearanceCut`，不要再硬挖山；基于 naturalized heightmap 重新计算/调整 `YarlungCoasterProfile.h` 控制点 Z/必要 XY，使轨道不穿山、离地合理、第一人称近景读成自然坡面。
- [ ] **【硬门槛 / 独立审阅 2026-06-19】A2 放行前必做：全环不穿山诊断**：clearance cut 已设半径=0 完全关闭，且轨道仅 8 控制点段间线性 lerp、运行时无贴地/clamp，单机位英雄帧无法证明整圈不穿山。须沿样条密采样地形 vs 轨道 Z（仿 `dump-yarlung-height-profile.py`）或多机位绕圈截图，证明全程轨道在地形之上且离地合理。建议同时保留一个很小的 inner clearance 半径作微小安全兜底（决策记录原意），而非完全=0。
- [ ] **A2 Done（新口径）**：用 hero `WaitSeconds 12` 或补充中远景取景验**中远景峡谷轮廓/尺度正确、不穿山、轨道贴地**即可标 Done；近景若仍有材质细节不足 deferred 到 C，但**人工货架墙/硬切槽不能放行**。**追加（独立审阅）：必须先过上一条全环穿山诊断；单机位截图不算数。**
- [x] **低打扰截图实测**：已成功。推荐命令：`powershell -ExecutionPolicy Bypass -File scripts\offscreen-shot.ps1 -Name <name> -WaitSeconds <seconds> -ResX 2560 -ResY 1440`。注意这是 offscreen/低打扰 smoke path，最终验收仍需确认画面有效并按量规打分。
- [ ] **英雄段人工确认**（阶段 0）：已先选 `WaitSeconds 12` 作为最佳努力默认；如用户想换英雄段，再重新截图并更新本文件。
- [x] **【重定位 2026-06-19】P0：C++ 行为保持式重构（初版）**：已切 `UCoasterTrackComponent`+`ECoasterSection`+`CoasterBanking` helper；编译通过；offscreen smoke `Saved\OffscreenShots\p0-p1-smoke.png` 非空。未做逐帧像素 diff，P2 前可接受。
- [x] **【重定位 2026-06-19】P1：离线轨道生成器 + 净空/舒适校验器（初版）**：已生成 5.03km `YarlungTrack.csv`，manifest `track` 块已写入，净空校验 PASS（min clearance 22.49m）。P1 draft 曾用 `3.8` 横向 G 临时门槛；P3 已废除该例外，当前最终横向门槛为 `2.5`。
- [x] **【重定位 2026-06-19】P2：运行时接入长轨道 CSV**：`LoadGeneratedTrack` + 距离分段 + RebuildSpline 已接入；Yarlung runtime 路径要求 `YarlungTrack.csv`，缺失/解析失败不回退旧 8 点短环。验证：build PASS、净空校验 PASS、offscreen `p2-longtrack-smoke.png` 显示 `Section=Outbound / Track≈1849.7m`。
- [x] **【外审 p0-p1 条件 / 阻断标 Done】乘坐舒适门禁可信化**：`verify-track-clearance.py` 已替换空壳纵向 G 与恒速横向 G，改用真实速度剖面 + CSV banking 后座椅 G；当前最终门槛 `lat≤2.5/long≤2.5/vert[-1.5,+5.5]` PASS（0 violations）。后续 P3 powered 已补齐 D9 airtime 与全环物理可行性。
- [x] **【外审 p0-p1 条件 / 阻断标 Done】风景未验证（overlay/几何关系轴）**：旧 `yarlung-track-overlay.png` 过糊的问题已由 `p4-scenery-overlay` 诊断闭环；随后 river mask 改为沿 DEM thalweg 生成，`river-mask samples=187/198(94.4%)`，轨道/谷底/江道关系可读。注意：这里只关闭“是否贴江/是否可读”的几何关系阻断，不代表照片级风景 Done；真实水体、材质、植被、近景崖仍属 P4/C/D/E。
- [x] **【外审 p0-p1 建议】P3 几何/节奏收口**：舒适门禁 + banking + D9 airtime + powered 物理可行性已完成。最终 P3 powered：`samples=1584 length=5055.2m min_clearance=42.40m min_radius=19.7m max_grade=63.6% speed=0.2-49.7mps mainline_min=9.99mps lat=1.01 vert=-0.89/3.07 long=1.06 airtime_samples=6@<=-0.30G low_speed_samples=0 stall_samples=0 violations=0`；runtime 加载 198 点/5057.2m。外审指出的负 G 0 次、Turnaround 8.7m 半径热点、1.8m/s clamp-masked stall 均已闭环。
- [ ] **【C/E 资产门】真实植被/岩壁资产**：`c1-full-scatter-v1` 证明本地 boulder-only 资产无法读成林芝密林，也无法解决近崖台阶。若继续 C/E 的“密林/岩壁遮蔽+细节”路线，需要授权可用的真实树/灌木/岩壁资产（Fab/Megascans/CC0）；没有资产时应按 fail-exit 转 D/E（水、轨道/车、成像），不要继续用 proxy 假装植被。
- [ ] **【重定位 2026-06-19】P4–P5**：走廊照片化(含环境迁出) / 验收增 D9 D10。P3 powered ride-experience 已闭环；下一步转 P4 风景/环境验证与走廊照片化，不要把视觉台阶误归 P3。

## 最终总结（到位后填写）
- （未到位）
