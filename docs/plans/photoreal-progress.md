# 照片级迭代进度（当前仪表盘）

> 这是断点续跑的状态文件。任何 agent 开工前先读这里确定"现在在哪、下一步做什么"，收工前写回。
> 历史长日志已归档到 `docs/plans/archive/photoreal-progress-pre-p0p1-cleanup-2026-06-21.md`。
> 流程：`photoreal-overhaul.md` · 验收：`photoreal-acceptance.md` · 操作：`AGENTS.md` · 脚手架：`codex-iteration-scaffold.md`
> 视觉方向复审：`visual-false-negative-audit.md`（否定路线前必须先排除 pipeline/资产/遮挡/截图 bug）

## 当前裁决

- **总体视觉：FAIL**。目标仍是第一人称 5km 雅鲁藏布/林芝风景过山车，画面要接近 `docs/refs/local/02_fp_yarlung_coaster_canyon.png` 和 `docs/refs/local/03_fp_mountain_coaster_valley.png`，不是"比原型好"。
- **当前技术路线**：只做第一人称视锥/轨道走廊；轨道是风景叙事/相机编排工具，不再限定沿江往返，可跨江、高架俯视、贴崖 overbank、俯冲或短暂翻滚；不回到 square full-map / UE Landscape 渲染路径；不恢复旧 8 点短环；不靠全图 fallback。
- **当前高收益方向（2026-06-21 改定：冲 AAA 照片级，走 Megascans-first）**：引擎已 AAA-ready（Lumen/Nanite/VSM/VT 全开），瓶颈是内容资产。旧程序化 canyon-wall / procedural river actor / gate-only UE Water actor 已从默认链路删除；不再给它们加 mask/材质/补丁。主线转向资产化：Nanite 岩壁散布、instanced 森林树冠、多层地表材质、单一显式 river surface 真实江水/结构。计划与资产清单见 `docs/plans/aaa-asset-pipeline.md`。
- **当前无人值守入口**：先跑 `.\scripts\yarlung-agent-status.ps1`，再按推荐命令执行 `.\scripts\iterate-yarlung.ps1`。触碰 terrain/water/config/生成关卡行为时，加跑 `.\scripts\test-yarlung.ps1 -Build`。每轮必须看 contact sheet 图片后再写结论。

## 最新记录

- **2026-06-22 authored slope patch / rock-wall kitbash（PARTIAL，真实接入坡面 kitbash；总体仍 FAIL）**：按上一轮结论做真正的 authored slope patch / rock-wall kitbash，而不是继续调 terrain vertex color。新增 `slope_patch_belt` placement 和 `EYarlungSceneryPlacement::SlopePatchBelt`：配置显式给 sample ranges、lateral bands、occupancy、track/river clearance、height/slope/scale/jitter，缺字段或非法值继续 fail-close；`YarlungSceneryActor` 新增 `SlopeRockWallA/B` 与 `SlopeForestPatchA` HISM 组件，沿指定轨道段两侧按近中景 bands 放置 Megascans quarry/nordic cliff 和 PN spruce half patch，并复用同一套 terrain height、river field、slope、track clearance resolver。`inspect-yarlung-map.py` 已把 `SlopeRockWall*` 纳入真实 rock/cliff 材质检查，避免默认灰材质 silent pass。迭代结论：v1/v2 证明远处 patch 不足以打掉主视锥 smooth green slope；v3 放开近侧 band 后绿色占比明显下降，但暴露 `spruce_half` 近景会读成横向木架/悬浮枝干，这是资产语义问题；v4 把 forest patch 推回中远景，只让 rock-wall 负责近景破面，最终实例约 `slope_rock_a=2011 / slope_rock_b=1966 / slope_forest_a=1474`。验证：JSON parse PASS；`git diff --check` PASS；C++ Build PASS；`iterate-yarlung.ps1 -Mode Actor -Preset Standard -Build -NamePrefix slope-kitbash-v4` PASS，map inspect 0 error/0 warning；contact sheet `Saved\Diagnostics\slope-kitbash-v4.png` 已 Read，RiskGate=WARN，worst=t90 risk **1.183** / green **0.082** / flat **0.645**（上一轮 `terrain-macro-patch-v1` worst risk **1.224** / green **0.370** / flat **0.713**）；`test-yarlung.ps1 -Build` 10/10 PASS。肉眼：河谷两侧岩壁更连续，t90/t150 裸绿坡被实物岩壁打碎，水面仍可见；但 t30 仍有大块 smooth corridor slope，t150 右侧近景 rock-wall 仍有重复/侧面读感，整体离参考图的湿岩密林坡面还有距离。下一步最高收益：继续把 slope patch 从“密集 HISM bands”推进到更作者化的分段模块/局部构图重排，或转水面 flow normal/reflection/depth；不要把 spruce-half 当近景坡面补丁使用。
- **2026-06-22 terrain macro patch + horizontal clearance bugfix（PARTIAL，小幅改善主坡；总体仍 FAIL）**：继续 AAA 视觉，按 `visual-false-negative-audit.md` 先区分 bug 与方向问题。观察 `asset-layers-final-v1` 最差 t30/t90/t150：主视锥仍是大片 smooth green corridor terrain，资产主要在远处/河岸。先修一个真实 placement bug：scenery belt 的轨道净空用 `StaticMesh.Bounds.SphereRadius * scale` 当水平半径，导致高树和竖直 cliff 的高度被误算进水平 footprint，近中坡会被过度清空；已改为 `BoxExtent.X/Y` 的水平半径。验证 `horizontal-clearance-v1`：Build/inspect/visual PASS，实例数小幅增加，但肉眼几乎不变，说明它是 correctness hardening，不是主视觉解法。随后做一次 targeted asset-density 配置实验 `slope-patch-v1`：cliff/forest/canopy 数量显著上升，但肉眼变成近景黑岩块/暗墙，仍没有参考图的湿岩密林坡面；按新 gate 归因为 `CONTENT-LIMIT / placement 不佳`，已回退配置，不保留。最终保留 terrain 生成层改动：`YarlungColorAtPosition()` 增加中坡/近河谷 macro patch mask，把湿岩、苔藓、碎石与 rock-surface alpha 混入主坡，避免纯顶点色平刷。验证：JSON parse PASS；`git diff --check` PASS；C++ Build PASS；`iterate-yarlung.ps1 -Mode Terrain -Preset Standard -Build -NamePrefix terrain-macro-patch-v1` PASS，map inspect 0 error/0 warning，contact sheet `Saved\Diagnostics\terrain-macro-patch-v1.png` 已 Read，RiskGate=WARN，worst=t30 risk **1.224**（上轮 asset-layers-final-v1 worst 1.243），flat **0.713**（上轮 0.725），green **0.370**（上轮 0.353）；`test-yarlung.ps1 -Build` 10/10 PASS。肉眼：左/右主坡多了一点湿斑和地表变化，没有黑墙回归，但仍明显是 smooth corridor terrain，不是 AAA。下一步最高收益：停止用大 cliff slab 或纯颜色继续硬推；做真正 authored slope patch/rock-wall kitbash（小模块、多断面、贴坡/沿等高线），或者重排英雄段让第一人称少看大平刷坡，同时继续水面材质级 flow normal/reflection/depth。
- **2026-06-22 visual false-negative audit protocol（DOCS，防止误杀高天花板方向）**：复盘过去被否掉或暂缓的路线后，结论是多条方向曾被 pipeline bug 污染判断：水体不可见/双水体/法线平面/SingleLayerWater 输出错误、cliff face 被坡面法线放倒、canopy 净空缺失、支撑阴影穿地、terrain 材质未接 Megascans surface、曝光/Rayleigh/cached pre-exposure 等。新增 `docs/plans/visual-false-negative-audit.md`，规定以后否定视觉方向前必须记录 mismatch、隔离证据、归因类型（`BUG` / `CONTENT-LIMIT` / `DIRECTION-LIMIT`）和后续动作；`AGENTS.md` 同步接入该 gate。当前裁决：旧 `YarlungCanyonWallActor`、旧 `YarlungRiverActor`、gate-only UE Water 双水体不恢复；但“单一显式水面 + 真实材质”、“连续 Nanite rock-wall kitbash”、“真实 foliage/forest-floor/scree 层”、“蓝天白云/大气复审”、“跨江/高架/贴崖路线构图”都不能按过去失败实验判死。下一步视觉仍优先山体/河岸 authored patch 与水面材质级 flow normal/reflection/depth。
- **2026-06-22 continuous Nanite cliff / forest-floor / scree layer（PARTIAL，资产层更连续；总体仍 FAIL）**：按“先上连续 Nanite cliff / forest-floor / 碎石资产层”的方向推进山体 AAA。新增固定 HISM 组件 `ScreeBoulders`、`CliffRockFacesF`、`ForestFloorHalfA/B`，`Config/yarlung-assets.json` 接入 Megascans Beach Boulder、Nordic Forest Cliff Large 和 PN spruce half meshes；新增 `ground_cover_belt` placement，沿轨道走廊 deterministic lateral bands 铺 talus scree 与半高林层，并让 placement 尊重每个 kind 的 `min_lateral_cm/max_lateral_cm`，避免全局 band 把不同资产语义混在一起；map inspect 同步把 `ScreeBoulders` 纳入真实 Fab rock/cliff 材质检查，继续 fail-close。迭代结论：v1 随机 scatter 太碰运气；v2 拉近地表层后主坡仍裸；v3 把 cliff belt 前移/放大、提高 occupancy，实例进入可读层级（约 scree=2770，cliff_a-f=1156-1344/组，forest_half_a/b=2407/2346），t90/t150 河谷对岸形成更厚连续岩壁，t150 右坡有碎石/岩面读感。另试过把 `M_YarlungMeshTerrain` 的 Megascans surface detail 拉到 0.82，肉眼和 risk 都变差（坡面更暗更平），已回退到稳定材质强度；保留贴脸修正：generic cliff `track_clearance_cm` 260m 级并降低最大 scale、canopy `track_clearance_cm` 提到 320m 级，避免 t60 左侧大黑块。验证：JSON parse PASS；`git diff --check` PASS；C++ Build PASS；`iterate-yarlung.ps1 -Mode Material -Preset Standard -Build -NamePrefix asset-layers-final-v1` PASS，map inspect 0 error/0 warning；contact sheet `Saved\Diagnostics\asset-layers-final-v1.png` 已 Read，RiskGate=WARN，worst=t30 risk **1.243**；`test-yarlung.ps1 -Build` 10/10 PASS。肉眼：这轮把“孤立岩块”推进成更连续资产层，但 t30/t120/t180 仍有大块 smooth corridor terrain 主坡，离参考图的密林湿岩峡谷还远。下一步最高收益：不要继续盲目增加 HISM 数量；应做坡面材质/几何 breakup 的 authored patch（rock-wall kitbash/forest-floor decals 或局部构图重排），并继续水面材质级 flow normal/reflection/depth。
- **2026-06-22 river-wall cliffs + visible-normal rockmask（PARTIAL，河谷岩壁体量增加；总体仍 FAIL）**：继续 AAA 视觉主线，先按当前瓶颈处理“山体像平刷坡、岩壁只是孤立块”的宏观问题。判定旧 `cliff_belt` 仍是沿轨道两侧撒点，不足以形成参考图那种顺河谷连续湿岩崖壁；新增 `AddRiverWallCliffs()`，从生成的 `FYarlungRiverField` 取真实河线，按河道切线两侧布置 Megascans cliff face，保持世界竖直并面向河心，同时投影回轨道 corridor 使用同一套地形高度、坡度、河道/轨道净空门。`Config/yarlung-assets.json` 新增必填 `river_wall_*` 参数，配置缺失/非法继续 fail-close；map inspect 中 cliff 实例约从上轮 **495/420/493/483/476** 提升到 **688/569/664/651/640**。第二个管线问题：terrain 湿岩/rock surface mask 原先用原始 DEM 平滑法线，忽略 corridor profile、relief、river carve 后的可见地形法线，导致生成陡坡仍可能按“原始缓坡”上森林色/森林纹理；已改为用最终 mesh normal 驱动 `YarlungCanyonWetRockMask()`，并小幅增强湿岩/碎石混合。第三刀尝试把 `YarlungTerrainRelief` 从约 11m 最大位移推到中等 26.5m（旧强雕刻曾造成折纸/条纹，这次不拉满），但肉眼只见小幅坡面变化，未解决 t180 裸坡。验证：JSON parse PASS；`git diff --check` PASS；C++ Build PASS；`iterate-yarlung.ps1 -Mode Terrain -Preset Standard -Build -NamePrefix macro-relief-riverwalls-v1` PASS，terrain 1,018,081 vertices / max_displacement_cm=2653.9 / Nanite true；`visual-survey.ps1 -Times 60,120,180,240 -NamePrefix macro-relief-riverwalls-v1-wide` PASS，contact sheet `Saved\Diagnostics\macro-relief-riverwalls-v1-wide.png` 已 Read；`test-yarlung.ps1 -Build` 10/10 PASS。肉眼：t60/t120/t240 的河谷两侧更有真实岩块体量，水面主视觉保持可见；没有回到旧硬条纹/折纸墙。仍未到 AAA：t180/t120 大面灰绿裸坡仍明显，岩壁是更密的资产层而不是真正连续 authored cliff system，水面仍缺 flow normal/reflection/depth。下一步最高收益：继续做山体可见坡面的连续 Nanite cliff/forest-floor/碎石资产层或局部构图重排；不要把当前 relief 参数当作主要解法。
- **2026-06-22 water lit geometry pass（PARTIAL，水面有流向纹理但 D3 仍 FAIL）**：继续 AAA 视觉，先处理当前最显眼的水面“青色哑光平片”。尝试把 `M_YarlungWaterSurface` 设为 UE `SingleLayerWater` 暴露出真实管线坑：UE 5.8 运行时直接报 `SingleLayerWater materials requires the use of SingleLayerWaterMaterial output node`，截图 gate 因 `Default Material will be used in game` 正确 fail-close；已撤掉半接的 SingleLayerWater，并在材质生成脚本里显式把 shading model 复位到 `MSM_DEFAULT_LIT`，避免旧 asset 状态残留。保留的视觉改动：`M_YarlungWaterSurface` 降 roughness/提高 specular、加弱 Fresnel grazing highlight；`SM_YarlungRiverSurface` 顶点色更深，几何波面从过大版本 v2 回收为受限扰动（下凹 clamp -24cm，上拱 88cm），避免河床/岸坡露底斑块。验证：Python compile PASS；`git diff --check` PASS；C++ Build PASS；`import-yarlung-corridor.ps1 -SkipAssetGeneration -SkipTrackGeneration -ForceMaterials -SkipTerrainMeshBuild -Verify` PASS；第一次 `water-singlelayer-v1`/`water-lit-geometry-v1` 被 gate 拦截，根因已修；最终 `visual-survey.ps1 -Times 60,120,180,240 -NamePrefix water-lit-geometry-v3` PASS，contact sheet `Saved\Diagnostics\water-lit-geometry-v3.png` 已 Read；`test-yarlung.ps1 -Build` 10/10 PASS。肉眼：v3 没有 v2 的棕色露底破片，水面比 `single-river-surface-v1` 多了流向/急流纹理，但仍像有纹理的平面水，不是参考图里的真实乳白青绿湍流；D3 不能标 Done。结论：Default Lit + 顶点/几何只能做到 partial；真正下一步应接材质级 flow normal / 反射 / depth-fade 或正确的 SingleLayerWater output node，而不是继续扩大几何波幅。
- **2026-06-22 single river surface cleanup（OK，删掉第二套水体；视觉不变）**：确认当前确实存在两套 water：`WaterZone + WaterBodyRiver` 只作为 gate-only 系统 actor 保留，真正可见的是 `SM_YarlungRiverSurface` 显式斜面水面。按“项目干净更重要 / fail-close”裁决，删除 gate-only UE Water 链路，避免双水体语义继续制造误判：删除 `YarlungWaterBuilder.cpp/.h`，`YarlungLandscapeImportCommandlet` 不再 spawn `WaterZone/WaterBodyRiver`，`.uproject` 移除 Water 插件，`CoasterSim.Build.cs` 移除 `Water` 模块依赖；`Config/yarlung-assets.json` 的 water 配置只保留 `surface_material/width_scale/min_width/max_width`，删掉 `river_material`、zone RT、depth、velocity、shape dilation、audio 等 UE Water-only 参数；`inspect-yarlung-map.py` 反向 fail-close，默认关卡出现 `WaterZone` 或 `WaterBodyRiver` 直接失败，并继续要求唯一 `YarlungRiverSurface` actor 可见且使用 `M_YarlungWaterSurface`；`inspect-yarlung-assets.py` 只检查一个 water surface material。同步更新 `AGENTS.md`、`photoreal-overhaul.md`、`aaa-asset-pipeline.md`，当前江水主线是显式 river surface 上做真实材质/法线/反射，不再是 UE Water hybrid。验证：JSON parse PASS；Python compile PASS；`git diff --check` PASS；C++ Build PASS；`import-yarlung-corridor.ps1 -SkipAssetGeneration -SkipTrackGeneration -SkipMaterials -SkipTerrainMeshBuild -Verify` PASS，重建 map 后 inspect 0 error/0 warning 且无 `WaterZone` / `WaterBodyRiver`；`test-yarlung.ps1 -Build` 10/10 PASS；`visual-survey.ps1 -Times 60,120,180,240 -NamePrefix single-river-surface-v1` PASS，contact sheet `Saved\Diagnostics\single-river-surface-v1.png` 已 Read，水仍可见。结论：这刀不是视觉提升，而是删掉不合逻辑的第二水体和配置复杂度；下一步视觉继续做水材质法线/反射/透明深度或山体连续岩壁。
- **2026-06-22 cliff orientation pipeline bugfix（OK，岩壁不再按坡面放倒；总体仍 FAIL）**：继续视觉主线，并按用户要求区分“bug”与“真实效果不足”。先做隔离诊断：当前正常帧 `diag-cliff-current-v1` 与 `-YarlungHideTerrain` 帧对比表明，Megascans cliff/forest 确实在渲染，但只是孤立块状散布，不是被 terrain 完整遮住；随后把 `cliff_belt` 单纯加密/拉近的 config 实验 `cliff-continuity-config-v1` 判定失败并回退，说明“继续撒更多同类点状 cliff”不是正确方向。进一步读 `AYarlungSceneryActor::AddCliffBelt()` 发现真实管线 bug：大尺度 cliff face 和小石头一样使用 `SurfaceAlignedRotation(Normal, Yaw)`，会把竖直岩壁的 Up 轴贴到地形法线，容易让岩壁倾倒、插坡或朝向轨道而非河谷。处理：cliff belt 现在用河道最近点计算面向河谷的 yaw，并保持世界竖直；boulder/shrub 等小物仍保留 surface align。验证：`git diff --check` PASS；C++ Build PASS；`visual-survey.ps1 -Times 60,120,180,240 -NamePrefix cliff-upright-river-facing-v1` PASS，contact sheet `Saved\Diagnostics\cliff-upright-river-facing-v1.png` 已 Read，worst=t60 risk **1.267** / flat **0.471**；`test-yarlung.ps1 -Build` 10/10 PASS。肉眼：t60/t240 的近岸岩体/崖壁更合理，少了被坡面放倒的读感；但 t120/t180 仍然有大面积光滑灰绿山坡。结论：岩壁 orientation 是 bug，已修；剩余问题不是“没渲染/被挡住”，而是内容表达层仍缺连续 Nanite cliff wall、坡面碎石/苔藓/林海体量和更强山体作者化。下一步不要再靠 scatter 密度或相机视线掩盖，应做连续岩壁/坡面资产层或重构山体可见坡面。
- **2026-06-22 terrain rock surface layer（PARTIAL，地表材质管线更真实；总体仍 FAIL）**：继续视觉主线，目标是处理 t120/t180/t240 大面积 flat 灰绿山坡。先确认本地已有可用 Megascans 岩石纹理：`Quarry_Cliff_uchwaffda` 4K base/normal/ORM 和已有 `Forest_Floor_sfjmafua`。发现当前 `M_YarlungMeshTerrain` 只有单套 `terrain.surface`，且是 forest floor；岩坡只能靠 vertex RGB 和同一套地表纹理，远看必然平刷。处理：`Config/yarlung-assets.json` 新增必填 `terrain.rock_surface`；`create-coaster-materials.py` 改为 fail-close 加载 forest + rock 两套 texture，并用 terrain vertex alpha 在两者之间混合 base/normal/ORM；`YarlungCorridorImportCommandlet::YarlungColorAtPosition()` 把 vertex alpha 正式定义为 `RockSurfaceMask`（由 canyon wet-rock、陡坡、湿岸和 ravine 组合），同时把宏观 palette 调到更可读的湿岩/苔藓/林带范围；`inspect-yarlung-assets.py` 检查两套 terrain surface。验证：JSON parse PASS；Python compile PASS；`git diff --check` PASS；C++ Build PASS；第一次 material rebuild 暴露 UE Lerp 节点不能继续取 `G/R` 分量，已改成先对两套 ORM 的 `G/R` 标量分别 Lerp；`import-yarlung-corridor.ps1 -SkipAssetGeneration -SkipTrackGeneration -ForceMaterials -Verify` PASS，terrain 1,018,081 vertices / Nanite true；`visual-survey.ps1 -Times 60,120,180,240 -NamePrefix terrain-rock-layer-v2` PASS，contact sheet `Saved\Diagnostics\terrain-rock-layer-v2.png` 已 Read；`test-yarlung.ps1 -Build` 10/10 PASS。肉眼：山坡比上一版略亮，右侧远坡出现更多湿岩/条带和苔藓读感，水/轨道构图保持；但这不是质变，t120/t180 仍然有大面积 corridor mesh 平坡，离参考图里的连续真实崖壁/林海细节还远。结论：地表材质层管线已经补上，下一步不要继续只调 terrain material；最高收益应转到连续 Nanite cliff kitbash/坡面岩壁 actor，或英雄段构图重排，减少主视锥里的大平刷坡。
- **2026-06-22 canopy/track proxy visual pipeline bugfix（OK，t60 黑树墙根因修复；总体仍 FAIL）**：继续按“视觉异常先查管线 bug”诊断。对 t60 做隔离：当前 `diag-t60-current-v1` 风险 **1.610**，画面像相机钻进黑树冠；`-YarlungHideScenery` 后风险降到 **1.352** 且河谷打开；`-YarlungHideRide` 后黑树墙仍在，排除轨道层为主因。代码根因：`cliff_belt` 已按 mesh bounds * scale 做 `track_clearance_cm` 过滤，但 `canopy_belt` 没有同等轨道净空门；当前 canopy 侧向带从 45m 起、scale 最高 5.35、近带 occupancy=1.0，大树冠可侵入第一人称走廊。处理：`FYarlungCanopyBeltConfig` 新增必填 `track_clearance_cm`，配置缺失/非法 fail-close；`AYarlungSceneryActor::AddCanopyBelt()` 按 mesh sphere radius * max scale 过滤轨道净空，`Config/yarlung-assets.json` 设为 260m。顺手修掉一个新暴露的 track proxy 视觉污染：远处高架支撑线没有距离裁剪且每 96m 一组，第一人称读成水面/山体上的细黑线；现在支撑 proxy 有 300m-600m cull、间距 220m、颜色改成较淡钢色，rails/track 主体保持可读。验证：JSON parse PASS；`git diff --check` PASS；C++ Build PASS；`visual-survey.ps1 -Times 60,120,180,240 -NamePrefix visual-pipeline-bugfix-v1` PASS，contact sheet `Saved\Diagnostics\visual-pipeline-bugfix-v1.png` 已 Read；`test-yarlung.ps1 -Build` 10/10 PASS。肉眼：t60 不再是全屏黑树墙，中心河谷/水面/高架轨道读出来；t120/t180/t240 维持林谷与江水构图。仍未到 AAA：左侧近景树仍偏黑，水面仍偏哑光，最大瓶颈转回山体大面 flat 灰绿、连续 Nanite 岩壁/地表 breakup 不足。下一步最高收益：做山体中远景连续岩壁/湿岩碎石/苔藓层，或重排英雄段构图减少大平刷坡，不要继续只调曝光。
- **2026-06-22 track proxy shadow/ground penetration bug（OK，去掉直线阴影污染；总体仍 FAIL）**：按“基于视觉继续找管线 bug”做隔离诊断。假设：画面里的长直灰线/三角阴影来自 ride/track visual 层。验证：`diag-hide-ride-v1` 隐藏 ride 后 t120/t180/t240 的长直线和大块灰色投影基本消失，risk 从当前正常图约 1.3 降到 1.06/0.67/0.53；`diag-hide-scenery-v1` 反而保留并暴露轨道支撑/阴影污染，说明根因不是山体/树。代码根因：`CoasterTrackVisuals` 的 support foot 被写成 `TerrainRefZ - 6000cm`，每根支柱故意插进地面 60m；这些程序化细管 proxy 还默认投射 VSM 阴影，远景读成黑色直线/三角架，污染水面和山体。处理：support foot 改为 `TerrainRefZ + 45cm`；程序化中心脊/护栏/枕木/斜撑/支撑关闭 cast shadow，保留可见红轨结构，避免未 authored 的 proxy shadow 主导画面。验证：C++ Build PASS；`git diff --check` PASS；`visual-survey.ps1 -Times 60,120,180,240 -NamePrefix track-shadow-bugfix-final` PASS，contact sheet `Saved\Diagnostics\track-shadow-bugfix-final.png` 已 Read；`test-yarlung.ps1 -Build` 10/10 PASS。肉眼：t120/t180 的巨大直线阴影/灰色投影基本消失，轨道仍可读，山水主体更干净；t60 仍偏暗且左侧树墙/岩体过重，水仍缺真实反射/深度。下一步继续按视觉找 bug：优先查 t60 近景黑墙/岩体材质或 foliage/shadow 链路，而不是调曝光。
- **2026-06-22 river surface normals + patch foam（PARTIAL，水面少了“硬青片/车道线”；总体仍 FAIL）**：继续按“视觉异常先想 bug”处理水面。排查 `SM_YarlungRiverSurface` 发现已有 `RippleZ` 几何起伏，但 mesh build 时所有 vertex instance tangent space 都强制写成 `UpVector + XAxis`，实际法线仍按平面吃光；这解释了水有高度扰动却仍像硬片。处理：`BuildYarlungRiverSurfaceStaticMesh()` 现在缓存 vertex positions，按相邻 along/across 点计算真实 per-vertex normal/tangent，再写入 triangle instances。第二个问题是 foam 由一维 sine 生成，远看像平行白色车道线；已改为 flow/across 2D patch noise 打碎泡沫，水色整体更深，foam 顶点从 **204 → 89**，避免大面积泛白。验证：C++ Build PASS；`git diff --check` PASS；`import-yarlung-corridor.ps1 -SkipAssetGeneration -SkipTrackGeneration -SkipMaterials -SkipTerrainMeshBuild -Verify` PASS；`visual-survey.ps1 -Times 60,120,180,240 -NamePrefix water-patch-foam-v1` PASS，contact sheet `Saved\Diagnostics\water-patch-foam-v1.png` 已 Read；`test-yarlung.ps1 -Build` 10/10 PASS。肉眼：水面连续白条明显减少，t120/t180/t240 的水不再那么刺眼，但仍缺真正反射、透明深度和小尺度法线贴图；这刀只能算水体管线修正，不是 D 阶段完成。下一步水面若继续推进，应做材质级 flow normal / reflection / depth fade，而不是继续增加 vertex foam。
- **2026-06-22 terrain surface texture wiring（PARTIAL，确认 flat 山体是材质链路 bug/gap；总体仍 FAIL）**：按“视觉不 work 先怀疑 bug”的原则诊断 `water-terrain-exposure-v3`。隔离 t60：`-YarlungHideScenery` 后仍是大块平涂绿灰 terrain/water；`-YarlungHideTerrain` 反而构图更通；单纯 `-YarlungExposureBias=1.35 -YarlungSkyLightIntensity=0.95` 只会照亮同一块平刷面，说明根因不是曝光。真正问题：已有 Megascans `Forest_Floor_sfjmafua` surface 资产在 `Content/Fab/Megascans/Surfaces/...`，但 `M_YarlungMeshTerrain` 仍只接 vertex color * scalar，没有任何 texture/normal/ORM。处理：`Config/yarlung-assets.json` 新增 `terrain.surface`，把 base/normal/ORM/tiling/strength 纳入 checked config；`create-coaster-materials.py` 改为 fail-close 加载三张 texture，缺失或类型错误直接失败，并把 terrain 材质改成“宏观 vertex color + Megascans 微表面 + normal + ORM roughness/AO”；`inspect-yarlung-assets.py` 增加 terrain surface 资产检查；`import-yarlung-corridor.ps1` 把 `Config/yarlung-assets.json` 纳入 material rebuild dependency，避免配置变了但旧材质缓存继续用。验证：JSON parse PASS；Python compile PASS；PowerShell parse PASS；`git diff --check` PASS；`import-yarlung-corridor.ps1 -SkipAssetGeneration -SkipTrackGeneration -ForceMaterials -SkipTerrainMeshBuild -Verify` PASS；`iterate-yarlung.ps1 -Mode Material -Preset Standard -NamePrefix terrain-surface-texture-v1 -Times 60,120,180,240` PASS，RiskGate=WARN，contact sheet `Saved\Diagnostics\terrain-surface-texture-v1.png` 已 Read；`test-yarlung.ps1 -Build` 10/10 PASS；terrain asset inspect commandlet PASS。肉眼：t120/t180/t240 的山坡有了微表面纹理和法线读感，未明显棕化，flat 从纯平涂改善；但 t60 仍被近景树墙/深阴影主导，水面仍像亮青硬片，山体仍缺连续真实岩壁/碎石/苔藓多层。下一步最高收益：继续把山体从“贴纹理的 corridor mesh”推进到连续 Nanite 岩壁/decals/坡面 kitbash，同时单独做水面材质法线/反射/深度，不要再用曝光调参掩盖。
- **2026-06-22 water/terrain color + exposure pass（PARTIAL，水和山体更有方向；总体仍 FAIL）**：继续视觉主线，先用 `visual-baseline-697c4d4` 复查当前画面。肉眼结论：最明显的宏观问题是水面占画面主体但像乳蓝硬片，远坡仍有薄荷绿平刷感；t60 近景森林/阴影偏黑。处理：`SM_YarlungRiverSurface` 从 17 cross lanes 加到 33 lanes，水面宽度略收窄，允许水面 mesh 重算法线；顶点色改为更深的青绿主水色、窄中心急流和断续岸边泡沫，foam 顶点从约 983 压到 **204**，避免整片泛白。`M_YarlungWaterSurface` roughness 0.44→0.24、specular 0.55→0.78。terrain vertex color 降低 steep slope 的森林底色权重，让真实树木承担森林体量，裸坡底色回到湿岩/暗绿地表；同时小幅提高成像通透度（ExposureBias 0.75→0.95、SkyLight 0.56→0.66、vignette 0.18→0.12）。验证：Python compile PASS；`git diff --check` PASS；C++ Build PASS；完整 `import-yarlung-corridor.ps1 -SkipAssetGeneration -SkipTrackGeneration -ForceMaterials -Verify` PASS，terrain 1,018,081 vertices / Nanite true，river surface rows=213 cross_lanes=33；`iterate-yarlung.ps1 -Mode Actor -Preset Standard -Build -NamePrefix water-terrain-exposure-v3 -Times 60,120,180,240` PASS，RiskGate=WARN，contact sheet `Saved\Diagnostics\water-terrain-exposure-v3.png` 已 Read；`test-yarlung.ps1 -Build` 10/10 PASS。肉眼：t120/t180/t240 的水有更明确流向和河槽边界，t240 峡谷纵深更接近参考；t60 仍偏暗且近景树墙遮挡重，水面仍缺真正反射/法线贴图/透明深度，整体未到 AAA。下一步不要继续只加泡沫线；优先做局部构图/近景遮挡、真实水材质法线/反射，或更大收益的岩壁/地表资产层。
- **2026-06-22 function/variable naming pass（OK，语义更清楚；视觉不变）**：继续按“阅读所有代码，修复 code smell，命名消歧义”清理 live 链路内部命名，重点是避免函数/变量名把当前 corridor StaticMesh 管线误导回旧 Landscape/heightfield 思路。改动：`YarlungCorridorProfile::AuthoredHeightCm()` 改为 `CorridorTerrainHeightCm()`；`FYarlungRiverField::ProjectContentRiverPath()/LoadFromProjectContent()` 改为 `GeneratedRiverCsvPath()/LoadGeneratedCsv()`；scenery/import commandlet 的 `LoadHeightmap/LoadYarlungHeightmap` 改为 `LoadCorridorSourceHeights`；局部 `HeightData` 改为 `EncodedHeights`，明确这是 r16 source grid 编码值，不是已生成可见地形；import 脚本 `$MacroTextureSources` 改为 `$CorridorSourceOutputs`。验证：PowerShell parse PASS；`git diff --check` PASS；旧名残留扫描 PASS（无 `AuthoredHeightCm/LoadFromProjectContent/ProjectContentRiverPath/LoadHeightmap/LoadYarlungHeightmap/MacroTextureSources/HeightData` live 命中）；C++ Build PASS；`test-yarlung.ps1 -Build` 10/10 PASS；`import-yarlung-corridor.ps1 -SkipAssetGeneration -SkipTrackGeneration -SkipMaterials -SkipTerrainMeshBuild -Verify` PASS，日志确认 `YarlungCorridorImport`、river surface/scatter/map inspect 成功。生成校验触碰的 `.uasset/.umap` 已还原，避免无语义二进制 diff。本轮不改视觉、不重评截图；下一步继续 AAA 视觉主线。
- **2026-06-22 corridor naming refactor（OK，消除旧 Landscape 语义；视觉不变）**：按“阅读所有代码，修复 code smell，命名消歧义”扫描 `Source/ scripts/ Config/ docs/plans` 的 live 链路，发现最大命名 smell 是当前可见地形已是 generated corridor StaticMesh，但核心 commandlet / 脚本入口仍叫 `YarlungLandscapeImport`、`import-yarlung-landscape.ps1`、`generate-yarlung-landscape-assets.py`，容易让后续改动误回 UE `ALandscape` 或旧 full-map 思路。本轮不留兼容层，改为 `YarlungCorridorImportCommandlet.*`、`-run=YarlungCorridorImport`、`scripts/import-yarlung-corridor.ps1`、`scripts/generate-yarlung-corridor-source-assets.py`；同步把 `YarlungGeneratedPaths` 的 map/dir 常量改为 Corridor 命名，并把 source generator manifest 从 `unreal_import.landscape_section_size` 改为 `corridor_static_mesh.source_grid_quads_per_axis`。当前 AGENTS / photoreal-overhaul / worlds-longest-coaster 已同步新入口；历史 archive/review/progress 旧名保留为当时证据。验证：Python compile PASS；PowerShell parse PASS；`git diff --check` PASS；C++ Build PASS；`test-yarlung.ps1 -Build` 10/10 PASS；新入口 `import-yarlung-corridor.ps1 -SkipAssetGeneration -SkipTrackGeneration -SkipMaterials -SkipTerrainMeshBuild -Verify` PASS，日志确认 `-run=YarlungCorridorImport`、`Imported Yarlung corridor map`、map inspect `Success - 0 error(s), 0 warning(s)`。本轮不改视觉、不重评截图；下一步继续视觉主线。
- **2026-06-22 generated path contract hardening（OK，防路径漂移；视觉不变）**：继续按“防范代码质量导致 bug”的方向收紧 C++ 生成物合同。新增 `Source/CoasterSim/YarlungGeneratedPaths.h`，把 live C++ 使用的 `Content/Generated/YarlungLandscape/*`、`/Game/Generated/YarlungLandscape/*`、`/Game/Generated/Materials/*` 路径集中为一个命名空间，避免 ride、scenery、river field、commandlet、track visuals 各自硬编码导致路径漂移。已替换 `CoasterRideActor`、`CoasterTrackVisuals`、`YarlungLandscapeImportCommandlet`、`YarlungRiverField`、`YarlungSceneryActor` 里的 live 路径读取；`rg` 扫描确认 C++ live 路径只剩集中 header，其他命中是 CSV 注释/错误文本。验证：`git diff --check` PASS；C++ Build PASS；`test-yarlung.ps1 -Build` 10/10 PASS；`import-yarlung-landscape.ps1 -SkipAssetGeneration -SkipTrackGeneration -SkipMaterials -SkipTerrainMeshBuild -Verify` PASS，map inspect `Success - 0 error(s), 0 warning(s)`。本轮不改视觉、不重评截图；下一步继续视觉主线，优先宏观构图/山体表面/水面可读性。
- **2026-06-22 layer misuse / fail-open 架构扫描（OK，继续收紧；总体仍 FAIL）**：按“这种用错层等不合逻辑的架构设计还有吗”做 live 链路扫描，重点查 `Source/ scripts/ Config/` 里把 ride 语义误用于视觉/地形、以及缺输入后继续跑的路径。修掉三类真实问题：① `generate-yarlung-landscape-assets.py` 的 cliff-slope softening 仍只读取 `Outbound` track guide，属于和 t180 scenery bug 同类的 section 层错用；已改为读取全 `YarlungTrack.csv`，manifest key 改为 `ride_corridor_cliff_softening`，并删除“无 track guide 也继续生成”的兼容路径，缺 guide 现在直接失败。② `CoasterRideActor` / `CoasterTrackComponent` 不再在 track CSV、section ranges、roll samples、terrain samples 缺失时用 1cm track / Coast / zero bank / zero terrain 继续跑，改为 fatal fail-close。③ `YarlungAtmosphere` 缺 river CSV 时不再静默关闭 fog，除显式 `-YarlungHideFog` 诊断外必须有 river input；`YarlungSceneryActor` 缺 track/heightmap/river field 时也 fatal，不再清空实例假成功。验证：Python compile PASS；`git diff --check` PASS；C++ Build PASS；full `import-yarlung-landscape.ps1 -ForceAssetGeneration -ForceTrackGeneration -ForceMaterials -Verify` PASS，track verify `violations=0`、min_clearance≈71.61m、scatter `canopy_a/b/c=5706/5720/5713`；`test-yarlung.ps1 -Build` 10/10 PASS；已 Read `Saved\Diagnostics\arch-layer-hardening-v1.png`。肉眼：没有明显视觉回归，t180/t240 正常，t60 仍是偏暗/近景密度问题。扫描后仍保留的合理 layer split：section 只服务 ride physics/HUD/gates；scenery/terrain coverage 走全轨道/river field；UE Water 仍是 gate-only 系统 actor，可见水面由显式 river surface mesh 承担。下一步视觉继续处理 t60 宏观构图/曝光和山体表面。
- **2026-06-22 scenery 全轨道覆盖修复（OK，t180 裸坡根因修掉；总体仍 FAIL）**：用户追问 t180 是否是旧代码 bug，确认是。根因不是“分 section”本身，而是 `YarlungSceneryActor` 把 section 误用成造景白名单：只散布 `Lift/Outbound/Turnaround/Return/Launch`，跳过 `Brake/Station`，所以 t180 HUD 显示 `Section: Brake` 时会出现异常裸坡。处理：`LoadOutboundTrack()` 改名为 `LoadSceneryTrack()`，删除硬编码 section 过滤，scenery 现在读取整条 `YarlungTrack.csv` 的所有样本；section 继续保留给物理、速度段、刹车/launch、HUD 和路线语义，不再控制视觉覆盖。最终 scatter 约：`riverbank=881`、`cliff_a/b/c/d/e=576/470/563/556/545`、`canopy_a/b/c=5707/5720/5713`。验证：C++ Build PASS；`import-yarlung-landscape.ps1 -SkipAssetGeneration -SkipTrackGeneration -ForceMaterials -SkipTerrainMeshBuild -Verify` PASS；`visual-survey.ps1 -Times 60,120,180,240 -NamePrefix scenery-all-sections-v1` PASS，并已 Read `Saved\Diagnostics\scenery-all-sections-v1.png` 与 t180 单帧；`test-yarlung.ps1 -Build` 10/10 PASS。肉眼：t180 两侧森林/岩壁体量明显恢复，不再是“Brake 段没造景”；当前最弱帧转为 t60，主要问题是偏暗、近 cliff/forest 过密和 corridor terrain 表面仍不够真实。下一步继续按视觉优先处理 t60 构图/明暗/山体表面，而不是再按 section 补丁式加白名单。
- **2026-06-22 forest massing + riverbank/rockwall pass（PARTIAL，林海体量提升；总体仍 FAIL）**：按“最高收益”优先处理山体/河岸宏观读感，而不是继续调轨道。新增 `RiverbankBoulders` HISM 组件，接入 Fab/Megascans beach boulder 资产；`canopy_belt` 外扩到 4.5m-222m 多条侧向带、降低河岸/高度过滤、提高树冠尺度与占有率；`cliff_belt` 拉近到 45m 起步并提高 occupancy/scale，用已有 Nordic/Quarry/Alaskan cliff 资产增加中景岩壁破碎度。最终 scatter 约：`riverbank=879`、`cliff_a/b/c/d/e=495/420/493/483/476`、`canopy_a/b/c=5011/4958/4974`。验证：JSON parse PASS；C++ Build PASS；`import-yarlung-landscape.ps1 -SkipAssetGeneration -SkipTrackGeneration -ForceMaterials -SkipTerrainMeshBuild -Verify` PASS；`test-yarlung.ps1 -Build` 10/10 PASS；已 Read `Saved\Diagnostics\forest-rockwalls-final.png` 和关键单帧。肉眼：t60/t120/t240 的森林体量、水面 framing、峡谷纵深明显改善，t240 已接近“高架俯视林谷江水”的正确方向；但 t180 仍是大片灰绿裸坡，说明关键剩余瓶颈不是 scatter 数量，而是 corridor terrain 自身的山体表面/材质与英雄段地形构图。失败实验：短暂加入 `SlopeCanopyTrees` scatter，实例数约 2304 但 t180 几乎无改善，已撤回以避免无效复杂度/性能成本；临时“灰化河岸 vertex-color”也被肉眼判定变脏并回退源码。下一步最高收益：针对 t180 这类裸坡做山体表面方案（宏观湿岩/碎石/苔藓材质层、坡面友好的连续 Nanite rock-wall kitbash、或局部地形/路线构图重排），不要继续盲目加树。
- **2026-06-21/22 authored scenic track + red steel track visual（OK，构图明显进步；总体仍 FAIL）**：按“过山车轨道好好修，自己凹出参考图里的形状；轨道建模也要精致些”改路线与轨道外观。旧 `scenic-freeform` 仍像算法小环/低空贴水，本轮改成参考图式 authored beats：山腰长下坡、跨河 S、对岸高弯、回跨贴崖，并保留 DEM/净空/G 值 gate。关键结果：`generate-yarlung-track.py` 默认生成 **5447.83m / 226 points**，`verify-track-clearance.py` PASS：min_clearance=**71.64m**、max_grade=**31.2%**、est_max_lat_g=**1.48**、est_vert_g=**-0.36/1.45**、airtime_samples=**4**、violations=0；`inspect-yarlung-spatial-contract.py --times 60,120,180,240` 从 2/4 提升到 **4/4** 河流入镜。轨道视觉从灰黑双管升级为生成式红色钢结构：红色双轨 + 中脊 + 横梁 + 三角斜撑 + 收窄侧护栏 + 蓝绿色支柱，仍由 `CoasterTrackVisuals` 随 CSV 自动生成，不手摆地图。验证：C++ Build PASS；`import-yarlung-landscape.ps1 -SkipAssetGeneration -SkipMaterials -SkipTerrainMeshBuild -Verify` PASS；`test-yarlung.ps1 -Build` 10/10 PASS；已 Read `Saved\Diagnostics\authored-red-track-v2.png`。肉眼：红轨、俯视河谷、S 形高架感明显更接近参考图；仍未到 AAA，主要短板是山体/河岸仍像灰绿 corridor mesh，远山/云层/真实岩壁不足，且轨道还缺车体/座舱、安全杆、检修平台细节。
- **2026-06-21/22 cliff clearance + terrain color pass（PARTIAL，修掉 t60 黑色巨板；总体仍 FAIL）**：重新诊断 `failclose-no-fallback-smoke` 之后 t60 几乎全黑的问题，分别用 `-YarlungHideScenery` / `-YarlungHideTerrain` 验证根因是 cliff belt 巨块过近过大，不是曝光或水 bug。处理：`Config/yarlung-assets.json` 把 cliff belt 推到更远侧向带、缩小 scale、降低 occupancy，并新增 required `track_clearance_cm=65000`；`YarlungSceneryActor::AddCliffBelt()` 按 mesh bounds * scale 做轨道净空过滤，过近直接不 scatter。`aaa-cliff-clearance-v1` 肉眼确认 t60 黑色 slab 消失。随后做地形顶点色与材质增益 pass：`YarlungTerrainBreakup()` 加宏/中/细噪声，森林/湿岩 palette 更深，`TerrainAlbedoGain=0.78`；`aaa-cliff-terrain-vfinal` 肉眼只有轻微改善，t120/t240 仍是大面积 flat 灰绿坡。失败实验：尝试用现有 Wheat Grass 做 ground cover，`aaa-groundcover-v1` 变成巨大棕黑尖刺/脏杆，已完全回退，live config/source 无 `GroundCover` / `ground_cover` / `Wheat_Grass` 残留。结论：山体不能靠地表草小补丁解决，下一步应上真实 forest floor/rock surface/decals/连续 Nanite 岩壁，或继续修路线构图与资产层。
- **2026-06-21/22 runtime fallback fail-close 清理（OK，管线更硬；总体仍 FAIL）**：按“怎么还有这么多 fallback？去掉，要 loudly fail / fail close”裁决，继续收紧 live 链路。`Config/yarlung-assets.json` 已无 `material_usage_roots` 这类宽泛扫描面；`create-coaster-materials.py` 只从 checked-in `scenery.components[].mesh` 的实际 StaticMesh material slots 推导需要打开的 `InstancedStaticMeshes` / `Nanite` usage，mesh 缺失、非 StaticMesh、空 material slot、材质实例 parent chain 断裂或循环都直接失败。`scripts/import-yarlung-landscape.ps1` 与 `scripts/offscreen-shot.ps1` 现在扫描 UE stdout/stderr/log，只要出现 `Default Material will be used in game`、`missing usage flag`、`LogMaterial: Error`、Yarlung asset load failure 等默认材质替换/材质错误模式就抛错停止；检测器命名改为 runtime asset substitution，避免把 gate 本身误读成 fallback。C++ 侧继续 fail-close：Yarlung asset config、scenery mesh、tint material、track tint material、tube mesh、terrain/water 必需材质、visible cloud material 都缺失即 Fatal；默认大气云不再从 `/Engine/EngineSky/...` 偷材质，云隐藏则不加载，云若要显示必须显式有材质。扫描结论：`rg fallback Source scripts Config` 已无 live 命中；剩余只有历史文档证据和 gate 里的 forbidden UE pattern 字符串。验证：JSON parse PASS；Python compile PASS；PowerShell parse PASS；`git diff --check` PASS（仅 CRLF 提示）；C++ Build PASS；`import-yarlung-landscape.ps1 -SkipAssetGeneration -SkipTrackGeneration -ForceMaterials -SkipTerrainMeshBuild -Verify` PASS，material/map inspect 都 `Success - 0 error(s), 0 warning(s)`；`test-yarlung.ps1 -Build` 10/10 PASS；`visual-survey.ps1 -Times 60,120 -NamePrefix failclose-no-fallback-smoke` PASS 并已 Read `Saved\Diagnostics\failclose-no-fallback-smoke.png`。肉眼：本轮不是视觉完成，t60 仍几乎全黑/被遮挡，t120 水沿河槽可见但山体仍 slab/flat；下一步视觉应优先修 t60 occlusion / cliff belt 与路线构图，再继续山体 AAA。
- **2026-06-21/22 全图偏灰 / Rayleigh 回归排查（PARTIAL，蓝天链路 OK；总体仍 FAIL）**：用户指出“全图一直偏灰，之前修好 Rayleigh 时天是蓝的”，本轮按 bug 处理而不是继续看指标。考古确认旧根因是 `SkyAtmosphere` 非物理 Rayleigh/Mie/AerialPerspective override；当前 `YarlungAtmosphere` 代码没有这些 override，Rayleigh 未回归。排查中发现一个真实诊断管线 bug：`-YarlungHideFog` 会被 `StartRideAt -> RebuildSpline -> AnchorFogToGeneratedRiver` 重新打开，batch 每张截图都会失效；已改为 `AnchorFogToGeneratedRiver()` 在命令行带 `-YarlungHideFog` 时直接保持 fog hidden 并记录日志，`diagnose-atmo-nofog-v2` 日志确认每次重定位都不再重开 fog。视觉结论：真正无雾图与默认差异很小，说明全图偏灰不是 Rayleigh/雾主因，而是近轮把 terrain wet-bank 灰岩/材质增益/水色压成大面积低饱和灰青。处理：默认 light/camera 回到更晴天的中档（Sun 100k lux、SkyLight 0.56、ExposureBias 0.75、轻薄远雾），收窄湿岩岸带、增强森林/苔藓覆盖、减少雪/灰岩泛白，水面降低 emissive/roughness、提高 specular 并回到青绿深水。验证：`python -m py_compile scripts/create-coaster-materials.py` PASS；`git diff --check` PASS；C++ Build PASS；`import-yarlung-landscape.ps1 -SkipAssetGeneration -SkipTrackGeneration -ForceMaterials -Verify` PASS；`import-yarlung-landscape.ps1 -SkipAssetGeneration -SkipTrackGeneration -ForceMaterials -SkipTerrainMeshBuild -Verify` PASS；已 Read `Saved\Diagnostics\diagnose-atmo-nofog-v2.png`、`Saved\Diagnostics\macro-sunny-material-v1.png`、`Saved\Diagnostics\macro-sunny-material-v2.png`。肉眼：v2 蓝天稳定、水和林坡比 v5 不再死灰，v1 过曝已压回；但山体主体仍是单层 corridor mesh，t120/t240 大面坡仍偏灰绿且 flat，离参考图的连续真实湿岩/密林/白云仍远。下一步优先做连续 Nanite 岩壁/真实 foliage 资产层和路线构图，不要再把问题误判成 Rayleigh。
- **2026-06-21/22 水面管线收口 + 初版流纹（OK，水不再走 UE Water 平面；总体仍 FAIL）**：继续 AAA 视觉推进时发现 `water-foam-lanes-v1/v2` 出现黑水，`v3` 又变成过亮大平板；根因是两个职责混在一起：`YarlungWaterBuilder` 仍强制 UE Water 的 212 个 renderable 可见，而真正能跟随 113.6m 河床落差的是 `SM_YarlungRiverSurface` 显式斜面水带。处理：UE Water 保留 `WaterZone + WaterBodyRiver`、材质/宽度/速度数据和 fail-close gate，但渲染组件改为 `gate_only` hidden，不再把水平 WaterZone 当主水面；`inspect-yarlung-map.py` 同步新合同，要求显式 `YarlungRiverSurface` actor 存在、可见且使用 `M_YarlungWaterSurface`。可见水面升级为 17 cross-lane ribbon，宽度收窄到河槽主流区（`HalfWidth * 0.42`，clamp 45m-105m），顶点色加入岸边/中线 broken foam 和 fast streak；水材质改为 BaseColor 与 VertexColor lerp，避免整片发光。验证：`python -m py_compile scripts/create-coaster-materials.py scripts/inspect-yarlung-map.py` PASS；C++ Build PASS；`import-yarlung-landscape.ps1 -SkipAssetGeneration -SkipTrackGeneration -ForceMaterials -SkipTerrainMeshBuild -Verify` PASS；`test-yarlung.ps1 -Build` 10/10 PASS；已 Read `Saved\Diagnostics\water-gateonly-foam-v3.png`。肉眼：水沿峡谷凹槽可见，宽度和流纹比 v1/v2 明显更可信；但仍是 opaque 顶点色水，没有真实法线、反射、透明深度或岸边湿岩，离参考图湍流江水仍远。下一步不要继续只堆顶点色，优先做河岸/山体材质与水面法线/反射方案。
- **2026-06-21/22 UE Editor Lumen 曝光 warning + 灰天修复（OK，默认截图回到蓝天基线；总体仍 FAIL）**：针对 Editor 红字 `Cached lighting in Lumen and real-time sky capture lighting is going to be clipped... Exposure: 15.7. Safe exposure range: [-8.0, 12.0]` 做根因定位。UE 5.8 Renderer 源码显示 `r.EyeAdaptation.CachedLightingPreExposure` 默认 `4` 对应 safe range `[-8, 12]`，物理日光曝光 15.7 超出范围；按引擎注释把项目配置设为 `8`，safe range 覆盖到约 `[-4, 16]`，修 Editor 默认 red overlay 的配置根因。视觉上做 A/B：`diagnose-sky-current` / `diagnose-sky-exposure-055` 说明单纯曝光只能小幅改善；`diagnose-no-clouds` 证明 SkyAtmosphere 本身能出蓝天，灰天主要来自 UE 默认 VolumetricCloud 材质把英雄段盖成阴云。处理：默认相机曝光 bias 从 `0.20` 提到 `0.55`；SkyLight 提到 `0.58`；雾降到 density `0.000070` / max opacity `0.42`；默认关闭 VolumetricCloud 渲染，先建立稳定 Clear Sky 基线，白云后续单独做可控云层/资产，不再让默认云材质污染基础日照。验证：C++ Build PASS；`git diff --check` PASS；`.\scripts\visual-survey.ps1 -Times 60,120 -NamePrefix fix-clear-sky-v1 -ResX 960 -ResY 540 -TimeoutSeconds 240` PASS；已 Read `Saved\Diagnostics\fix-clear-sky-v1.png`，t60 出现蓝天，t120 不再有灰云污染。剩余明显短板：无白云层、山体/水面/资产密度仍非 AAA；下一步应在稳定蓝天曝光下继续做水流反射/泡沫和山体岩壁森林，而不是继续追曝光。
- **2026-06-21/22 repo 兼容层/dead code 扫描（OK，水材质链更 fail-close；视觉不变）**：按“扫整个 repo，还有什么不需要的兼容层/dead code”裁决，重点扫 `Source/`、`scripts/`、`Config/` 的 live 链路。已清理一处真实兼容层：删除 `Config/yarlung-assets.json` 的 `water.fallback_river_material`、`FYarlungWaterConfig::FallbackRiverMaterialPath` 与 `YarlungWaterBuilder` 的 UE plugin 默认材质 fallback；`river_material` / `surface_material` 缺失现在直接失败。顺手把 `BuildYarlungRiverSurfaceStaticMesh()` 从硬编码 `/Game/Generated/Materials/M_YarlungWaterSurface` 改为读同一份 asset config。已删除死的 `MI_YarlungWaterRiver.uasset`、对应 Python 生成分支、import 脚本强制检查和未使用的 material-instance helper；当前水材质唯一 live 路径是 `M_YarlungWaterSurface`。扫描结论：脚本入口基本收敛，剩余 `normalize(..., fallback)` 属数值退化保护，不是旧兼容；历史 docs/reviews 保留旧名作为证据，不按 live 指南处理。验证：targeted `rg` 只剩本记录；JSON parse PASS；Python compile PASS；PowerShell parse PASS；C++ Build PASS；`import-yarlung-landscape.ps1 -SkipAssetGeneration -SkipTrackGeneration -ForceMaterials -SkipMapImport -Verify` PASS；`import-yarlung-landscape.ps1 -SkipAssetGeneration -SkipTrackGeneration -SkipMaterials -SkipTerrainMeshBuild -Verify` PASS，日志确认 `material=M_YarlungWaterSurface static_material=M_YarlungWaterSurface`。
- **2026-06-21/22 截图脚本兼容层删除（OK，接口更窄；视觉不变）**：按“没必要兼容旧用法”继续精简截图 harness。`scripts/offscreen-shot.ps1` 删除旧 `-WaitSeconds`、`-Mode` alias、`-SimulateWait`、`-KeepSourceFrames`；单帧截图现在必须显式传 `-JumpSeconds`，多帧路径继续由 `-BatchJumpSeconds` 驱动。`scripts/visual-survey.ps1` 删除 `-LegacyPerShot`，统一只走 batch capture，不再保留逐帧冷启动 fallback。当前文档命令同步到 `-JumpSeconds` / `-CaptureMode`。验证：PowerShell parse PASS；`git diff --check` PASS；`.\scripts\visual-survey.ps1 -Times 60,120 -NamePrefix simplify-shot-api-batch-smoke -ResX 640 -ResY 360 -TimeoutSeconds 240` PASS，已 Read `Saved\Diagnostics\simplify-shot-api-batch-smoke.png`；`.\scripts\offscreen-shot.ps1 -Name simplify-shot-api-single-smoke -JumpSeconds 60 -ResX 640 -ResY 360 -TimeoutSeconds 240 -CaptureMode MovieFrames` PASS，已 Read `Saved\OffscreenShots\simplify-shot-api-single-smoke.png`。
- **2026-06-21/22 截图脚本 harness 精简（OK，行为不变；视觉不变）**：按“水 bug 已修，先重构脚本”裁决，收敛截图链路的重复接口。新增 `scripts/yarlung-shot-lib.ps1`，把时间列表解析、fresh/non-empty 输出断言、UE 参数扁平化集中到一个 helper；`scripts/offscreen-shot.ps1` 和 `scripts/visual-survey.ps1` 不再各自复制这些函数。`offscreen-shot.ps1` 内部参数从含混的 capture mode 命名改为 `-CaptureMode`，避免和 `iterate-yarlung.ps1 -Mode Actor/Material/Terrain` 语义撞车。验证：PowerShell parse PASS；`git diff --check` PASS；`.\scripts\visual-survey.ps1 -Times 60,120 -NamePrefix refactor-shot-lib-batch-smoke -ResX 640 -ResY 360 -TimeoutSeconds 240` PASS，已 Read `Saved\Diagnostics\refactor-shot-lib-batch-smoke.png`；single-shot smoke PASS。视觉不作为本轮进展评价。
- **2026-06-22 峡谷水不可见 = 根因定位 + 修复（OK，水全程可见；总体仍 FAIL）**：根因**不是相机也不是材质**，而是 UE Water 把河面渲染成 WaterZone Z(265200) 的**水平面**，而河谷地面沿途比它高 6–31m（河床全长落差 **113.6m**），把水埋在地形下。决定性证据：同一第一人称帧加 `-YarlungHideTerrain` 时整片水面露出（蓝/青），正常帧被灰地形盖住——水一直在渲染，是地形画在它上面（用户判定"相机本来没问题、河没渲染出来"成立）。试过把 WaterZone Z 移到河床中点 268877 仍是平面，确认 UE Water 单 zone GPU 动态水对这条极端河（113.6m 落差 / 363m 宽）**不行**。**修复**：`YarlungLandscapeImportCommandlet` 新增 `BuildYarlungRiverSurfaceStaticMesh` 生成 `SM_YarlungRiverSurface`——沿河床斜面的显式水面 ribbon（surface=bed+220cm，跟随落差，宽≈河 half-width），用 `M_YarlungWaterSurface` 渲染，始终浮在挖好河槽上方 ~5m，**永不被埋**；UE Water actor 保留过 gate，其平面留在 ribbon 下方。验证：C++ Build PASS；`test-yarlung.ps1 -Build` 10/10 PASS；`water-restore-verify` RiskGate=WARN risk=1.359 green=**0.822**（埋住时 ≈0.001），t60/120/180/240 肉眼都见青绿江水。**已回退的弯路**：scenic 下视相机（盯着灰地形盖子，方向错）、Single Layer Water 水材质（无头 -game 下 SLW 无可解析反射→黑镜，green 掉回 0.001）。**未做（下一步，用户已确认方向）**：水面仍哑光青绿，无反射/泡沫/**奔流**；用河 CSV `flow` 列 + 河道切线给水面做沿流向滚动法线 + 急流近岸白浪，真实反射最好在编辑器里调。
- **2026-06-21/22 水不可见问题 WIP（已被上面的 2026-06-22 修复取代；保留作历史）**：用户明确指出“不要看指标了，现在水完全没显示出来”，本轮按肉眼截图重新诊断。关键结论：`WaterZone + WaterBodyRiver` 存在且隔离 terrain/scenery/ride 后能渲染，但正常第一人称画面仍几乎看不到水；`Saved\Diagnostics\water-river-overhead-v5.png` 这种“故意把轨道压到河上方”的诊断路线也只是少数帧边缘露出蓝灰水带，主画面仍被灰白 terrain 占满，说明问题不是单纯材质颜色，而是水/河槽/轨道/相机的生成管线没有把水作为英雄画面对象。WIP 改动：`assets.local` 分叉已删除，资产配置只读 checked-in `Config/yarlung-assets.json`；`FYarlungRiverField::QueryNearest()` 已从 nearest row 改成 nearest segment projection，并插值河面/宽度/flow，修掉“UE Water spline 连续但 terrain carve 按离散圆点挖槽”的语义错位；默认 scenic 路线已重新生成并通过 `verify-track-clearance.py`（violations=0），`inspect-yarlung-spatial-contract.py --times 30,45,60,75,90,105,120,150,180,210,240,270,300` 为 11/13，但截图 `Saved\Diagnostics\water-segment-river-default-v1.png` 肉眼仍几乎无水，只有极少边缘蓝灰。验证已跑：C++ Build PASS；`python -m py_compile ...` PASS；`import-yarlung-landscape.ps1 -SkipAssetGeneration -SkipTrackGeneration -SkipMaterials -Verify` PASS，日志 `river_carved_vertices=160833 max_river_carve_cm=34449.0`、UE Water render path material/static material 都为 `M_YarlungWaterSurface`。未跑最终 `test-yarlung.ps1 -Build`，未提交。下个 session 不要继续用指标证明“可见”；先看上述 contact sheet，然后二选一：A) 继续做真正的 scenic route/hero camera solver，让合法路线在主视锥稳定俯视河槽；B) 保留 UE Water actor 作为水系统，同时在 commandlet 从同一 river field 生成显式可见的 river surface StaticMesh/WaterMeshOverride，并把 map inspect 加上“第一人称关键帧必须真的看到水”的 gate。当前 WIP dirty 包含 source/config/docs/generated assets；v5 invalid route 已被默认合法 route 覆盖，不应提交 invalid 诊断路线。
- **2026-06-21/22 默认曝光回正（OK，视觉可读性提升；总体仍 FAIL）**：针对“为什么这么暗，是不是曝光下修太多”检查相机链路，确认 `CoasterRideCamera` 默认 `AutoExposureBias=-1.45` 是当前阴暗灰坡的重要原因。用命令行覆盖先做 A/B：`-YarlungExposureBias=0.2` 风险从基线 worst=1.500 降到 1.460，t60 从 1.00 降到 0.87，暗部可读性改善且 `washed≈0`；`-YarlungExposureBias=0.8` 直接过曝，worst=3.097、`washed=0.795`，判拒绝。已把默认值改为 `0.20`，并用无覆盖参数重跑 `aaa-default-exposure-02` 验证默认输出等同 A/B 选中档。验证：C++ Build PASS；`.\scripts\visual-survey.ps1 -Times 60,120,180,240 -NamePrefix aaa-default-exposure-02 -ResX 960 -ResY 540 -TimeoutSeconds 240` PASS；已 Read contact sheet `Saved\Diagnostics\aaa-default-exposure-02.png`。肉眼：画面不再被过暗压住，但仍明显非照片级，主短板转为大面积裸灰坡、森林/岩壁资产覆盖不足、江水没有成为第一视觉主体；下一步应继续做英雄构图/路线和山体森林岩壁密度，而不是再加曝光。
- **2026-06-21/22 checked-in 森林密度与资产配置（OK，配置不再分叉；总体仍 FAIL）**：先前 local-only 的 PN spruce/Fab 路径、boulder/cliff/shrub/canopy 数量、canopy belt 密度已并入 `Config/yarlung-assets.json`，本地 override 文件已删除且不再被代码读取。历史截图 `Saved\Diagnostics\aaa-local-forest-density-v1.png` 仍说明“只加树数量”不是 AAA 解；下一刀应做路线/英雄视角、山体坡面材质/岩壁覆盖和可见江水。
- **2026-06-21 current-doc terrain terminology cleanup（OK，文档对齐当前管线；视觉不变）**：按“扫描全部，先把 outdated 文档修了，貌似已经不 heightfield 了”裁决，扫 current docs/config/generated README 中的 `heightfield` / `Landscape` / legacy canyon-wall / procedural river 说法。更新当前入口文档的统一口径：`.r16`/DEM 是离线 source data 与数据契约，当前运行时可见地形是 commandlet 生成的 `SM_YarlungCorridorTerrain` StaticMesh，不是 UE `ALandscape`；旧 macro/Landscape material 链、`YarlungCanyonWallActor`、`YarlungRiverActor` 都不得作为 live guidance 回流。同步修 `generate-yarlung-landscape-assets.py` 产出的 manifest metadata，避免下次重生又写回旧术语。历史 archive/review 保留原文不改，作为当时证据。验证：targeted `rg` 只剩本轮记录和“已删除旧 heightfield 诊断脚本”的历史说明；`python -m py_compile scripts\generate-yarlung-landscape-assets.py` PASS；`python -m json.tool` PASS；`git diff --check` PASS。
- **2026-06-21 screenshot/visual-survey gate 收紧（OK，截图链路更难假成功；视觉不变）**：继续 hardening 到截图端。`scripts/offscreen-shot.ps1` 现在把 batch times 显式解析为 `60,120` / `60+120` / `60 120` 都可用，避免 `powershell -File ... -Times 60,120` 被当作单个 `60120`；batch 输出和单帧最终输出都要求 fresh + non-empty，不再只看 UE exit code 或任意 Saved PNG。`scripts/visual-survey.ps1` 分析前删除旧 CSV/JSON/contact sheet，分析后要求三份产物 fresh + non-empty，并校验 JSON 行数等于截图数，避免读旧报告。验证：PowerShell parse PASS；`git diff --check` PASS；`.\scripts\visual-survey.ps1 -Times 60,120 -NamePrefix harden-shot-gate-smoke -ResX 640 -ResY 360 -TimeoutSeconds 180` PASS，产出 `t60`/`t120` 两张新图和 2 行 JSON；已 Read contact sheet，视觉仍是当前已知 FAIL/WARN，不作为视觉进展；`.\scripts\offscreen-shot.ps1 -Name harden-single-shot-smoke -JumpSeconds 60 -ResX 640 -ResY 360 -TimeoutSeconds 180` PASS，单帧最终 PNG fresh/non-empty。
- **2026-06-21 asset config scalar fail-close（OK，配置链路更硬；视觉不变）**：继续精简/hardening 时收掉 `YarlungAssetConfig` 最后一个明显 fail-open 点：scatter kind、canopy belt、water、component count/seed 都是显式必填字段；`mesh`、`tint`、`canopy_belt_seed` 保持设计上的可选，但只要出现就必须类型正确。后续配置已进一步收敛为只读 checked-in `Config/yarlung-assets.json`，不再有 local override。
- **2026-06-21 commandlet/material gate 再收紧（OK，默认链路更硬；视觉不变）**：继续沿“水 bug 暴露系统性 fail-open”的方向检查后，修掉两处还不够干净的 gate。`scripts/import-yarlung-landscape.ps1` 现在对 UE Python/material/map import/verify 不只看 exit code，还要求日志里出现 `Success - 0 error(s)`，防止 commandlet 半成功或错误日志被吞掉；`create-coaster-materials.py` 改用 UE 5.8 非 deprecated 的 `set_base_material_usage(..., true)`，并复用已有 `Material` 资产而不是删除重建，避免 `ObjectTools` warning 和不必要的资产 churn；`inspect-yarlung-assets.py` 在加载可选本地资产前先 `does_asset_exist()`，缺失时只输出诊断，不再制造 UE Error 噪声。验证：PowerShell parse PASS；`python -m py_compile` PASS；`.\scripts\import-yarlung-landscape.ps1 -SkipAssetGeneration -SkipTrackGeneration -ForceMaterials -SkipMapImport` PASS，material commandlet `Success - 0 error(s), 0 warning(s)`；`.\scripts\import-yarlung-landscape.ps1 -SkipAssetGeneration -SkipTrackGeneration -SkipMaterials -SkipMapImport -Verify` PASS，inspect commandlet `Success - 0 error(s), 0 warning(s)`。本轮更新 generated material assets 是有意输出，视觉未重评。
- **2026-06-21 fail-close 第二层收口（OK，默认链路更不容易假成功；视觉不变）**：继续追问“所有 bug-prone 的地方是否都修了”后，再扫一轮 fail-open/静默降级点并修掉默认 Yarlung 路径里的高风险项。`CoasterTrackComponent` 现在遇到 generated track CSV 未知 section、section range/roll sample 构造失败时直接加载失败，不再默默当 `Coast`、0 bank、0 terrain height 继续跑；`YarlungLandscapeImportCommandlet` 现在要求 corridor terrain actor、ride actor、UE Water、scenery actor 全部 spawn 成功，否则 commandlet 返回失败；`YarlungWaterBuilder` 现在要求 WaterZone、river material、surface material 存在，不再用 WaterBodyRiver 默认材质或无 zone 降级；`inspect-yarlung-map.py` 从 deprecated `EditorLevelLibrary` 迁到 `UnrealEditorSubsystem` / `EditorActorSubsystem`，verify map 现在能做到 0 warning。验证：C++ Build PASS；`.\scripts\import-yarlung-landscape.ps1 -SkipAssetGeneration -SkipTrackGeneration -SkipMaterials -SkipTerrainMeshBuild -Verify` PASS，import commandlet `Success - 0 error(s), 0 warning(s)`；`.\scripts\import-yarlung-landscape.ps1 -SkipAssetGeneration -SkipTrackGeneration -SkipMaterials -SkipMapImport -Verify` PASS，inspect `Success - 0 error(s), 0 warning(s)`；`.\scripts\test-yarlung.ps1` PASS，10 个 Yarlung automation tests；`python -m py_compile` PASS；`git diff --check` PASS。生成 `.umap` 仅因验证重存，已还原，不作为视觉变更提交。
- **2026-06-21 fail-close setup/gate 全 repo 扫描（OK，水 bug 暴露的是系统性闸门问题）**：结论：这次“完全没水/水代码 bug”不只是单点视觉问题，而是暴露了工程链路曾经半 fail-open。已收口：`import-yarlung-landscape.ps1` 的材质/inspect 都统一走 `-run=pythonscript -script=...`；`YarlungTerrainProfile` / `YarlungAssetConfig` 缺配置、解析失败、关键结构缺失、非法水宽/深度、启用组件缺 mesh 等现在直接 Fatal，不再返回零配置/空配置继续生成误导画面；`create-coaster-materials.py` 不再用 `load_asset()` 探测缺失资产，避免 UE Error 噪声，同时在找不到 Water plugin parent 时回退项目自管 `M_YarlungWaterSurface`；`.uproject` 删除旧 `ProceduralMeshComponent` 插件声明；旧 `CorridorProfile.AuthoredHeight` 测试从程序化 canyon-wall 时代的 28000cm 期望改成当前“中等地形 relief、不恢复旧墙体”的 12000cm 期望。新增 `scripts/test-yarlung.ps1`，把 Yarlung automation 包成正式 gate：要求发现测试、无 Fail/Error/Fatal、日志含 `TEST COMPLETE. EXIT CODE: 0`。验证：JSON parse PASS；C++ Build PASS；`import-yarlung-landscape.ps1 -SkipAssetGeneration -SkipTrackGeneration -ForceMaterials -SkipTerrainMeshBuild -Verify` PASS；`python -m py_compile` PASS；`git diff --check` PASS；`.\scripts\test-yarlung.ps1 -Build` PASS，发现并通过 10 个 `CoasterSim.Yarlung` 测试。Review 记录：`docs/reviews/2026-06-21-fail-close-setup-scan.md`。下一步视觉可以继续，但 terrain/water/config/关卡生成相关改动必须带这个 gate。
- **2026-06-21 UE Water 可见性诊断 + gate 修复（OK，确认不是“完全没水”；视觉仍 FAIL）**：针对“完全没水/是不是水代码 bug”重跑诊断。结论：确实有 bug，但核心不是 `WaterBodyRiver` 没生成，而是旧验证链路把 UE WaterZone 渲染路径误判为失败：UE Water 会让 `WaterBodyRiverComponent` 的 spline mesh 隐藏，由 `WaterZone` mesh tile 渲染水面；旧 `inspect-yarlung-map.py` 强制要求 spline mesh visible，导致误报。另一个真 bug 是 `import-yarlung-landscape.ps1 -Verify` 使用了无效 `-ExecutePythonScript` 形态，Python inspect 报错也可能假成功；已改为 `-run=pythonscript -script=...`。水体隔离截图 `Saved\Diagnostics\water-isolate-currentmap-v2.png` 已 Read：隐藏 terrain/scenery/ride 后可见大面积 UE Water 水面，证明水是生成并渲染的。正常第一人称 `Saved\Diagnostics\exposure-water-v1.png` 已 Read：t120 中央能看到暗灰蓝水面，但被灰白地形、构图和材质读性吞掉，离参考图的乳白青绿湍流还很远。同步修复 `M_YarlungMeshTerrain` stale material fallback（删除重建脚本自管材质，清掉 `Missing input texture` warning），并把相机 pitch/exposure 小调为更利于俯视河槽。验证：C++ Build PASS；`.\scripts\import-yarlung-landscape.ps1 -SkipAssetGeneration -SkipTrackGeneration -SkipMaterials -SkipMapImport -Verify` PASS；`.\scripts\import-yarlung-landscape.ps1 -SkipAssetGeneration -SkipTrackGeneration -ForceMaterials -SkipTerrainMeshBuild -Verify` PASS；`visual-survey` `exposure-water-v1` PASS，worst=`t120` risk=1.559、washed=0.000。下一步不是继续怀疑 WaterBody 是否存在，而是修河槽/路线构图与水材质可读性：让水在英雄帧成为主体，并用地形通道、深度/泡沫/流速把它做成雅鲁藏布感。
- **2026-06-21 脚本清理：删除旧诊断/预览工具（OK，项目更干净）**：删除不在当前 live pipeline 的一次性脚本；当前入口收敛为 `iterate/import/generate/verify/inspect/offscreen/visual-survey/analyze`。PolyHaven/Megaplant 旧默认层已清理；PN spruce/Fab 资产路径现在写入 checked-in `Config/yarlung-assets.json`，资产本体仍可留本地不上 GitHub。
- **2026-06-21 PolyHaven live pipeline 全清理（OK，项目更干净；视觉需重评）**：按“项目干净更重要”裁决，不再保守保留兼容入口。删除 PolyHaven 源资产、下载脚本、模型导入脚本、生成的 `/Game/Generated/Models/*` 与 `/Game/Generated/Materials/PolyHaven/AerialGrassRock/*`；`import-yarlung-landscape.ps1` 删除 `-SkipModels/-ForceModels` 和 model import 阶段；`iterate-yarlung.ps1 -Mode Full` 不再 ForceModels；`Config/yarlung-assets.json` 默认 shrub mesh 清空且 count=0；`Config/yarlung-iteration.json` 不再把 `Content/Generated/Models/` 作为 intentional tracked output；`create-coaster-materials.py` 去掉 `SourceAssets/PolyHaven/aerial_grass_rock` 贴图导入，`M_YarlungMeshTerrain` 改为纯 vertex-color 参数材质，避免旧低保真贴图继续占据 pipeline。文档同步：`photoreal-overhaul.md` 的构建顺序/架构图不再包含 PolyHaven 模型导入，`aaa-asset-pipeline.md` 标记 PolyHaven 为已清理旧层。验证：`python -m json.tool` PASS；PowerShell parse PASS；`python -m py_compile` PASS；`git diff --check` PASS；`.\scripts\iterate-yarlung.ps1 -Mode Material -Preset Quick -SkipCapture -RestoreGeneratedMap -NamePrefix cleanup-polyhaven-smoke` PASS，manifest=`Saved\Diagnostics\cleanup-polyhaven-smoke-run.json`，map import + verify PASS，生成 `.umap` 已自动还原，RiskGate=UNKNOWN（有意跳过截图）。视觉上山体可能更 flat，但这是有意用干净 pipeline 换掉旧低保真 fallback，真正 AAA 仍靠 Fab/Megascans/本地 foliage/cliff 资产。
- **2026-06-21 轨道拓扑放宽为开放式 scenic layout（决策同步；代码未改）**：用户明确“过山车轨迹不一定要沿河，横跨河、翻滚都行，别太被限制”。裁决：雅鲁藏布江/河谷底线从“必须贴着走的线路约束”降级为视觉 anchor；轨道可以主动跨江、抬高、贴崖、overbank、俯冲、airtime 或短暂 roll/inversion，只要过净空/G 值/闭环长度/第一人称空间合同。已同步 `worlds-longest-coaster.md` 与 `photoreal-overhaul.md`，把旧“沿江往返 out-and-back”为主的描述改为风景优先开放式 scenic route。诊断补充：本轮曾快速试过把旧 thalweg-offset 生成器加 S/crossing 曲线，结果要么 `min_radius` 极小、横向 G/坡度超限，要么仍 `visible_samples=0/5` 看不到水；该方向已回滚，不保留为默认。下一步不要继续硬弯旧 offset 生成器，应做真正的 control-point/scenic scoring route generator：用 DEM/river 派生高处俯视点、跨江点、贴崖点、远山方向和站台 bench，生成多候选路线并由 `verify-track-clearance.py` + `inspect-yarlung-spatial-contract.py` gate。
- **2026-06-21 大资产覆盖层 + 英雄走廊诊断（OK/FAIL 混合；默认视觉仍 FAIL）**：用户确认大资产可以留本地、不上 GitHub；资产路径/散布配置现已统一进入 checked-in `Config/yarlung-assets.json`，不再使用 local override。Asset Registry 已确认 PN spruce whole-tree StaticMesh 可用；本地树/岩壁资产接线成功，但英雄帧仍需要稳定看见江水 + 对岸湿岩 + 森林坡。
- **2026-06-21 UE 5.8 `Rename` deprecation warning 修复（OK，repo 质量提升；视觉不变）**：按“修 warning”裁决，清掉 `YarlungLandscapeImportCommandlet.cpp` 生成 corridor terrain mesh 时的 UE 5.8 deprecated flag：替换旧 asset 前显式 `ResetLoaders(MeshPackage)`，并把 `REN_ForceNoResetLoaders` 改为 `REN_AllowPackageLinkerMismatch`，符合 UE 5.8 对 `Rename()` 的新语义，不再依赖未来会移除的隐式 loader reset。验证：`git diff --check` PASS；C++ Build PASS 且不再出现 `Rename will no longer call ResetLoaders`；`.\scripts\iterate-yarlung.ps1 -Mode Actor -Preset Quick -Build -SkipCapture -RestoreGeneratedMap -NamePrefix rename-warning-fix-smoke` PASS，commandlet 结束 `Success - 0 error(s), 0 warning(s)`，manifest=`Saved\Diagnostics\rename-warning-fix-smoke-run.json`，RiskGate=UNKNOWN（有意跳过截图），生成 `.umap` 已自动还原。
- **2026-06-21 `YarlungSceneryActor` placement gate 收口（OK，复杂度下降；视觉不变）**：继续执行代码/repo 精简，把 `AddScatterRule()` 与 `AddCanopyBelt()` 里重复的 placement 判定收进 `TryResolvePlacement()`：terrain bounds、river clearance、track/terrain height sampling、`YarlungCorridorProfile::AuthoredHeightCm()`、height range 与 slope gate 现在由同一处负责；rock/cliff/canopy 仍保留各自的随机、yaw、scale 与 transform 策略，参数和预期行为不变。验证：`git diff --check` PASS；C++ Build PASS；`.\scripts\iterate-yarlung.ps1 -Mode Actor -Preset Quick -Build -SkipCapture -RestoreGeneratedMap -NamePrefix scenery-placement-helper-smoke` PASS，manifest=`Saved\Diagnostics\scenery-placement-helper-smoke-run.json`，RiskGate=UNKNOWN（有意跳过截图），生成 `.umap` 已自动还原。下一步代码清理候选：继续把 scenery placement 抽成独立模块/测试面，或拆 terrain surface coloring。
- **2026-06-21 `iterate-yarlung` 生成地图 dirty contract（OK，repo 质量提升；视觉不变）**：继续执行代码/repo 清理，把 Actor/Material smoke 会重写 tracked `.umap` 的隐性成本显式化。`scripts/iterate-yarlung.ps1` 新增 `-RestoreGeneratedMap`：运行前记录 `Content/Generated/YarlungLandscape/YarlungLandscape_Level.umap` 是否 dirty，运行后把 `generated_map.restore_requested/dirty_before/dirty_after/restored` 写进 manifest/handoff；只有运行前 clean 且运行后 dirty 时才自动 `git restore` 这一个生成地图，若运行前已有改动则跳过并记录原因，避免误删 intentional generated WIP。文档同步到 `docs/plans/codex-iteration-scaffold.md` 与 `docs/reviews/2026-06-21-code-repo-quality-scan.md`。验证：PowerShell parse PASS；`git diff --check` PASS；`.\scripts\iterate-yarlung.ps1 -Mode Actor -Preset Quick -Build -SkipCapture -RestoreGeneratedMap -NamePrefix generated-map-restore-smoke` PASS，manifest=`Saved\Diagnostics\generated-map-restore-smoke-run.json`，记录 `dirty_before=false / dirty_after=true / restored=true`，最终 worktree 没有残留 `.umap` dirt。
- **2026-06-21 代码/repo 质量扫描 + ride 起点小重构（OK，视觉不变）**：按“除了视觉，看看代码和 repo 还有没有可以精简/提升质量的机会”裁决，暂停未通过的 canopy-density 视觉 WIP（RiskGate=FAIL，已回滚 config 与生成 `.umap`），转向代码侧清理。清掉本地 ignored `scripts/__pycache__` 扫描噪声；`ACoasterRideActor::StartRideFromCommandLine()` 与批量截图定位入口现在共用 `ComputeAdvancedTrackRatio()`，删除两处重复的 start-distance / speed / wraparound 公式，行为等价。验证：`git diff --check` PASS；C++ Build PASS；`.\scripts\iterate-yarlung.ps1 -Mode Actor -Preset Quick -Build -SkipCapture -NamePrefix ride-start-helper-smoke` PASS，manifest=`Saved\Diagnostics\ride-start-helper-smoke-run.json`，RiskGate=UNKNOWN（有意跳过截图），生成 `.umap` dirt 已还原。新增代码质量扫描文档：`docs/reviews/2026-06-21-code-repo-quality-scan.md`。下一步代码清理优先级：scenery placement 深化、Actor smoke 生成 `.umap` dirty contract、UE 5.8 `Rename` deprecation。
- **2026-06-21 山体视觉实验/资产缺口确认（FAIL；不保留代码）**：按“轨道后面可以更大起伏，先把山体视觉搞好”的裁决，先验证山体而不是继续抬高轨道。高处俯视轨道实验能让河进入构图，但当前试跑触发物理 gate 失败（`max_grade=251.5%`、`est_vert_g=-3.89/6.51`、`violations=54`），延后到山体可信后再重排。山体方向试了两类：①地形色/材质微调（`mountain-rock-balance-v2`、`mountain-texture-lift-v3`）把黄绿坡面变成灰泥大墙，RiskGate 仍 FAIL（risk≈1.79/1.81，flat>0.90），拒绝；②沿相机走廊加 cliff belt（`mountain-cliff-belt-v1/v2`）v1 出现黑色岩板和穿插，v2 变少但仍像硬塞的大 slab，RiskGate FAIL（v2 risk=1.665，flat=0.830），拒绝。处理：所有源码/配置/generated WIP 已回滚，不把失败方案留在默认管线。根因判断：不能再靠单 DEM-derived terrain 材质和大块岩板硬调，山体 AAA 的缺口是资产层：HISM/ISM 可用的整树针叶 StaticMesh 或明确的 PCG/foliage 路径、坡面友好的模块化 cliff/rock wall、以及多层地表材质。当前导入的 Megaplant 完整树多为 `SkeletalMesh`，现有 HISM 只能实例化 branch/top 静态网格，所以森林仍是 branch-clump 临时近似。
- **2026-06-21 `CoasterRideCamera` 镜头链路拆分（OK，复杂度下降；视觉不变）**：继续按“清理/精简”裁决，把第一人称相机 rig transform、FOV、曝光、ACES/film、运动模糊、暗角、色彩和 DOF 默认值从 `ACoasterRideActor` 构造器拆到 `CoasterRideCamera` 模块；actor 构造器只创建 `RideCamera` 并调用 `CoasterRideCamera::Configure()`，每帧相机定位改为 `ApplyRigTransform()`，去掉构造器和 `UpdateFirstPersonCamera()` 的位置/俯仰双写。`CoasterRideActor.cpp` 从 374 行降到 322 行。验证：首次 build 抓到 UE `FVector/FRotator` 不能 `constexpr`，已改为普通 `const`；随后 `git diff --check` PASS；C++ Build PASS；`.\scripts\iterate-yarlung.ps1 -Mode Actor -Preset Quick -Build -NamePrefix split-ride-camera-v1 -Times 60` PASS，manifest=`Saved\Diagnostics\split-ride-camera-v1-run.json`，RiskGate=WARN，risk=1.441，contact sheet=`Saved\Diagnostics\split-ride-camera-v1.png`。肉眼：相机/轨道/云雾位置正常，视觉与上一轮等价；仍是黄绿山谷，水与参考构图问题未解决。
- **2026-06-21 `CoasterRideCapture` 截图链路拆分（OK，复杂度下降；视觉不变）**：继续按“清理/精简”裁决，把批量截图与命令行 capture 状态从 `ACoasterRideActor` 拆到 `CoasterRideCapture::FState`：`YarlungBatchShotTimes/Dir/Prefix/Res/Settle/Post` 解析、stage/index/frame-wait、截图请求、完成后退出都在独立模块里；actor 只保留一个 `BatchCapture` 成员和“按秒数定位 ride”的回调。`CoasterRideActor.cpp` 从 496 行降到 374 行，header 去掉一组截图状态字段。验证：`git diff --check` PASS；C++ Build PASS（仅剩 UE `Rename` deprecated warning）；`.\scripts\iterate-yarlung.ps1 -Mode Actor -Preset Quick -Build -NamePrefix split-ride-capture-v1 -Times 60` PASS，manifest=`Saved\Diagnostics\split-ride-capture-v1-run.json`，RiskGate=WARN，risk=1.441，contact sheet=`Saved\Diagnostics\split-ride-capture-v1.png`。肉眼：相机/轨道/支撑/云雾正常，视觉仍未改善；此轮只是架构精简。
- **2026-06-21 `CoasterRideActor` 职责拆分（OK，复杂度下降；视觉不变）**：按“拆”裁决，把 `ACoasterRideActor` 从仿真+相机+截图+轨道绘制+大气配置的巨类拆成三段：actor 保留轨道采样/运动仿真/第一人称相机/批量截图/telemetry；新增 `CoasterTrackVisuals` 负责 cylinder rails/ties/supports 的 mesh/material/instance rebuild/可见性；新增 `YarlungAtmosphere` 负责 SkyLight/SunLight/SkyAtmosphere/VolumetricCloud/Fog 配置、体积云 MID、river fog anchor、光照诊断命令行参数。`CoasterRideActor.cpp` 从 800+ 行降到 496 行，默认可见输出仍由同一数据链生成。验证：`git diff --check` PASS；C++ Build PASS（仅剩 UE `Rename` deprecated warning）；`.\scripts\iterate-yarlung.ps1 -Mode Actor -Preset Quick -Build -NamePrefix split-ride-visual-atmosphere-v1 -Times 60` PASS，manifest=`Saved\Diagnostics\split-ride-visual-atmosphere-v1-run.json`，RiskGate=WARN，risk=1.448，contact sheet=`Saved\Diagnostics\split-ride-visual-atmosphere-v1.png`。肉眼：轨道/支撑/云雾仍正常渲染；画面仍是黄绿远离参考图，下一步视觉重点不变：先修相机/轨道/英雄段与河道构图。
- **2026-06-21 Git 维护 + 隐藏 TrainBody placeholder 删除（OK，复杂度下降）**：按“维护 git / 还能精简吗”裁决，先执行 `git gc --prune=now`，对象库从 loose `10461` 个 / `8.48 GiB`、packs=50 收敛到 loose=0、packs=1、pack size=`635.89 MiB`。随后删除 `ACoasterRideActor` 里一直隐藏的 `TrainBody` / `/Engine/BasicShapes/Cube` placeholder：`TrainRoot` 仍作为相机/运动挂点，轨道 rails/ties/supports 保持 cylinder 管轨；`YarlungHideRide` 只控制真实可见的轨道实例组件。此改动不补假车体，不改变 D 阶段缺口：仍需要 authored cockpit/train/安全杆资产，但默认链路不再携带隐藏灰盒车体。验证：`rg` 确认 live source 无 `TrainBody` / `Cube.Cube`；`git diff --check` PASS；C++ Build PASS（仅剩 UE `Rename` deprecated warning）；`.\scripts\iterate-yarlung.ps1 -Mode Actor -Preset Quick -Build -SkipCapture -NamePrefix git-maint-trainbody-delete-smoke` PASS，manifest=`Saved\Diagnostics\git-maint-trainbody-delete-smoke-run.json`，RiskGate=UNKNOWN（有意跳过截图）。
- **2026-06-21 资产配置 + 水体 Builder 管线精简（OK，复杂度下降；视觉仍 FAIL）**：继续按“精简重构，降低复杂度”裁决，把可变资产路径/散布参数/水体参数从 C++ 硬编码迁到 `Config/yarlung-assets.json`，新增 `YarlungAssetConfig` 统一读取 scenery components、scatter kinds、canopy belt 和 UE Water 参数；`YarlungSceneryActor` 构造器只保留 HISM 组件创建，运行时从配置绑定 Megascans/生成资产并执行同一套 scatter 规则；`YarlungWaterBuilder` 从 `YarlungLandscapeImportCommandlet` 抽出 UE Water 生成，commandlet 只负责调用 world actor 构建步骤。删除 `scripts/inspect-assets.py` / `scripts/inspect-water-material-params.py`，合并为 `scripts/inspect-yarlung-assets.py`；`create-coaster-materials.py` 的 imported-material usage roots 也改读配置。验证：`python -m json.tool Config\yarlung-assets.json` PASS；`python -m py_compile scripts\create-coaster-materials.py scripts\inspect-yarlung-assets.py scripts\inspect-yarlung-map.py scripts\inspect-yarlung-spatial-contract.py` PASS；C++ Build PASS（仅剩 UE `Rename` deprecated warning）；`.\scripts\iterate-yarlung.ps1 -Mode Actor -Preset Quick -Build -NamePrefix refactor-asset-config-water-builder-v1 -Times 60` PASS，manifest=`Saved\Diagnostics\refactor-asset-config-water-builder-v1-run.json`，RiskGate=WARN，risk=1.430，contact sheet=`Saved\Diagnostics\refactor-asset-config-water-builder-v1.png`。肉眼：地图/轨道/scenery/UE Water 生成链路正常，但画面仍是远离参考图的黄绿山谷，水没有成为第一人称主体；下一步视觉应优先修相机/轨道/英雄段与河道构图，再做水材质泡沫，而不是怀疑资产配置链路。
- **2026-06-21 RiverField 单模块精简（OK，复杂度下降）**：继续按“精简重构，降低复杂度”裁决，把河道链路从 `YarlungRiverCsv.h + FYarlungRiverField + 多处直接 CSV 调用` 收口为单一 `FYarlungRiverField` 模块：`FYarlungRiverRow` 与 CSV 解析内聚到 `YarlungRiverField`，删除 `YarlungRiverCsv.h`，`CoasterRideActor` 的雾锚点与 `YarlungLandscapeImportCommandlet` 的 terrain/UE Water 入口都只依赖 `RiverField`。同时移除 `RiverField` 对旧 `YarlungTerrainProfile::RiverCenterY` 的 silent fallback：生成河道缺失或样本少于 4 时 fail-fast，不再悄悄切换到旧解析河线。`iterate-yarlung.ps1` 的 Actor/Material/Terrain/Full 参数分支改为 mode-to-flags 表，减少重复布尔参数块。验证：PowerShell syntax PASS；`python -m py_compile scripts/inspect-yarlung-spatial-contract.py scripts/generate-yarlung-track.py scripts/verify-track-clearance.py` PASS；C++ Build PASS（仅剩 UE `Rename` deprecated warning）；`.\scripts\iterate-yarlung.ps1 -Mode Actor -Preset Quick -Build -SkipCapture -NamePrefix simplify-riverfield-single-module-smoke` PASS，manifest=`Saved\Diagnostics\simplify-riverfield-single-module-smoke-run.json`，RiskGate=UNKNOWN（有意跳过截图），日志确认 fog anchor/UE Water/scenery 都从新链路正常生成。
- **2026-06-21 架构/管线重构：RiverField + artifact DAG（OK，视觉问题定位更准）**：按“架构和管线要合理”裁决，新增 `FYarlungRiverField` 作为河道空间事实模块，统一提供最近河点、河面高度、水面 lift、河宽、river distance/mask；`YarlungLandscapeImportCommandlet` 的 terrain color / wet-rock / ravine / relief / river carve 与 `YarlungSceneryActor` 的 scatter clearance 已切到同一 river field，旧 `RiverCenterY(X)` 不再参与默认视觉生成链路（仅保留为配置/测试 fallback）。`import-yarlung-landscape.ps1` 增加正式 `generate track -> verify generated track` 阶段；`iterate-yarlung.ps1 -Mode Full` 现在强制重生 `YarlungTrack.csv` 并跑 `verify-track-clearance.py`，Actor/Material/Terrain 快循环显式跳过 track step，避免地形/河道变化后轨道 stale 的假成功。新增 `scripts/inspect-yarlung-spatial-contract.py`，从 track+river 计算第一人称镜头是否能看见河；当前诊断 `visible_samples=0/5`，CSV=`Saved/Diagnostics/yarlung-spatial-contract.csv`，证明水 actor 存在但当前相机/轨道构图没有把河纳入视锥。验证：`python -m py_compile scripts/create-coaster-materials.py scripts/inspect-yarlung-map.py scripts/verify-track-clearance.py scripts/generate-yarlung-track.py scripts/inspect-yarlung-spatial-contract.py` PASS；C++ Build PASS；当时 `.\scripts\import-yarlung-landscape.ps1 -SkipAssetGeneration -SkipMaterials -SkipModels -SkipMapImport` PASS 且 track verify `violations=0`（注：`-SkipModels` 已在 2026-06-21 PolyHaven cleanup 后退役）；`.\scripts\iterate-yarlung.ps1 -Mode Terrain -Preset Quick -Build -SkipCapture -NamePrefix arch-riverfield-pipeline-smoke` PASS，manifest=`Saved\Diagnostics\arch-riverfield-pipeline-smoke-run.json`，RiskGate=UNKNOWN（有意跳过截图）。下一步视觉不应继续怀疑 UE Water 生成；应调英雄段/相机/轨道相对河道，让 spatial contract 至少有 sampled frame 入视锥，再做水材质/泡沫。
- **2026-06-21 旧 procedural river / ProceduralMesh 清理（OK）**：继续按“先精简架构和程序”裁决，把已被 UE Water 取代的 `YarlungRiverActor.cpp/.h` 删除；删除旧 `M_YarlungRiverWater.uasset` / `M_YarlungRiverFoam.uasset`，移除 `create-coaster-materials.py` 中旧 river material 生成分支，并从 `CoasterSim.Build.cs` 去掉 `ProceduralMeshComponent` 依赖。`YarlungRiverCsv.h` 保留为唯一共享 river spline 数据解析器，服务 commandlet 生成 UE Water spline 与雾/构图锚点；`inspect-yarlung-map.py` 继续禁止默认地图出现 legacy `YarlungRiverActor`。验证：`python -m py_compile scripts/create-coaster-materials.py scripts/inspect-yarlung-map.py` PASS；C++ Build PASS（仅剩 `Rename` deprecated 旧警告）；`.\scripts\iterate-yarlung.ps1 -Mode ScreenshotOnly -Preset Quick -NamePrefix simplify-no-proc-river-v1 -Times 60` RiskGate=**OK**，worst=`t60` risk=0.464，contact sheet=`Saved\Diagnostics\simplify-no-proc-river-v1.png`。肉眼：无 missing asset/material，UE Water/关卡正常加载；视觉仍未达 AAA，下一步才进入 UE Water 材质可读性和真实岩壁/植被层。
- **2026-06-21 精简默认造景链路（OK，视觉仍 FAIL）**：按“先精简再调视觉”裁决，删除旧 `YarlungCanyonWallActor.cpp/.h`、停止在 `YarlungLandscapeImportCommandlet` 默认生成 `YarlungAuthoredCanyonWalls`、删除 `M_YarlungCanyonWall.uasset` 与 `create-coaster-materials.py` 中对应材质生成分支，并把 `inspect-yarlung-map.py` 改成禁止默认地图出现 `YarlungCanyonWallActor`。同时移除 `CoasterGameMode` 里的运行时 `ConfigureRuntimeYarlungWater()` 二次修补，水体配置收敛到 commandlet 生成阶段。验证：C++ Build PASS；`python -m py_compile scripts/create-coaster-materials.py scripts/inspect-yarlung-map.py` PASS；`.\scripts\iterate-yarlung.ps1 -Mode Actor -Preset Standard -Build -NamePrefix simplify-no-canyonwall-v1 -Times 57,60,66,237` RiskGate=**OK**，worst=`t60` risk=0.555，contact sheet=`Saved\Diagnostics\simplify-no-canyonwall-v1.png`。肉眼：假山片/黑洞/程序化幕布消失，空间关系更干净；仍明显非照片级，短板变为河面像浅泥滩、DEM 山体条纹、branch-clump 树冠、缺真实连续岩壁。下一步：在精简后的默认链路上调 UE Water 材质/可读性，并用 Nanite cliff/真实 foliage 做资产层，不恢复旧 canyon-wall。
- **2026-06-21 UE Water 河槽遮挡 bug 修复（WARN，未到 D Done）**：按用户要求不再用水体资产路线，改用 UE Water。`YarlungLandscapeImportCommandlet` 生成 `WaterZone + WaterBodyRiver`，读取 `YarlungRiver.csv` 作为开放样条，使用 UE 默认 `RiverMesh` / `Water_Material_River`，并禁止旧 `YarlungRiverActor` 作为主水体；`inspect-yarlung-map.py` 增加 UE WaterZone/WaterBodyRiver/可见 spline mesh 校验。排查关键 bug：正常第一人称图看不到水，但隔离图 `ue-water-isolate-v1` 隐藏 terrain/canyon/scenery 后能看到灰色 UE Water ribbon，说明不是 Water actor 没生成，而是 authored canyon wall 从轨道两侧 9m 起铺面，轨道近河段直接盖住河槽。修复：`YarlungCanyonWallActor` 给森林坡/湿崖 quad 加 river-clearance mask，离 `YarlungTerrain::RiverCenterY(X)` 54m 内不生成墙面，把槽让给 UE Water。验证：`.\scripts\iterate-yarlung.ps1 -Mode Actor -Preset Standard -Build -NamePrefix ue-water-river-clearance-v1 -Times 12,45,240`，RiskGate=**WARN**，worst=`t45` risk=1.225（修复前 `ue-water-runtime-fix-v1` risk=1.648），flat=0.488（修复前 0.764），contact sheet=`Saved\Diagnostics\ue-water-river-clearance-v1.png`。肉眼：t45 左侧峡谷已有可见浅色水面/河槽，但仍偏灰、缺乳白青绿湍流与泡沫，D 阶段水体不能标 Done；下一步应在 UE Water 内做材质/流速/泡沫可读性，或重排英雄段让河面成为主视觉。
- **2026-06-21 车体误判纠正 + 假车鼻清理（OK，但仍无真实车体）**：更正前一轮判断：当前工程**没有 authored 车体/座舱资产**，只有 `TrainBody` 这个 `BasicShapes::Cube` placeholder，已默认隐藏；截图里被误读成"车体/车鼻"的大灰白块主要来自过粗支撑横梁/圆管端面以及贴近相机的轨枕/钢轨端盖，不是真车。修复：`CoasterRideActor` 把支撑直径从 1.4%-of-height 的 1.5-4.7m 级别降到 0.4%-of-height、42-120cm clamp，去掉第一人称底部最污染画面的支撑顶横梁，轨枕从外突 10cm 管改成轨内 6cm 管；同时隐藏 placeholder cube，避免把灰盒当成验收对象。验证：`.\scripts\iterate-yarlung.ps1 -Mode Actor -Preset Route -Build -NamePrefix aaa-no-fake-car-route-v1`，RiskGate=**OK**，worst=`t15` risk=0.889，contact sheet=`Saved\Diagnostics\aaa-no-fake-car-route-v1.png`；对照单帧 `aaa-thin-ties-t240-v1` risk=0.213。肉眼：t240 仍有最近钢轨/轨枕端盖透视，但不能再称为车体；D 阶段真正缺口仍是 authored train/cockpit/安全杆资产，不能用灰盒管架顶替。
- **2026-06-21 水体小调实验被拒绝（不保留代码）**：尝试在 `AYarlungRiverActor` 内增加水面横向细分、断续 foam lanes、青绿/乳白顶点色与更高 alpha（`aaa-river-turbulence-v1/v2`）。验证均 Build/import/screenshot PASS 且 RiskGate=OK，但肉眼和指标都说明这条小调不值：v1 worst=`t30` risk=1.032 flat=0.438，v2 risk=1.070 flat=0.465，均比 `aaa-canopy-route-v3` risk=1.008 flat=0.429 更差；t30 水面继续读成浅色半透明平片，未接近参考里的真实湍流江水。处理：已回退 `YarlungRiverActor.cpp` / river material 默认值改动，不把失败方案留在 WIP。结论：D 阶段不能靠 procedural ribbon 继续调泡沫线解决，下一步水体应走真实 flow-normal/depth-fade/reflection-refraction 或 UE Water/材质资产化；短期更高收益仍是整树 foliage、连续 canyon cliff、authored cockpit/train。
- **2026-06-21 HISM 森林走廊铺开（WARN→OK，但非最终 AAA）**：把 `YarlungSceneryActor` 的树冠散布从只读 `Outbound` 扩到 `Lift/Outbound/Turnaround/Return/Launch`，并用 HISM 的 Norway Spruce branch/top 静态网格做多条近中景 canopy belt（Station/Brake 仍跳过）。关键根因：`aaa-canopy-belts-v1` 虽有 canopy 实例，但 t30 是 `Launch` 段，旧 `LoadOutboundTrack()` 不散布该段，所以最坏帧仍是空光滑坡；v3 后 `Yarlung scatter instances` 约 canopy_a=1203 / b=1194 / c=1210。验证：`.\scripts\iterate-yarlung.ps1 -Mode Actor -Preset Standard -Build -NamePrefix aaa-canopy-route-v3`，RiskGate=**OK**，worst=`t30` risk=1.008（v1 1.389），flat=0.429（v1 0.650），contact sheet=`Saved\Diagnostics\aaa-canopy-route-v3.png`。肉眼：t90/t150 已有连续密林/灌丛体积，t30 不再是纯空坡；仍明显非最终 AAA，树种近看像 branch-clump/灌木，右侧 canyon-wall 仍有程序化幕布/棋盘纹，江水低保真。下一步：用真正整树 foliage/PCG 替代 branch-clump，或转 D1 江水/近景水体改善第一人称主体。
- **2026-06-21 首次 Megascans 集成（管线打通）**：用户用 Fab 导入 Nordic Forest Cliff（Raw Nanite 8K）、Beach Boulder、Norway Spruce/Aleppo Pine 树套件。`YarlungSceneryActor` 重新启用散布并指向 Megascans 网格（cliff→CliffRockFacesA/B，boulder→RockOutcrops），`ApplyMaterials` 跳过它们以保留扫描 PBR 材质。**关键坑**：Megascans 基材质缺 `Used with Nanite` + `Used with Instanced Static Meshes` flag，无头 -game 下回退默认灰材质（编辑器拖拽会自动修，headless 不会）→ `create-coaster-materials.py` 加 `enable_fab_material_usages()` 给 `/Game/Fab/Materials` 全部基材质打开这俩 flag，警告消失、扫描材质正确渲染。散布按渲染迭代：v3 cliff scale 8–18、lateral 60–480m、~60 块岩壁 + 81 巨石。结果：扫描 Nanite 灰岩已作为真实岩壁出现在山坡（见 `aaa-megascans-cliff-v3-t90.png`），RiskGate=WARN。**现状＝管线验证通过、非最终 AAA**：仍是"绿坡撒石头"，缺连续岩壁/森林树冠/构图。下一步：接针叶林 + 多几块岩壁拼连续墙 + 调比例构图。
- **2026-06-21 轨道结构去灰盒：管轨化（非资产，AAA 方向）**：rails/ties/supports/支柱从 `BasicShapes::Cube` 换成 `Cylinder` + 新 `MakeTubeTransform`（Z 轴沿段对齐），画面中心的灰板支柱变成圆管钢结构（rails 12cm、ties 10cm、支柱按 LegThickness 直径）。`coaster-tube-track-v1` RiskGate=WARN risk=1.390（↓from 1.420）flat=0.655。注意：当时所谓"车体"只是 `TrainBody` placeholder，不是真实车体资产；后续已隐藏，D 阶段仍需 authored cockpit/train。这是不依赖 Megascans 的前景 hero 结构，先做掉。
- **2026-06-21 改定冲 AAA，走 Megascans-first（资产管线）**：确认 AAA 是内容问题不是引擎问题（`DefaultEngine.ini` 已 Lumen/Nanite/VSM/VT/DF/SM6 全开）。程序化路线封顶在 WARN（risk 1.42），不再投入折中微调。新建 `docs/plans/aaa-asset-pipeline.md`：Megascans/Fab 资产清单（cliff×4-6、boulder×6-10、conifer/阔叶树+下层植被、地表 surface×4-5、可选远山/过山车）+ 落地后我做的接线（岩壁散布替换程序化幕布、森林 instanced foliage、多层地形材质、江水、结构去灰盒）。**阻塞：等人工用 Quixel Bridge 登录 Fab 导入 Megascans**；资产到位后我全自动接线。程序化基线（视线下压 -16°、canyon-wall 贴图、致密化、aerial haze）保留作远景/底层 fill。
- **2026-06-21 canyon-wall 致密化 + 森林绿 + aerial haze（procedural 收尾）**：apron AcrossValues 7→13、cliff HeightValues 8→15 消地形横带；ForestColor 提绿提亮、ravine/gully 正弦降频软化去灯芯绒；ValleyFog density/opacity 抬高做远山 aerial perspective（start 拉远保近景清晰）。`mountain-walls-dense-haze-v1` RiskGate=WARN risk=1.420 flat=0.665 washed=0.00。肉眼：apron 变绿、远景有雾层次；条纹略减但程序化幕布本质仍在，故转 AAA 资产。
- **2026-06-21 canyon-wall 贴图化 + 视线下压（FAIL→WARN，首次跨过 fail 阈值）**：定位到真正瓶颈不是 terrain mesh 材质，而是占据画面主体的两张程序化 canyon-wall mesh（`YarlungCanyonWallActor` 的 `ForestApronMesh` / `WetCliffMesh`）一直用纯 vertex-color 的 `M_YarlungCanyonWall`（无 albedo/normal 贴图、7-8 顶点宽平滑着色），所以无论相机怎么调 `flat_block` 都卡在 0.6-0.8。改动两刀：(1) 相机 pitch 从 -4° 压到 -16°（`CoasterRideActor` 构造器 + `UpdateFirstPersonCamera` 两处都改，后者每帧覆盖），把画面从灰白中坡换成峡谷纵深 → `washed` 0.252→0.00；(2) 把 `M_YarlungCanyonWall` 与 `M_YarlungMeshTerrain` 改成扫描 grass/rock 细节贴图（diffuse×macro vertex color×gain + normal + roughness），canyon-wall UV 各向异性严重（along ~170-200m/单位、across ~480-700m/单位）所以 tiling 用 20×72 粗配真实纹素密度压条纹。最终：`.\scripts\iterate-yarlung.ps1 -Mode Material -Preset Standard -Build -NamePrefix mountain-wall-textured-final`，RiskGate=**WARN**，worst=`t30`，risk=1.422（基线 1.599），washed=0.00，green=0.00，flat=0.665（基线 0.60→曾涨到 0.79，贴图后回落）。contact sheet=`Saved\Diagnostics\mountain-wall-textured-final.png`。肉眼：山体首次读成有颗粒的喜马拉雅坡面而非灰盒，t90/t150 出现真实河谷纵深；仍存问题：①authored vertex-color 的正弦冲沟+离散横带产生灯芯绒条纹（要改 C++ ForestColor/WetCliffColor 频率，需 Terrain/Actor rebuild）②green 掉到 0（贴图把森林绿压成橄榄灰；试过 luma-only grain 想保色但反而丢颗粒、flat 回 0.777，已回退 RGB multiply）③轨道支柱仍是 `BasicShapes::Cube` 灰盒占据画面中心。
- **2026-06-21 macro canyon profile v2**：重新对照 `docs/refs/local/02_fp_yarlung_coaster_canyon.png` 与 `03_fp_mountain_coaster_valley.png`，确认目标不是"更高的 smooth wall"，而是山腰第一人称视角下的河谷纵深、森林覆盖山体、灰岩/湿岩崖面、远山层次。尝试 `mountain-macro-profile-v1` 后出现刀切暗缝和大块泥山；v2 改成更宽的山脊/浅冲沟，并增强 terrain vertex color 的森林覆盖。最终验证以已重建材质的 `mountain-macro-profile-v2-material-reset.png` 为准：RiskGate=FAIL，worst=`t90`，risk=1.599，washed=0.252，green=0.150，flat=0.601，edge=0.031。肉眼结论：比基线更有山体尺度和绿量，但仍明显是程序化大面山皮，缺真实树冠颗粒、岩壁破碎、河谷开阔构图；不能按阶段 C 完成。下一步必须转向真实/半真实资产层或重排山腰视线，不能继续只调 profile。
- **2026-06-21 dead source asset cleanup**：删除运行时已死的 source 输入资产：`SourceAssets/PolyHaven/leafy_grass/*.jpg` 与 `SourceAssets/Generated/YarlungLandscape/YarlungTsangpo_macro_*.tga`（约 72MB tracked files）。这些只服务旧 `M_YarlungLandscapeGround` / macro TGA / LeafyGrass 链，live 代码不再引用。同步更新 `photoreal-overhaul.md` 当前架构说明为 corridor static mesh + live material/scenery actor 路线；`worlds-longest-coaster.md` 去掉 macro generated-asset 表述；旧 bug log 加 historical note。验证：`rg` 确认 live `scripts/Source/Config/AGENTS` 无已删资产引用（剩余命中为历史 docs/reviews）；`python -m py_compile` 相关脚本 PASS；`yarlung-agent-status.ps1` PASS。视觉未重评，山体照片级仍 FAIL。
- **2026-06-21 P0/P1 repo hygiene**：旧 145KB `photoreal-progress.md` 已压缩为当前仪表盘，完整历史移到 `docs/plans/archive/photoreal-progress-pre-p0p1-cleanup-2026-06-21.md`；旧半自动 `auto-iterate/AUTOSTATE/Claude verifier` spec 移到 `docs/superpowers/archive/2026-06-18-auto-iterate-design.md` 并标为 historical；根目录两张参考图副本已删除，保留 gitignored `docs/refs/local/` 版本；`yarlung-agent-status.ps1` 增加 dirty 分组，能区分 generated tracked dirty、local-only refs、source/docs dirty、other dirty。验证：`Config/yarlung-iteration.json` JSON parse PASS；`yarlung-agent-status.ps1` 文本 PASS；`yarlung-agent-status.ps1 -Json` PASS；`iterate-yarlung.ps1 -Mode ScreenshotOnly -Preset Quick -SkipCapture -NamePrefix p0p1-cleanup-smoke` PASS（RiskGate=UNKNOWN，产出 manifest/handoff）。视觉未重评，当前山体照片级仍 FAIL。
- **2026-06-20/21 Codex unattended scaffold v2**：`Config/yarlung-iteration.json` 统一默认模式、Quick/Standard/Route/Hero/Final preset、risk gate 阈值、L1-L3 本地参考锚点；`scripts/iterate-yarlung.ps1` 读取 config，输出 `run.json` + `handoff.md` + `RiskGate=OK|WARN|FAIL|UNKNOWN`。最近 smoke：`scaffold-harness-shot-v2` 已 Read，RiskGate=WARN，视觉仍灰盒/假山体。
- **2026-06-20/21 山体 muted base v1**：proxy cliff/scatter 噪声已压掉，错误黑点和程序化肋纹减少；但画面仍是灰绿 DEM-derived corridor terrain 山皮，缺真实 forest canopy / wet cliff / authored canyon-wall 主体，山体照片级仍 FAIL。
- **2026-06-20/21 截图速度优化**：`visual-survey.ps1` 默认 batch capture，一次 UE 启动内跳多个第一人称时间点；低分辨率两时点约 14.0s，旧 per-shot 约 27.5s。

## 阶段状态

| 阶段 | 状态 | 当前判断 |
|---|---:|---|
| 0 参照/英雄段 | ✅ | 英雄段与 L1-L3 local refs 已确定；本地 refs 不入库。 |
| A 真实地形/路线基础 | 🟨 | DEM/长轨道/走廊基础可运行；不再用旧短环。2026-06-21/22 已把默认路线改成 authored scenic S/cross-river layout，并通过净空/G gate 与 4/4 spatial river visibility；仍需继续精选英雄段、优化远山/贴崖构图和整条 ride 节奏。 |
| B 光照/曝光/大气基础 | 🟨 | 物理日光/曝光有基础；2026-06-21/22 已修 UE Lumen cached pre-exposure warning，并把默认 VolumetricCloud 阴天层切到 Clear Sky 蓝天基线。剩余：白云需要单独可控层，整体仍受山体资产和成像层拖累。 |
| C 山体/峡谷几何 | 🟦 | 旧程序化 canyon-wall 已删除；当前靠 DEM-derived corridor StaticMesh + Megascans scatter。2026-06-22 已加密森林/河岸 boulder/rockwall，t60/t120/t240 有明显进步；t180 裸坡证明剩余瓶颈是 corridor terrain 表面与连续岩壁/材质层，不是继续堆 scatter 数量。 |
| D 江水/轨道/车 | 🟦 | 轨道已从黑灰管线推进到红色钢结构（双轨/中脊/横梁/护栏/斜撑/蓝绿支柱）；隐藏 `TrainBody` placeholder 已删除；**江水第一人称全程可见已修复**。当前默认链路只有一个可见水体：`SM_YarlungRiverSurface` 斜面 mesh；gate-only UE Water actor 已从代码/配置/插件依赖删除。剩余：水面仍无真实法线/反射/透明深度，authored cockpit/train/安全杆和检修平台仍缺。 |
| E 密林/成像收尾 | 🟨 | HISM 树冠走廊已铺到约 5k/类，t240 读成林海峡谷；仍是 spruce HISM 近似，局部段落存在裸坡/重复感。需要整树 StaticMesh foliage 或 PCG 路径，以及成像/运动模糊/镜头收尾。 |
| F 全程铺开/性能 | ⬜ | 需整条 on-rails 视锥验收，当前未到。 |

状态图例：⬜ TODO ／ 🟦 进行中 ／ 🟨 部分完成 ／ ✅ Done ／ ⛔ NEEDS-HUMAN ／ ❌ Blocked

## 下一步动作

1. **先保持 repo 清洁**：运行 `.\scripts\yarlung-agent-status.ps1`，确认 dirty 分组。提交时只 stage intentional files；generated assets dirty 需要说明来自哪次 build/visual change。
2. **继续视觉时用快循环**：
   ```powershell
   .\scripts\iterate-yarlung.ps1 -Mode Actor -Preset Standard -Build -NamePrefix <short-name>
   ```
   如果改变 terrain mesh/vertex color/displacement，改用 `-Mode Terrain`。
3. **当前最高优先级：t180 裸坡的山体表面/构图根因 + 水面成像，不再追“是否有水”**：
   - 否定任何视觉方向前，先执行 `docs/plans/visual-false-negative-audit.md`：用隔离图/inspect/材质日志排除 pipeline bug，并把结论写成 `BUG` / `CONTENT-LIMIT` / `DIRECTION-LIMIT`。没有证据时，不许把方向判死。
   - 单水体合同已验证：默认 map 不含 `WaterZone` / `WaterBodyRiver`，且唯一 `YarlungRiverSurface` 可见；后续不要恢复 WaterZone/WaterBodyRiver，也不要恢复旧 `YarlungRiverActor`。
   - 下一轮优先做山体/河岸：`slope-kitbash-v4` 已证明 authored rock-wall belt 能减少裸绿坡，但近景仍有重复/侧面读感，t30 仍露出 smooth corridor slope。继续方向应是更作者化的分段 rock-wall 模块、forest-floor/碎石贴坡 patch、或局部构图重排；不要把 spruce-half 当近景坡面补丁，也不要只继续堆 HISM 数量。
   - 水面当前 `water-lit-geometry-v3` 只是 partial：Default Lit + 顶点/几何能提供流向纹理，但无法达到真实反射/透明深度。下一刀应做材质级 flow normal / depth-fade / reflection，或正确接 UE `SingleLayerWaterMaterial` output node；不要再继续扩大几何波幅。
   - 截图验收继续以 `Saved\Diagnostics\<name>.png` 肉眼为准：水应沿峡谷凹槽、无遮挡/不穿石头、不是大块水平平板。
4. **主线（AAA）= 资产导入/资产路径修正，见 `docs/plans/aaa-asset-pipeline.md`**：
   - **【当前优先】** 先补 HISM/ISM 可用的整树针叶/冷杉/云杉 StaticMesh，或明确改成可使用现有树资产的 PCG/foliage actor 路径；否则山体只能停留在 branch-clump 近似。
   - **【当前优先】** 选择坡面友好的模块化 cliff/rock wall（多尺寸、可旋转、非单块巨板），沿峡谷两侧 kitbash；不要恢复旧 `YarlungCanyonWallActor`，也不要用大 slab 硬盖坡。
   - **【路线/轨道】** 默认路线已是 authored cross-river scenic S，并通过 4/4 river visibility；下一轮路线只做有明确视觉收益的英雄段精选/局部重排，不再回到旧 thalweg-offset 正弦硬弯。轨道视觉下一步是车体/座舱/安全杆、检修平台/扶手、支柱基础和更真实的轨枕/连接件。
   - 山体读成真实密林+灰绿湿岩后，再扩大轨道高低起伏、贴崖 overbank 或短翻滚；当前路线已经允许跨江和大起伏，只受 gate 约束。
   - 资产落地后 agent 全自动接线：Nanite 岩壁散布 → instanced/PCG 森林树冠（替换 branch-clump） → 多层地形材质 → 显式 river surface 江水升级 → authored cockpit/train → 成像收尾。
   - 不要恢复旧 `YarlungCanyonWallActor` / `YarlungRiverActor` 程序化分支；不要回到 square full-map fallback。
4. **验收要求**：每次必须打开 `Saved\Diagnostics\<name>.png` contact sheet，并把命令、manifest、risk gate、肉眼 verdict、下一步写回本文件。

## 独立外审索引

- `docs/reviews/2026-06-21-fail-close-setup-scan.md`：水 bug 暴露的是半 fail-open setup/gate 问题；本轮已修核心链路并新增 `scripts/test-yarlung.ps1`。
- `docs/reviews/2026-06-20-architecture-and-cleanup.md`：架构清理已执行多轮；剩余更激进项不要混进视觉迭代，除非单独验证。
- `docs/reviews/2026-06-20-bottleneck-misframed-strategic-pivot.md`：视觉瓶颈曾被纠偏为整体画面链路；当前仍需避免只烧在低收益 DEM-derived terrain 微调上。
- `docs/reviews/2026-06-19-nanite-mesh-terrain.md`：Nanite 替换基础成立，但单值 DEM-source / DEM 忠实 mesh 仍读成折面；不能把 C 标 Done。

## 历史与证据

- 完整旧进度账本：`docs/plans/archive/photoreal-progress-pre-p0p1-cleanup-2026-06-21.md`
- 迭代脚手架说明：`docs/plans/codex-iteration-scaffold.md`
- 画质基础读物：`docs/learn/rendering-basics.md`
- 本地视觉参考：`docs/refs/local/`（gitignored，license-unverified，不发布）
